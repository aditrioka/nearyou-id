// Per-configuration xcconfig for the `Prod Release` build configuration (mobile-ios-build-config-matrix).
// env values (Production.xcconfig — .nearyou.app + cobalt AppIcon + placeholder API) + the RELEASE Pods base.
// This is the App Store config; the only one resolving the cobalt production AppIcon.
#include "Production.xcconfig"
#include "../Pods/Target Support Files/Pods-iosApp/Pods-iosApp.prod release.xcconfig"
