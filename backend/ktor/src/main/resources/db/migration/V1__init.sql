-- Intentional placeholder. Real schema starts at V2.
-- This migration exists so Flyway has a baseline entry in flyway_schema_history
-- and so the migration pipeline is provably wired end-to-end.
SELECT 1;
