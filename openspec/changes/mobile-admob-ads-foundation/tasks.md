## 1. Substrate + preflight verification (do first)

- [ ] 1.1 Substrate re-check (docs/11 §1 + project.md library re-check): decide the Google Mobile Ads SDK pin — stable GMA SDK (default, native-ads-mature) vs the 2026 GMA Next-Gen SDK (smaller/faster, mandatory background init). Pin the **Android** Gradle SDK + UMP, AND the **iOS** CocoaPods (`Google-Mobile-Ads-SDK`, `GoogleUserMessagingPlatform`) versions in `gradle/libs.versions.toml` (the FirebaseMessaging/Sentry/RevenueCat pod-version-pin precedent). Add a `docs/09-Versions.md` rationale entry for each.
- [ ] 1.2 Pin Google's DOCUMENTED test ad-unit IDs for staging/debug only (Android native-advanced `ca-app-pub-3940256099942544/2247696110`, iOS native `ca-app-pub-3940256099942544/3986624511`) behind the build flavor; confirm a test native ad renders on a Firebase Test Lab device BEFORE wiring any real unit (hardcoded IDs drift — verify the upstream constants first).
- [ ] 1.3 File `follow-up` issues (label `follow-up` + `area:mobile`) for each explicitly-deferred PLACEMENT/format (iOS is NOT deferred — both actuals ship this slice): interstitials, profile-banner + chat-list placements, AppLovin MAX mediation. Record the issue numbers in the PR body AND back-fill them into the corresponding deferred requirements in `specs/mobile-ads/spec.md` (docs/12 §3 wants the tracking issue referenced in the requirement).

## 2. Backend — `ads-config` capability

- [x] 2.1 Add `AdsConfigService` reading `ads_enabled` via the Remote-Config→Redis seam (`remote_config:{flag:ads_enabled}`) with a per-flag 30–60 s TTL override (docs/11 §3.3); default `false` when absent. — `AdsEnabledFlagGate` (`{scope:remote_config}:{flag:ads_enabled}` cache key, 30s TTL, fail-closed FALSE — the `ImageUploadFlagGate` sibling) + `AdsConfigService`.
- [x] 2.2 Premium suppression in the service: return `ads_enabled = false` when the viewer's `subscription_status ∈ {premium_active, premium_billing_retry}` (the access-control `PREMIUM_STATES` set, NOT the `premium_active`-only badge formula — grace-period users keep the ad-free benefit, docs/08 item 4), regardless of the global flag.
- [x] 2.3 Add `AdsConfigRoutes` — thin authenticated `GET /api/v1/config/ads` (route parses/authenticates/responds, no SQL) returning `{ adsEnabled, timelineFrequency }` (camelCase wire per project DTO convention; `timelineFrequency` default 6, clamped 5–7); registered in `Application.kt` route table.
- [x] 2.4 `AdsConfigRoutesTest` (9 tests, all green) + `AdsEnabledFlagGateTest` (8 tests, all green): Free+flag ON → `adsEnabled true` + frequency in the 5–7 range (asserts the band, not the literal default); `premium_active`+flag ON → `false`; `premium_billing_retry` (grace)+flag ON → `false`; flag absent/false → `false`; unauthenticated → 401; short-TTL flag propagation in BOTH directions. DB-tagged, pool `autoClose(hikari())` size 2 (docs/11 §3.2 / PR #157).

## 3. `:infra:admob` module (vendor-SDK seam, invariant #16)

- [ ] 3.1 Create root-level KMP module `:infra:admob`; add to `settings.gradle.kts`. Verify it is NOT added to the backend Dockerfile COPY list (mobile-only infra; the settings↔Dockerfile CI guard must stay green — `:infra:revenuecat` mobile-module precedent).
- [ ] 3.2 commonMain: vendor-SDK-free `AdProvider` interface (initialize, request UMP consent + report state, load native ad → `NativeAdContent`, dispose) + plain models `NativeAdContent` / `ConsentState` / `AdRequestMode { PERSONALIZED, NON_PERSONALIZED }`.
- [ ] 3.3 androidMain actual: Google Mobile Ads + UMP SDK — init, UMP consent info request + form presentation when required, native-ad load → `NativeAdContent`; force non-personalized (`npa`) request when `AdRequestMode.NON_PERSONALIZED`.
- [ ] 3.4 iosMain actual (D7, full parity): apply the `native.cocoapods` plugin to `:infra:admob` and declare `pod("Google-Mobile-Ads-SDK")` + `pod("GoogleUserMessagingPlatform")` so the plugin generates the cinterop (the GoogleSignIn-pod precedent). Implement init, UMP consent info request + form presentation, native-ad load → `NativeAdContent`, and the non-personalized request path over the generated bindings. Handle ObjC **category** members with explicit `import platform.<Framework>.<symbol>` (docs/11 §2.5) and verify with `linkDebugFrameworkIosSimulatorArm64` locally (Linux CI can't catch cinterop/category errors).
- [ ] 3.5 `mobile/app` cocoapods block: add the same `pod("Google-Mobile-Ads-SDK")` + `pod("GoogleUserMessagingPlatform")` with `linkOnly = true` so the final `ComposeApp` framework links the Pod binaries (the Sentry/RevenueCat `linkOnly` precedent — the `:infra:admob` klib owns the bindings). Bind `AdProvider` in the Koin platform modules (real on both Android + iOS); vendor dep `implementation`-scoped so it never reaches the `:mobile:app` compile classpath.
- [ ] 3.6 **Add** a new clause to `lint/detekt-rules/.../VendorSdkLeakageScanTest.kt` enforcing invariant #16 for the Ads SDK: a `roots = [mobile/app/src]` × `prefixes = ["com.google.android.gms.ads.", "com.google.android.ump."]` block (mirroring the existing `io.sentry.` / `com.revenuecat.` mobile blocks). Use the PRECISE `com.google.android.gms.ads.` prefix — a bare `com.google.android.gms.` would over-match legitimate Play-services client deps. The pre-existing scan does NOT cover Ads, so "stays green" without this clause is a no-op; the new clause must go red if a GMA/UMP import appears in `:mobile:app`.

## 4. Mobile — `mobile-ads` consumption

- [ ] 4.1 `AdsConfigApiClient` + `AdsConfigRepository` (GET `/api/v1/config/ads`) with a sealed outcome; a fetch failure resolves to ads-OFF (fail-safe, never error chrome).
- [ ] 4.2 Ad-eligibility wiring: initialize `AdProvider` + run the UMP gate ONLY when `ads_enabled = true`; read the stored `ads_personalization` (via `ConsentSnapshotStore`) and map to `AdRequestMode` (OFF/declined → NON_PERSONALIZED).
- [ ] 4.3 Sealed `FeedItem { Post | NativeAd }`; interleave a native-ad slot every `timeline_frequency` posts in Nearby/Following/Global via the canonical `PostFeedList<T>` seam (docs/11 §2.1) with stable `key` + `contentType` (§2.4). Post fetch/order unchanged.
- [ ] 4.4 Native-ad card in `ui/components` reusing `PostCard` geometry + a "Bersponsor" label — consult the mockup board feed-card frame (docs/11 §2.8) and translate to Compose M3 idioms (tokens, not literals).
- [ ] 4.5 Add the "Bersponsor" (+ any UMP-prompt) string(s) to `:shared:resources` (no hardcoded UI string — invariant); import each key explicitly.
- [ ] 4.6 `AdProvider` lifecycle: load ads off the composition path, dispose on feed-item disposal / `onCleared`.
- [ ] 4.7 **Data minimization (UU PDP, docs/01:173 / docs/06)**: the `:infra:admob` ad request SHALL NOT pass precise device coordinates to the SDK — disable the GMA location signal / do not set a precise `location` on the request (city-level only, if any). Verified by a negative-guard test (5.x) asserting no precise coordinate reaches the request builder.

## 5. Tests + verification (DoD, docs/11 §5)

- [ ] 5.1 commonTest: `AdsConfigRepository` fail-safe (fetch fail → ads OFF); `ads_personalization` → `AdRequestMode` mapping (OFF→NON_PERSONALIZED, ON+consent→PERSONALIZED); `FeedItem` interleave math — slot every N; feed < N → none; **feed exactly == N boundary** (off-by-one: define + assert whether a trailing slot appears); **a ≥2N feed yields ≥2 slots at the correct indices with DISTINCT stable keys** (duplicate `key`s crash LazyColumn, §2.4); order preserved.
- [ ] 5.2 commonTest with a **fake `AdProvider`**: UMP gate ordering — consent is requested BEFORE any native-ad load when ads enabled; a UMP/consent failure yields no ads (fail-safe); **the ad request carries no precise coordinate** (data-minimization, task 4.7 — assert the request the provider receives has no precise lat/lng). (Backs the "Consent form shown when required", "Consent flow failure shows no ads", and data-minimization scenarios; the seam is fakeable since `:mobile:app` depends on the interface.)
- [ ] 5.3 Robolectric `*ScreenTest`: Free+enabled → native-ad slot present with the localized "Bersponsor" label, **rendered from the injected `AdProvider` / `NativeAdContent` seam** (assert the node is bound to the provider's content, not just "a labelled slot exists" — this is the positive anchor for the "app consumes ads only via the interface" scenario); premium or flag-OFF or config-fetch-fail → no slot. **Negative guards**: no interstitial node on app-open/post-submit; no ad node on the profile, conversation-list, or chat-thread screens. Add to the Release-variant exclude + verify `:mobile:app:testDevReleaseUnitTest` (docs/11 §2.7).
- [ ] 5.4 iOS verify (parity): `linkDebugFrameworkIosSimulatorArm64` green after the cinterop work (the cocoapods/category compile gate, §2.5); an `iosSimulatorArm64Test` (kotlin.test `@Test` — kotest commonTest doesn't run on K/N) asserting the iOS `AdProvider` constructs + the gating/`AdRequestMode` mapping holds; a manual simulator/device ad-render smoke for the live native-ad slot (iOS is not CI-gated — manual evidence in the PR body). Backs the "iOS serves ads through the cinterop actual" + "both actuals honor the shared gating" scenarios.
- [ ] 5.5 Structural scenarios verified by inspection/lint, not unit tests (mapped, not dropped): SDK-leak absence + app-consumes-via-interface → the `vendor-sdk-leakage-scan` (task 3.6); no-mediation-adapter → diff/dependency inspection; `ads-config` adds no migration → no `V*.sql` in the diff + the existing Flyway parity check.
- [ ] 5.6 Android Firebase Test Lab: a test native ad renders in a live feed (`scripts/test_android.sh` / `scripts/run_on_device.sh`) — capture screenshot/video as manual-verification evidence for the PR body (docs/11 §5 DoD).
- [ ] 5.7 Pre-push gate green: `./gradlew ktlintCheck detekt :backend:ktor:test :lint:detekt-rules:test`.

## 6. Docs + module bookkeeping

- [ ] 6.1 Add `:infra:admob` to `dev/module-descriptions.txt` + run `dev/scripts/sync-readme.sh --write` (root README module-list invariant).
- [ ] 6.2 docs/09 SDK-pin entry (from 1.1). **Amend docs/01 § KMP Integration (line ~177)**: the `AdProvider` interface home moves from `:core:data` to the new vendor-fenced `:infra:admob` module (per docs/11 §2.6 vendor-SDK seam — the `:infra:revenuecat` / `:infra:supabase-realtime` precedent); this is an intentional reconciliation, declared in design.md D1. Note the `ads-config` backend test lane in docs/13 if a new lane is added.
- [ ] 6.3 No docs/11 § Pattern Registry amend (this change follows the existing state/list/expect-actual/vendor-`:infra`/backend-layering patterns — no deviation); confirm the design's Standards-conformance note matches the shipped code at archive.
