# content-moderation-keyword-lists — Tasks

## 1. External-data sanity checks (gate before any coding)

- [ ] 1.1 Verify that the staging Firebase project has the three Remote Config parameters reserved per Pre-Phase 1 §36 (`moderation_profanity_list`, `moderation_uu_ite_list`, `moderation_match_threshold`) — if absent, create them with empty/default values via Firebase Console BEFORE wiring the loader (otherwise integration tests against staging fail with `parameter_not_found` ambiguously instead of the spec'd cascade).
- [ ] 1.2 Verify that the staging GCP Secret Manager has `staging-content-moderation-fallback-list` slot reserved — if absent, document the gap in `FOLLOW_UPS.md` (slot creation + sentinel JSON value is operations work, not in-code work; spec scenarios still verify the cascade independent of slot presence via fixture).

(The Firebase Admin SDK Remote Config Java surface — `FirebaseRemoteConfig.getInstance(app).getTemplate().get()` — is the canonical path established by the existing `:infra:fcm` consumer; SDK-surface verification is a developer concept-check, not external-data, and is covered implicitly by Phase 3 implementation work + tests. The original task 1.1 covering this was removed in proposal-review round 1.)

## 2. `KeywordMatcher` in `:core:domain`

- [ ] 2.1 Add new file `core/domain/src/main/kotlin/id/nearyou/app/domain/moderation/KeywordMatcher.kt` containing the pure-Kotlin Aho-Corasick implementation per `specs/content-moderation-keyword-lists/spec.md` `### Requirement: KeywordMatcher is a pure-Kotlin Aho-Corasick engine in :core:domain`. Pure functions, no I/O, no vendor deps. Public surface: `data class MatchResult(matchedKeywords: List<String>, matchCount: Int)` + top-level `object KeywordMatcher` with `fun match(content: String, keywords: List<String>): MatchResult`.
- [ ] 2.2 Implement word-boundary detection helper (whitespace + `[.,!?;:()\[\]{}]` + start/end-of-string per spec; reuse a single immutable `Set<Char>` for boundary chars — case-insensitive comparison via `.lowercase(Locale.forLanguageTag("id"))` for ID-locale correctness on Indonesian text).
- [ ] 2.3 Implement Aho-Corasick automaton construction over the keyword list. No internal caching — build a fresh automaton per call (sub-millisecond cost for ~50 patterns; spec contract per round-1 review tightening). A future memoization layer is reserved if p99 profiling shows the rebuild matters.
- [ ] 2.4 Implement scan: walk content once through the automaton, collect distinct keyword hits (skip suffix-link emissions of duplicates of an already-collected keyword), respect word-boundary on both sides of every emitted match. Return `MatchResult` with `matchedKeywords` in first-occurrence order.
- [ ] 2.5 Add unit tests at `core/domain/src/test/kotlin/id/nearyou/app/domain/moderation/KeywordMatcherTest.kt` covering all 8 scenarios in `specs/content-moderation-keyword-lists/spec.md` `### Requirement: KeywordMatcher is a pure-Kotlin Aho-Corasick engine in :core:domain`. Use Kotest `StringSpec` style.
- [ ] 2.6 Add edge-case unit tests not in spec scenarios but worth covering: (a) keyword that IS the entire content (`match("foo", listOf("foo"))` → 1 hit), (b) overlapping keywords (`["abc", "bcd"]` against `"abcd"` — both fire under Aho-Corasick), (c) Indonesian non-ASCII characters (`"halo dünia"` lowercase preserves diacritic), (d) very long content (10k chars + 50 keywords) — confirms no quadratic blowup.
- [ ] 2.7 Add a Detekt-rules guard against introducing a vendor Aho-Corasick library: extend `lint/detekt-rules` with a check that `:core:domain`'s `build.gradle.kts` does NOT declare `org.ahocorasick:*` or `com.hankcs:*` dependencies. Defer to follow-up if the existing detekt-rules layout makes this awkward (it's a belt-and-suspenders rule — the spec scenario covers the contract).

## 3. Scaffold `:infra:remote-config` module

- [ ] 3.1 Create directory `infra/remote-config/` with `build.gradle.kts` mirroring [`infra/fcm/build.gradle.kts`](../../../infra/fcm/build.gradle.kts) — declare `kotlin("jvm")` plugin, `implementation(libs.firebase.admin)` (BOM-managed via existing `9.5.0` pin in [`gradle/libs.versions.toml`](../../../gradle/libs.versions.toml)), `implementation(libs.kotlinx.serialization.json)`, the standard test dependencies. NO Koin / Ktor dependencies (the module exposes pure-data interfaces consumed by `:backend:ktor`).
- [ ] 3.2 Add `include(":infra:remote-config")` to [`settings.gradle.kts`](../../../settings.gradle.kts) alongside the other `:infra:*` includes.
- [ ] 3.3 Add a one-line entry to [`dev/module-descriptions.txt`](../../../dev/module-descriptions.txt) describing `:infra:remote-config` (e.g., `:infra:remote-config — Firebase Remote Config client (platform-wide tunable config)`). Run `dev/scripts/sync-readme.sh --write` to regenerate the README module list per [`CLAUDE.md`](../../../CLAUDE.md) "Root README module list is auto-generated" maintenance trigger.
- [ ] 3.4 Implement `infra/remote-config/src/main/kotlin/id/nearyou/app/infra/remoteconfig/FirebaseAdminInitForRemoteConfig.kt` — a singleton initializer that calls `FirebaseApp.initializeApp(options, "nearyou-rc")` with `options` built from a `GoogleCredentials.fromStream(...)` over the service-account JSON. Mirror [`infra/fcm/.../FirebaseAdminInit.kt`](../../../infra/fcm/src/main/kotlin/id/nearyou/app/infra/fcm/FirebaseAdminInit.kt) — same idempotent-init pattern, distinct app name `"nearyou-rc"` to avoid collision with `"nearyou-default"`.
- [ ] 3.5 Implement `infra/remote-config/src/main/kotlin/id/nearyou/app/infra/remoteconfig/RemoteConfigClient.kt` — public interface with `fun fetchStringList(parameterName: String): List<String>?` returning null on parameter-missing (caller treats as fallback signal), `fun fetchInt(parameterName: String): Int?` for the threshold parameter, `fun fetchBoolean(parameterName: String): Boolean?` (reserved for future kill-switch use cases). Implementation calls `FirebaseRemoteConfig.getInstance(firebaseApp).getTemplate().get()` — extracts `Parameter.getDefaultValue().getValue()` (a JSON string for arrays, a numeric string for ints) — parses via kotlinx.serialization. NO Firebase Admin SDK types leak into the public interface.
- [ ] 3.6 Add unit tests at `infra/remote-config/src/test/kotlin/id/nearyou/app/infra/remoteconfig/RemoteConfigClientTest.kt` covering: successful string-array fetch, parameter-missing returns null, malformed JSON returns null + logs WARN, int parse + clamp, boolean parse, named-FirebaseApp isolation (`"nearyou-rc"` distinct from `"nearyou-default"`).
- [ ] 3.7 Add `:backend:ktor` `build.gradle.kts` dependency: `implementation(project(":infra:remote-config"))`. Verify no `import com.google.firebase.remoteconfig` appears in any `:backend:ktor` source file (per the spec scenario "`:backend:ktor` source files contain no Firebase Remote Config import").

## 4. `ModerationListLoader` in `:backend:ktor`

- [ ] 4.1 Add new package `backend/ktor/src/main/kotlin/id/nearyou/app/moderation/` with the `ModerationListLoader` interface + implementation. Define `enum class ModerationList { ProfanityList, UuIteList }` + `interface ModerationListLoader { fun load(list: ModerationList): List<String>; fun loadThreshold(): Int }`.
- [ ] 4.2 Inject `RemoteConfigClient` from `:infra:remote-config` via Koin into `ModerationListLoader`. The loader's Tier 2 implementation calls `remoteConfigClient.fetchStringList("moderation_profanity_list")` (etc.); the Firebase Admin SDK plumbing is encapsulated in `:infra:remote-config`.
- [ ] 4.3 Implement Tier 1 (Redis cache) read + write. Key shape exactly `{scope:mod_list}:{tier:profanity}` / `{tier:uu_ite}` / `{tier:threshold}`. Reuse the existing Lettuce `RedisClient` from `:infra:redis`. JSON serialize via kotlinx.serialization (existing project standard).
- [ ] 4.4 Implement Tier 2 (Remote Config via `RemoteConfigClient`) parse + fallback emit. On null return from the client / empty payload, log Sentry WARN per `### Requirement: ModerationListLoader resolves keyword lists via the canonical 4-tier fallback ladder` and proceed.
- [ ] 4.5 Implement Tier 3 (repo file) read via `ClassLoader.getResourceAsStream("/moderation/profanity.default.txt")`. Trim whitespace, drop empty + comment lines (`#` prefix). On null stream (resource missing), log Sentry WARN and proceed.
- [ ] 4.6 Implement Tier 4 (Secret Manager) read via `secretResolver.resolve(secretKey(env, "content-moderation-fallback-list"))`. Parse JSON `{"profanity": [...], "uu_ite": [...]}` shape. On null/parse-error, log Sentry ERROR with `event = "moderation_list_unavailable"`, return `emptyList()`.
- [ ] 4.7 Implement at-most-one-Sentry-event-per-`load()`-call rate limiting (per `### Requirement: Tier-fallback Sentry events emit at most once per load(list) call`). Track within-call cascade state; emit the most-severe terminal event only.
- [ ] 4.8 Implement `loadThreshold()` with the same 4-tier shape but using `remoteConfigClient.fetchInt("moderation_match_threshold")` for Tier 2. Default 3 on cascade-to-bottom. Clamp to `[1, 10000]` per `### Requirement: ModerationMatchThresholdLoader resolves the threshold via the same Remote Config + fallback path`.
- [ ] 4.9 Add Koin module wiring in `backend/ktor/src/main/kotlin/id/nearyou/app/config/AppKoinModule.kt` (or wherever the canonical singleton wiring lives) to bind `ModerationListLoader` as a singleton.
- [ ] 4.10 Add integration tests at `backend/ktor/src/test/kotlin/id/nearyou/app/moderation/ModerationListLoaderTest.kt` covering the 7 cascade scenarios in the spec (Tier 1 hit, Tier 2 success, Tier 2 fail → Tier 3 success, Tier 2 empty → Tier 3 success, Tier 3 missing → Tier 4 success, all four fail → emptyList + ERROR, secret-key-helper enforcement). Use Lettuce against the Postgres+Redis test container; mock `RemoteConfigClient` + `SecretResolver`.

## 5. Repo-committed default fallback wordlists

- [ ] 5.1 Create `backend/ktor/src/main/resources/moderation/profanity.default.txt` with a header comment block (`# Profanity fallback list — see docs/06-Security-Privacy.md §155`) + a placeholder sentinel line `__seed_profanity_placeholder__`. Real production wordlist lives in Firebase Remote Config; this file is a fail-soft fallback.
- [ ] 5.2 Create `backend/ktor/src/main/resources/moderation/uu_ite.default.txt` with the same header convention + placeholder sentinel `__seed_uu_ite_placeholder__`.
- [ ] 5.3 Add a boot-time integrity check in `Application.module()` (or near it): on startup, the loader's Tier 3 read MUST succeed for both lists OR a Sentry WARN + INFO log line is emitted. Implementation: invoke `loader.load(ProfanityList)` + `loader.load(UuIteList)` once at boot in a non-blocking coroutine; the first invocation primes the cache + verifies ladder integrity; failures land in Sentry pre-traffic.
- [ ] 5.4 Document the wordlist update process in `docs/07-Operations.md` § Moderation runbook (new section if absent): operator updates Remote Config → 5-min TTL elapse → new list active. Quarterly UU ITE legal review per `docs/06-Security-Privacy.md:159` updates BOTH the Remote Config parameter AND the repo `.default.txt` (so a Remote Config outage falls back to a recent-and-vetted list).

## 6. `TextModerator` orchestrator

- [ ] 6.1 Add `backend/ktor/src/main/kotlin/id/nearyou/app/moderation/TextModerator.kt` implementing the `Verdict` sealed interface + the `moderate(content: String): Verdict` orchestration per `specs/content-moderation-keyword-lists/spec.md` `### Requirement: TextModerator.moderate(content) orchestrates loader + matcher into a Verdict`.
- [ ] 6.2 Implement profanity-precedence short-circuit: profanity match returns `Reject` immediately, UU ITE list + threshold are NOT loaded. Verifiable via a fixture that primes the loader with a profanity hit + records call counts on the loader's UU ITE method (which should be zero).
- [ ] 6.3 Implement Sentry breadcrumb emission for `Reject` and `Flag` outcomes per `### Requirement: TextModerator Sentry events do NOT carry user content`. Payload includes `event`, `verdict_kind`, `matched_keyword_count`. Payload does NOT include the matched keywords list, the original content, or any user identifier.
- [ ] 6.4 Add Koin singleton wiring for `TextModerator`.
- [ ] 6.5 Add unit tests at `backend/ktor/src/test/kotlin/id/nearyou/app/moderation/TextModeratorTest.kt` covering the 5 spec scenarios (single profanity → Reject; UU ITE below threshold → Allow; UU ITE at threshold → Flag; UU ITE above threshold → Flag; both lists empty → Allow; profanity precedence over UU ITE).
- [ ] 6.6 Add Sentry-payload-content tests verifying that for both `Reject` and `Flag` outcomes, the captured Sentry event does NOT contain the original content string OR the matched keyword strings (use a sentinel like `"sentinel-content-DO-NOT-LEAK"` per existing `observability-otel-foundation` test pattern).

## 7. Integration: `POST /api/v1/posts`

- [ ] 7.1 Locate the canonical post-creation handler (likely `backend/ktor/.../post/PostRoutes.kt` + `PostService`). Inject `TextModerator` via Koin into the service.
- [ ] 7.2 Wire `TextModerator.moderate(content)` AFTER the existing length guard, BEFORE the INSERT, per the call-order requirement in `specs/post-creation/spec.md`.
- [ ] 7.3 Map `Verdict.Reject` to HTTP 400 with the existing error envelope: `error.code = "content_moderated_profanity"`, `error.message = "Konten ini mengandung kata yang tidak diperbolehkan. Silakan ubah dan coba lagi."`. Sanity-check against the existing error envelope pattern from `### Requirement: Error envelope matches existing auth routes`.
- [ ] 7.4 Map `Verdict.Flag` to: proceed with `INSERT INTO posts ...` AND in the same SQL transaction issue `INSERT INTO moderation_queue (target_type, target_id, trigger, priority, status) VALUES ('post', :new_post_id, 'uu_ite_keyword_match', 5, 'pending') ON CONFLICT (target_type, target_id, trigger) DO NOTHING`. Response 201 unchanged.
- [ ] 7.5 Map `Verdict.Allow` to: existing INSERT path unchanged; no queue row. (Smoke regression for the 99% path.)
- [ ] 7.6 Add integration tests at `backend/ktor/src/test/kotlin/id/nearyou/app/post/PostCreationModerationIntegrationTest.kt` covering all 7 scenarios in `specs/post-creation/spec.md` (profanity → 400 + no INSERT; UU ITE flag → 201 + queue row + is_auto_hidden=FALSE; UU ITE below threshold → 201 + no queue; allowed → 201 + no queue; flag transaction atomicity; idempotent retry; Reject body keyword leak).
- [ ] 7.7 Add the static-call-order assertion test (per `### Requirement: Moderator runs AFTER length guard and BEFORE INSERT (call order)` scenario "Static analysis confirms call order"): verify the handler source lists `contentLengthGuard` then `TextModerator.moderate(...)` then `INSERT` in that order. Reuse the static-source-scan test pattern from [`backend/ktor/src/test/kotlin/id/nearyou/app/notifications/FcmDispatchStructuralTest.kt`](../../../backend/ktor/src/test/kotlin/id/nearyou/app/notifications/FcmDispatchStructuralTest.kt) — `File("src/main/kotlin/...").readText()` substring scans for canonical method names + relative `indexOf` ordering checks. The test asserts (a) the moderator import is present, (b) `TextModerator.moderate` appears in the handler source, (c) `TextModerator.moderate`'s `indexOf` is ≥ the length-guard symbol's `indexOf` AND ≤ the INSERT/`postRepository.create` symbol's `indexOf`. The Kotlin DSL with closures may make exact relative-position checks brittle across nested lambdas — if so, downgrade to "moderator import + symbol presence" assertions and rely on reviewer attestation for ordering, per the format-lens H1 review note.
- [ ] 7.8 Add the oversized-payload-no-moderator-call test verifying that 281+ char content short-circuits BEFORE `TextModerator.moderate(...)` is invoked (mock-spy assertion).

## 8. Integration: `POST /api/v1/posts/{post_id}/replies`

- [ ] 8.1 Locate the canonical post-replies handler. Inject `TextModerator`.
- [ ] 8.2 Wire `TextModerator.moderate(...)` AFTER existing rate limit + visibility resolution + length guard, BEFORE INSERT (per call-order requirement in `specs/post-replies/spec.md`).
- [ ] 8.3 Map `Verdict.Reject` to HTTP 400 + canonical envelope (same code/message as posts). NO `post_replies` row, NO notification, NO queue row.
- [ ] 8.4 Map `Verdict.Flag` to: INSERT proceeds; in same transaction insert `moderation_queue` row with `target_type='reply'`, `target_id=<new_reply_id>`, `trigger='uu_ite_keyword_match'`, `ON CONFLICT DO NOTHING`. The `post_replied` notification IS still emitted (per spec, Flag is "soft-flag, normal user-visible flow").
- [ ] 8.5 Map `Verdict.Allow` to: existing INSERT + notification path unchanged.
- [ ] 8.6 Add integration tests at `backend/ktor/src/test/kotlin/id/nearyou/app/post/PostRepliesModerationIntegrationTest.kt` covering all 5 scenarios in `specs/post-replies/spec.md`.
- [ ] 8.7 Add the rate-limit-precedence test: caller A at 21 attempts gets 429 (NOT 400-content-moderated), and `TextModerator.moderate(...)` is not called for the 21st request.

## 9. Integration: `POST /api/v1/chat/{conversation_id}/messages`

- [ ] 9.1 Locate the canonical chat send handler. Inject `TextModerator`.
- [ ] 9.2 Wire `TextModerator.moderate(...)` AFTER existing rate limit + conversation/participant lookup + block check + length guard, BEFORE INSERT, BEFORE `ChatRealtimeClient.publish(...)` (per call-order requirement in `specs/chat-conversations/spec.md`).
- [ ] 9.3 Map `Verdict.Reject` to HTTP 400 + canonical envelope. NO `chat_messages` row, NO `last_message_at` update, NO broadcast, NO notification, NO queue row.
- [ ] 9.4 Map `Verdict.Flag` to: INSERT proceeds; in same transaction insert `moderation_queue` row with `target_type='chat_message'`, `target_id=<new_message_id>`, `trigger='uu_ite_keyword_match'`, `ON CONFLICT DO NOTHING`. Broadcast + notification + `last_message_at` update proceed normally.
- [ ] 9.5 Add integration tests at `backend/ktor/src/test/kotlin/id/nearyou/app/chat/ChatModerationIntegrationTest.kt` covering all 4 scenarios in `specs/chat-conversations/spec.md` (the embedded-post bypass scenario was dropped in proposal-review round 1: the existing canonical send-message endpoint requires `content` 1–2000 chars per [`openspec/specs/chat-conversations/spec.md`](../../../openspec/specs/chat-conversations/spec.md) `### Requirement: Send-message endpoint`; embedded-only sends are deferred to a future `chat-embedded-posts` capability; the bypass branch is unreachable in the current API and will be reintroduced in that change).
- [ ] 9.6 Add the block-check-precedence test: blocked caller gets 403 (NOT 400-content-moderated), and `TextModerator.moderate(...)` is not invoked.
- [ ] 9.7 Add the broadcast-rollback-on-INSERT-failure test: when INSERT fails on a Flag verdict, `ChatRealtimeClient.publish(...)` is NOT called (broadcast happens post-COMMIT).

## 10. Detekt rule (optional, defer if review-loop iterations consume the budget)

- [ ] 10.1 Add `lint/detekt-rules/src/main/kotlin/id/nearyou/lint/ContentWriteRequiresModerationRule.kt` detecting any function that calls `INSERT INTO posts ... content`, `INSERT INTO post_replies ... content`, or `INSERT INTO chat_messages ... content` without first calling `TextModerator.moderate(...)` in the enclosing function. Annotation `// @allow-content-write-without-moderation: <reason>` on the enclosing function suppresses the violation. Recognized reasons: `tombstone`, `admin_redaction`, `seed`.
- [ ] 10.2 Wire the new rule into `lint/detekt-rules` config + the `:lint:detekt-rules` test harness.
- [ ] 10.3 Add unit tests at `lint/detekt-rules/src/test/kotlin/id/nearyou/lint/ContentWriteRequiresModerationRuleTest.kt` covering: (a) handler with moderate-then-INSERT passes, (b) handler with INSERT-without-moderate fails, (c) annotated allow-list cases pass, (d) admin path passes (admin module is exempt).
- [ ] 10.4 Decision gate: if review-loop iterations near the 2-round cap (per `/next-change` skill Phase D step 14), DEFER tasks 10.1–10.3 to a follow-up change `content-write-moderation-detekt-rule` with a `FOLLOW_UPS.md` entry. The spec contract (call order requirement) is reviewer-checked even without the lint rule; the rule is defense-in-depth.

## 11. Validation, smoke, archive

- [ ] 11.1 Run `openspec validate content-moderation-keyword-lists --strict` and ensure zero errors.
- [ ] 11.2 Run `./gradlew ktlintCheck detekt :backend:ktor:test :core:domain:test :infra:remote-config:test :lint:detekt-rules:test` locally and ensure full green BEFORE pushing the feat commits (per `CLAUDE.md` § Pre-push verification).
- [ ] 11.3 Create the staging smoke script at `dev/scripts/smoke-content-moderation-keyword-lists.sh` (executable bash) covering: (a) POST a profanity-sentinel post → expect 400 + `code = "content_moderated_profanity"`; (b) POST a UU-ITE-sentinel post (3 hits) → expect 201 + DB query confirms `moderation_queue` row with `target_type = 'post'`, `trigger = 'uu_ite_keyword_match'`; (c) POST an allowed post → expect 201 + no queue row; (d) repeat (a)+(b) for `/api/v1/posts/{post_id}/replies` and `/api/v1/chat/{conversation_id}/messages`.
- [ ] 11.4 Manual staging deploy: `gh workflow run deploy-staging.yml --ref content-moderation-keyword-lists`. Poll deploy run; on success, prime staging Remote Config with sentinel `moderation_profanity_list = ["sentinel-profanity"]`, `moderation_uu_ite_list = ["sentinel-uuite-1", "sentinel-uuite-2", "sentinel-uuite-3"]`, `moderation_match_threshold = 3`.
- [ ] 11.5 Run `dev/scripts/smoke-content-moderation-keyword-lists.sh` against the staging branch deploy. Verify Sentry shows the boot-time loader-priming WARN/INFO events for the placeholder fallback files.
- [ ] 11.6 Verify the fail-open path: temporarily clear staging `moderation_profanity_list` Remote Config to `[]` → POST a profanity-sentinel → expect 201 (fail-open works because empty list = no possible matches). Restore Remote Config; verify next request after 5-min TTL elapse rejects the same content.
- [ ] 11.7 Tick relevant tasks in this list as they ship; update PR body with progress per `CLAUDE.md` § PR title and body MUST stay current.
- [ ] 11.8 At end of feat phase, retitle PR via `gh pr edit <pr> --title 'feat(moderation): content-moderation-keyword-lists'` (or whatever conventional-commit prefix dominates) and update body to reflect implementation status.
- [ ] 11.9 Run `/opsx:archive` once feat phase is complete and smoke is green to push the archive commit + spec sync.

## 12. Follow-ups + canonical-doc reconciliation

The 4 canonical-doc divergences identified during Phase B step 7 (reconciliation) are already captured in [`FOLLOW_UPS.md`](../../../FOLLOW_UPS.md) entry `content-moderation-canonical-doc-amendments`, added during proposal authoring. The implementation tasks below add additional FOLLOW_UPS entries that are forward-looking (architectural extraction, deferred features) rather than reconciliation items.

- [ ] 12.1 Verify [`FOLLOW_UPS.md`](../../../FOLLOW_UPS.md) entry `content-moderation-canonical-doc-amendments` is still present + reflects the actually-shipped layer semantics. If implementation diverges from the spec'd Layer 1/2/3 split, update the entry.
- [ ] 12.2 Add `FOLLOW_UPS.md` entry: future `:infra:firebase-app` extraction — both `:infra:fcm` and `:infra:remote-config` currently initialize their own named FirebaseApp from the same `firebase-admin-sa` secret. A shared `:infra:firebase-app` module that owns the FirebaseApp init + is depended on by both would be the cleanest factoring. Out of scope here; logged for when a third Firebase consumer arrives or when the duplication becomes painful.
- [ ] 12.3 Add `FOLLOW_UPS.md` entry: deferred follow-up `text-moderation-cache-invalidation-endpoint` for an OIDC-authed `POST /internal/moderation-list-bust` endpoint that clears the Redis cache on operator-edit — only if Month 3+ data shows the 5-min TTL is too stale.
- [ ] 12.4 Add `FOLLOW_UPS.md` entry: deferred follow-up `perspective-api-stopgap` (Phase 2 §16) — uses `TextModerator` boundary as foundation + adds async 500ms-timeout fail-open layer + writes `trigger = 'perspective_api_high_score'`.
- [ ] 12.5 If task 10 was deferred, add `FOLLOW_UPS.md` entry: `content-write-moderation-detekt-rule` per task 10.4.
