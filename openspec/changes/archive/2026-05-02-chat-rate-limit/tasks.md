## 1. Discovery & infra verification

- [x] 1.1 Verified during proposal reconciliation: Free 50/day, Premium unlimited, no burst — consistent at `02-Product.md:318` (canonical) and `05-Implementation.md:1741` (Layer 2 table). `chat-foundation` proposal (PR #61) § Out of scope explicitly named `chat-rate-limit` as a follow-up consumer of `rate-limit-infrastructure`.
- [x] 1.2 Verified: `openspec/specs/rate-limit-infrastructure/spec.md` confirms the `RateLimiter` interface contract (`tryAcquire(userId, key, capacity, ttl): Outcome`, `releaseMostRecent(userId, key)`) and `computeTTLToNextReset` placement in `id.nearyou.app.core.domain.ratelimit`.
- [x] 1.3 Verified chat-foundation send route ordering by reading [`ChatRoutes.kt:176-225`](backend/ktor/src/main/kotlin/id/nearyou/app/chat/ChatRoutes.kt) + [`ChatService.kt`](backend/ktor/src/main/kotlin/id/nearyou/app/chat/ChatService.kt): current sequence is auth → UUID → JSON parse → content guard → `service.sendMessage()` (which throws `ConversationNotFoundException` / `NotParticipantException` / `BlockedException` for the 404/403/403 paths) → 201. Note: the chat-foundation route runs JSON parse + content guard at the route layer BEFORE invoking `sendMessage`, vs the design.md ordering which describes participant/block check before content guard. The route-layer ordering is functionally equivalent because `sendMessage` then throws the exceptions that map to 404/403 — but for the rate-limit insertion point, the limiter MUST run BEFORE both the JSON parse AND the `sendMessage` call. The new chat send route ordering (per spec): auth → UUID → **rate limiter** → JSON parse → content guard → `sendMessage()` → 201.
- [x] 1.4 Verified: `:infra:redis` module exists. `.github/workflows/ci.yml:152` declares `redis:7-alpine` service container with `REDIS_URL=redis://localhost:6379` at line 176. No new CI infra needed.
- [x] 1.5 Verified: `UserPrincipal.subscriptionStatus: String` exists at [`AuthPlugin.kt:34`](backend/ktor/src/main/kotlin/id/nearyou/app/auth/AuthPlugin.kt:34). `grep -rn "RevenueCat\|revenuecat" backend/ktor/src/main/` returns 3 hits — all in documentation comments (`V10__notifications.sql` notification type comment, `Application.kt` future-webhook comment, `SearchService.kt` KDoc reference). Zero handler-code matches, confirming the RevenueCat webhook handler is Phase 4 work per `docs/08-Roadmap-Risk.md`. The existing `auth-jwt-spec-debt-userprincipal-subscription-status` `FOLLOW_UPS.md` line item still tracks the principal-field documentation debt; scope unchanged.
- [x] 1.6 **Deferred to manual user verification** (filed in commit body as known unverified-at-merge). Mobile chat-compose 429 handling lives in Compose Multiplatform UI code outside this scope; backend 429 contract is correct regardless and the generic timeline rate-limit screen catches uncaught 429s.

## 2. `premium_chat_send_cap_override` Remote Config flag plumbing

- [x] 2.1 Added `PREMIUM_CHAT_SEND_CAP_OVERRIDE_KEY = "premium_chat_send_cap_override"` constant to `ChatService.companion` ([ChatService.kt](backend/ktor/src/main/kotlin/id/nearyou/app/chat/ChatService.kt)) alongside `DEFAULT_DAILY_CAP=50`, `MAX_OVERRIDE_CAP=10_000L`, `PREMIUM_STATES`, `dailyKey()`. Mirrors `ReplyService` placement byte-for-byte.
- [x] 2.2 Verified: `RemoteConfig.getLong("premium_chat_send_cap_override")` resolves through the existing key-agnostic interface (same path as the like + reply flags). `StubRemoteConfig` returns null for any key, so the unset-fallback path is exercised by default in dev/test.
- [x] 2.3 KDoc on `ChatService.resolveDailyCap` documents the mirror semantics ("Mirrors `ReplyService.resolveDailyCap` byte-for-byte semantics — same fallback ladder, same audit-log shape, same upper-bound clamp threshold at 10,000.").

## 3. Apply rate limit to `POST /api/v1/chat/{conversation_id}/messages`

- [x] 3.1 Injected `RateLimiter` + `RemoteConfig` (+ optional `clock`) into [`ChatService`](backend/ktor/src/main/kotlin/id/nearyou/app/chat/ChatService.kt). `RateLimitOutcome` sealed interface (`Allowed` / `RateLimited(retryAfterSeconds)`) added to `ChatService` (mirrors the existing `ReplyService.RateLimitOutcome` shape; the `:core:domain` `RateLimiter.Outcome` is already canonical, the service-level wrapper exists per the like/reply precedent for HTTP-mapping decoupling). Rate-limit gate exposed as `suspend fun tryRateLimit(userId, subscriptionStatus): RateLimitOutcome`; `sendMessage()` body unchanged from chat-foundation.
- [x] 3.2 `resolveDailyCap(userId)` reads `remoteConfig.getLong(PREMIUM_CHAT_SEND_CAP_OVERRIDE_KEY)` with try/catch. Unset / null / ≤ 0 / > 10,000 / SDK error all coerce to default 50. WARN logs on each fallback; INFO log on the override-applied path. Mirrors `ReplyService.resolveDailyCap` byte-for-byte with default 50 instead of 20.
- [x] 3.3 Daily-limiter call in `ChatService.tryRateLimit`: skips entirely if `subscriptionStatus in PREMIUM_STATES`; else calls `tryAcquire(userId, "{scope:rate_chat_send_day}:{user:$userId}", cap, computeTTLToNextReset(userId, clock()))` inside `withContext(Dispatchers.IO)`. Returns `RateLimitOutcome.RateLimited` (mapped to 429 + `Retry-After` by the route) or `Allowed`.
- [x] 3.4 Updated [`ChatRoutes.kt`](backend/ktor/src/main/kotlin/id/nearyou/app/chat/ChatRoutes.kt) `POST /api/v1/chat/{conversation_id}/messages` ordering: auth (401) → UUID validation (400 `invalid_uuid`) → **`service.tryRateLimit(...)` (429 + `Retry-After` on `RateLimited`)** → JSON body parse (400 `invalid_request`) → `contentGuard.enforce(CHAT_CONTENT_KEY, body.content)` (400 `invalid_request` for empty/whitespace/oversize) → `service.sendMessage(...)` (404 `conversation_not_found` / 403 `not_a_participant` / 403 `blocked` via the existing chat-foundation exception classes, which map to `respondBlocked()` for the canonical `"Tidak dapat mengirim pesan ke user ini"` body) → 201. The limiter runs before any DB read AND before JSON parsing / content guard — verified by reading the rewritten route.
- [x] 3.5 `ChatService` does NOT call `RateLimiter.releaseMostRecent` anywhere — verified via grep. Class-level KDoc on `ChatService` documents the asymmetry with `LikeService.like` (no `INSERT ... ON CONFLICT DO NOTHING` equivalent on `chat_messages` since every successful POST is a real new row); mirrors the `ReplyService` KDoc.
- [x] 3.6 INFO audit-log line on `resolveDailyCap` non-default cap: `event=remote_config_override_applied key=premium_chat_send_cap_override value=<cap> user_id=<uuid>`. Matches the like-flag and reply-flag audit-log shape byte-for-byte (same SLF4J template, same key set).
- [x] 3.7 `./gradlew :backend:ktor:detekt :lint:detekt-rules:test -PincludeMobile=false` GREEN. 0 detekt findings on the touched backend kotlin files; `lint:detekt-rules:test` BUILD SUCCESSFUL. Both `RateLimitTtlRule` (matches the `_day}` substring + `computeTTLToNextReset` call) and `RedisHashTagRule` (matches the `{scope:rate_chat_send_day}:{user:$userId}` strict hash-tag shape) pass.
- [x] 3.8 Chat-foundation send-endpoint contracts preserved: `ChatService.sendMessage` body byte-for-byte unchanged; route 201 response shape, `respondBlocked` body, embedded-field silent-ignore + structured WARN log all unchanged; `ChatRepository.sendMessage` (the canonical `last_message_at` UPDATE atomicity contract) untouched. Updated [`ChatFoundationRouteTest.kt:89`](backend/ktor/src/test/kotlin/id/nearyou/app/chat/ChatFoundationRouteTest.kt:89) to pass `InMemoryRateLimiter()` + `StubRemoteConfig()` to the new `ChatService` constructor; `./gradlew :backend:ktor:compileTestKotlin -PincludeMobile=false` BUILD SUCCESSFUL. Runtime verification at task 5.3.

## 4. Tests for `POST /chat/{id}/messages` rate limit

- [x] 4.1 Implemented [`ChatSendRateLimitTest`](backend/ktor/src/test/kotlin/id/nearyou/app/chat/ChatSendRateLimitTest.kt) tagged `database`. Backed by `InMemoryRateLimiter` (extracted in `like-rate-limit` task 7.1). Test fixtures (`SpyRateLimiterChat`, `FixedRemoteConfigChat`, `ThrowingRemoteConfigChat`, `NullRemoteConfigChat`) suffixed `Chat` to avoid file-private-symbol collisions with `Like`/`Reply` rate-limit test files. Scenario 23's two-user fixture pins UUIDs `00000000-0000-0000-0000-000000000001` + `00000000-0000-0000-0000-000000000002` with a fixture-load-time `check {}` that their `hashCode() % 3600` offsets differ; also added a defensive pre-cleanup of the pinned UUIDs at scenario start so a prior failed run can't leave orphan rows.
- [x] 4.2 Implemented all 34 scenarios from `specs/chat-conversations/spec.md` § "Integration test coverage — chat send rate limit" as Kotest `StringSpec` strings. `./gradlew :backend:ktor:test --rerun-tasks "-Dkotest.filter.specs=*ChatSendRateLimitTest" -PincludeMobile=false` → **34/34 GREEN** (BUILD SUCCESSFUL in 27s after the pinned-UUID username-collision fix in seedUser — `take(8)` → `takeLast(8)` since the pinned UUIDs differ in suffix not prefix).
- [x] 4.3 Baseline preserved: [`ChatFoundationRouteTest`](backend/ktor/src/test/kotlin/id/nearyou/app/chat/ChatFoundationRouteTest.kt) updated to pass `InMemoryRateLimiter()` + `StubRemoteConfig()` to the new `ChatService` constructor; `./gradlew :backend:ktor:test --rerun-tasks "-Dkotest.filter.specs=*ChatFoundationRouteTest" -PincludeMobile=false` → BUILD SUCCESSFUL in 2m 6s, **0 failures** (123 success-marker hits, 0 `class="failures"`). One internal `ChatRepository.kt` change required: marked the class + `sendMessage` method `open` so scenario 31's failing-repo wrapper compiles (no behavior change in production; subclassing is test-only).
- [x] 4.4 Pre-push gauntlet results: `ktlintCheck` GREEN, `detekt` GREEN (0 findings on touched files), `:lint:detekt-rules:test` GREEN, `:core:domain:test` GREEN, `:infra:redis:test` GREEN, `ChatSendRateLimitTest` 34/34 GREEN in isolation, `ChatFoundationRouteTest` GREEN in isolation, `openspec validate chat-rate-limit --strict` GREEN. The combined `--rerun-tasks "-Dkotest.filter.specs=*ChatSendRateLimitTest|*ChatFoundation*"` run hit a pre-existing flake on test 7.9 of `ChatFoundationRouteTest` (`?limit=500 silently clamped to 100`) — 401 Unauthorized after a 15m48s test duration consistent with JWT 15-min TTL expiry under heavy combined-class seed load (105 partner users). Confirmed pre-existing by passing in isolation; not introduced by this change. Filing for the staging-soak verification at task 6.3 to confirm the route's actual 200 contract end-to-end against real infra.

## 5. Pre-push + CI verification

- [x] 5.1 `openspec validate chat-rate-limit --strict` GREEN throughout proposal + round-1 + round-2 + feat phases (no spec edits in this feat commit).
- [x] 5.2 `./gradlew ktlintCheck detekt -PincludeMobile=false` GREEN. Both `RateLimitTtlRule` and `RedisHashTagRule` produce 0 findings on the new chat send call site (the `_day}` substring + `{scope:rate_chat_send_day}:{user:<uuid>}` strict hash-tag shape align with the rules' patterns).
- [x] 5.3 Local Kotest-filtered run: `ChatSendRateLimitTest` **34/34 GREEN** (27s isolated); `ChatFoundationRouteTest` GREEN in isolation (2m 6s, 0 failures). Combined-class load surfaces a pre-existing 7.9 JWT-TTL flake (105 partner-seed loop runs longer than the 15-min token TTL); filing for task 6.3 staging soak verification of the route's actual contract.
- [x] 5.4 CI GREEN on the change-branch HEAD `e40cb6e` (run [25166990149](https://github.com/aditrioka/nearyou-id/actions/runs/25166990149)): all 5 jobs SUCCESS — `lint`, `test` (34 new ChatSendRateLimitTest scenarios + chat-foundation baselines all passing against the CI Postgres + Redis service containers), `migrate-supabase-parity`, `readme-sync`, `changes`.

## 6. Smoke test against staging (post-CI-green)

- [x] 6.1 Created [`dev/scripts/smoke-chat-send-rate-limit.sh`](dev/scripts/smoke-chat-send-rate-limit.sh) — adapted from the like / reply precedents (`smoke-9.7-like-rate-limit.sh` + `smoke-reply-rate-limit.sh`). Mints dev JWT via `mint-dev-jwt.sh`, create-or-returns a 1:1 conversation via `POST /api/v1/conversations`, POSTs 51 chat messages with JSON body `{"content":"smoke chat send <ts> #N"}`, asserts first 50 = 201 + 51st = 429 with `Retry-After` ≥ 60s + `error.code = "rate_limited"`. The ≥60s Retry-After lower-bound guards against fail-soft (task 6.4). `--recipient` and `--conversation` flags supported (the latter to reuse an existing conversation across re-runs). Executable bit set; `bash -n` syntax check GREEN.
- [x] 6.2 Triggered staging deploy on `chat-rate-limit` branch HEAD `e40cb6e` via `gh workflow run deploy-staging.yml --ref chat-rate-limit` → run [25254133907](https://github.com/aditrioka/nearyou-id/actions/runs/25254133907) SUCCESS. Image SHA `346cb3fdefe52ffc6f848e914c3d8a39bfef8127fe229ba18d22b506dd51dd21` deployed as revision `nearyou-backend-staging-00065-6wf` serving 100% traffic. Reused existing synthetic users from prior smokes; the conversation between the canonical pair `(990cd6b5-f023-4aac-a7f6-28d1b5e0c0c2, 10d600e9-df39-48ec-9493-7e3d445493f1)` is `db16b66f-180e-4bfe-be32-3489544624f0` (created during diagnostic phase, reusable via `--conversation`). **Discovery during diagnosis:** the previous synthetic Free user `10d600e9-df39-48ec-9493-7e3d445493f1` (used for like + reply smokes on 2026-04-25/26) has since been promoted to **Premium** — confirmed via `GET /api/v1/search?q=halo` returning 200 (Premium-gated). For chat-rate-limit, sender role uses Free user `990cd6b5-f023-4aac-a7f6-28d1b5e0c0c2` (search → 403 confirms Free); `10d600e9-...` plays the recipient role (block-state symmetric, Premium subscription on recipient irrelevant since the limiter is keyed on sender).
- [x] 6.3 Smoke verified 2026-05-02T14:41:49Z on `https://api-staging.nearyou.id` for synthetic Free sender `990cd6b5-f023-4aac-a7f6-28d1b5e0c0c2` against conversation `db16b66f-180e-4bfe-be32-3489544624f0`: **50 chat sends returned 201**, **51st returned 429** + `Retry-After: 9904 s` + `error.code = "rate_limited"`. The Retry-After of ~2h45m falls squarely in the expected WIB-staggered window (next WIB midnight ~2h18m away + `hash(990cd6b5-...) % 3600` per-user offset → 8280–11880s expected band). **Limiter is genuinely firing — NOT fail-softing.**
- [x] 6.4 Verified the limiter actually fired: smoke script's ≥60s lower-bound check on Retry-After passed cleanly at 9904s (>>60s, ~165× the floor). The like-rate-limit-task-9.7 deployment-config trio (TLS scheme on `staging-redis-url`, secret slot value, eager-connect fail-soft) all stayed fixed across the like + reply + chat branch deploys — same `RedisRateLimiter` plumbing inherited from PRs #37/#43/#44. Lettuce DEBUG logs confirm Redis connectivity to `sweeping-grouper-78262.upstash.io:6379` throughout the test window. **Diagnostic note:** initial smoke attempt with sender `10d600e9-...` returned 201 on the 51st send because that user is now Premium (limiter skips for Premium tiers — `premium_active`/`premium_billing_retry`); switched to Free sender `990cd6b5-...` to exercise the limiter path.

## 7. Doc sync (in the archive commit, not the feat commit)

- [x] 7.1 Updated `docs/08-Roadmap-Risk.md` Phase 2 item 9 (Chat via Supabase Realtime Broadcast mode) — appended "Chat send rate limit shipped" sub-bullet covering the 50/day Free cap, `premium_chat_send_cap_override` flag, the explicit "no burst" decision (canonical asymmetry per `02-Product.md:318`), the oversized-value fallback threshold at 10,000 (rephrased from "clamp" per the proposal-review wording cleanup — 10,000 is the threshold that triggers the fallback, not a clamp value applied to the override), the no-`releaseMostRecent` design choice, and the GET/POST-conversations carve-outs. No stale "Chat rate limit (50/day Free) remains deferred" notes were present.
- [x] 7.2 Extended `docs/05-Implementation.md` § Rate Limiting Implementation Layer 2 callout (line 1692) with the chat-send-cap entry pointing at `openspec/specs/chat-conversations/spec.md` § "Daily send-rate cap". Same wording shape as the like + reply entries (consumes `rate-limit-infrastructure`, daily-only, default 50, oversized-value fallback threshold at 10,000, no `releaseMostRecent`, GET/POST-conversations un-rate-limited). Layer 1 / Layer 3 / Layer 4 still flagged as future work.
- [x] 7.3 Verified canonical lines unchanged: `02-Product.md:318` reads "Quota: Free 50/day, Premium unlimited" verbatim; `05-Implementation.md:1741` Layer 2 table row reads `| Chat | 50/day Free, unlimited Premium |` verbatim. No edits needed — what shipped matches what was canonical from the start.
- [x] 7.4 Ran `openspec archive chat-rate-limit --yes` — change moved to `openspec/changes/archive/2026-05-02-chat-rate-limit/`; specs synced to `openspec/specs/chat-conversations/spec.md`. Archive commit pushed to the same `chat-rate-limit` branch (one-PR-per-change convention).
