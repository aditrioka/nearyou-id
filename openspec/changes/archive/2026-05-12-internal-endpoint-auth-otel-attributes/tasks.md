## 1. ServiceAccountIdHasher helper

- [x] 1.1 Create `infra/otel/src/main/kotlin/id/nearyou/app/infra/otel/ServiceAccountIdHasher.kt` mirroring the structural shape of [`UserIdHasher.kt`](../../../infra/otel/src/main/kotlin/id/nearyou/app/infra/otel/UserIdHasher.kt) and [`IpHasher.kt`](../../../infra/otel/src/main/kotlin/id/nearyou/app/infra/otel/IpHasher.kt). Single `object ServiceAccountIdHasher` with one function `fun hash(sub: String): String`.
- [x] 1.2 Implement `hash(sub)` as: `require(sub.isNotBlank())` defensive guard, then return the first 16 hex characters of `SHA-256(sub.toByteArray(StandardCharsets.UTF_8))` (use `MessageDigest.getInstance("SHA-256")`, format via `joinToString("") { "%02x".format(it) }.substring(0, 16)` or equivalent). Mirror `IpHasher.hash` byte-for-byte except for the input-validation message (no IP-specific wording).
- [x] 1.3 Add KDoc covering: helper purpose (per-principal correlation for `/internal/*` OIDC-auth requests), output shape (16-hex truncated SHA-256, mirror sibling of `UserIdHasher` and `IpHasher`), input source (the verified OIDC `sub` claim, NOT raw token bytes — link to the WARN-log token-correlation distinction at [`internal-endpoint-auth/spec.md:20`](../../../openspec/specs/internal-endpoint-auth/spec.md)), defensive-guard rationale (blank `sub` → `IllegalArgumentException` to avoid silent shared-bucket collapse), reference back to the spec at `openspec/specs/internal-endpoint-auth/spec.md` § "Requirement: `/internal/*` server spans carry `service.account.id`".

## 2. Helper unit tests

- [x] 2.1 Create `infra/otel/src/test/kotlin/id/nearyou/app/infra/otel/ServiceAccountIdHasherTest.kt` mirroring [`UserIdHasherTest.kt`](../../../infra/otel/src/test/kotlin/id/nearyou/app/infra/otel/UserIdHasherTest.kt) and `IpHasherTest.kt` shape (Kotest `StringSpec` or whichever shape the existing tests use — verify during apply).
- [x] 2.2 Implement Scenario "Hash is deterministic": call `hash(S)` twice with the same input, assert identical 16-hex output.
- [x] 2.3 Implement Scenario "Hash differs between distinct inputs": call `hash(S1)` and `hash(S2)` with two distinct inputs, assert outputs differ.
- [x] 2.4 Implement Scenario "Output shape is exactly 16 lowercase hex chars": call `hash(s)` on a representative input, assert `result.matches(Regex("^[0-9a-f]{16}$"))`.
- [x] 2.5 Implement Scenario "Rejects blank input fail-fast": invoke `hash("")`, `hash(" ")`, `hash("\t\n")`, assert each throws `IllegalArgumentException`.
- [x] 2.6 Implement parity test: assert `ServiceAccountIdHasher.hash("test")` matches an externally-computed reference value (e.g., the first 16 hex chars of `SHA-256("test".toByteArray(UTF-8))` = `9f86d081884c7d65...`); guards against accidental algorithm/encoding drift.

## 3. Attribute writer integration

- [x] 3.1 The integration point is identified: [`InternalEndpointAuth.kt:79`](../../../backend/ktor/src/main/kotlin/id/nearyou/app/internal/InternalEndpointAuth.kt) — the line `call.attributes.put(OidcSubjectKey, claims.sub)` inside the success branch of `verifier.verify(token)`. The new writer call goes IMMEDIATELY AFTER this line (still inside the same `try { ... }` block, BEFORE the catch handlers at lines 80-95).
- [x] 3.2 Add the attribute-writer call as a byte-for-byte mirror of the precedent at [`AuthPlugin.kt:113-120`](../../../backend/ktor/src/main/kotlin/id/nearyou/app/auth/AuthPlugin.kt):
  ```kotlin
  try {
      io.opentelemetry.api.trace.Span.current()
          .setAttribute("service.account.id", id.nearyou.app.infra.otel.ServiceAccountIdHasher.hash(claims.sub))
  } catch (_: Throwable) {
      // Span recording failure MUST NOT block authentication —
      // observability is a side-effect surface. Per spec
      // § "Best-effort write silently no-ops on helper throw"
      // + § "Best-effort write silently no-ops on SpanProcessor failure".
  }
  ```
  The `Throwable` (not `Exception`) catch matches the AuthPlugin precedent and covers two failure modes: (a) `Span.current()` returns the no-op span when OTel SDK is uninitialised (no-throw path), AND (b) the helper throws `IllegalArgumentException` when `claims.sub` is blank (REACHABLE in production per `GoogleOidcTokenVerifier.kt:90`'s `decoded.subject ?: ""` substitution).
- [x] 3.3 The mutual-exclusion contract is STRUCTURAL via route-scoped plugin installation, NOT via a `principal is X` discriminator (no `OidcServicePrincipal` type exists in the codebase per design.md § Decision 6). The `InternalEndpointAuth` plugin is route-scoped to `/internal/*`; the `AuthPlugin` is route-scoped to `/api/v1/*`; the two writers are inlined into their respective plugins and structurally cannot fire on the same Ktor server span. No type-gate code is needed in this task.
- [x] 3.4 Verify the necessary `:infra:otel` dependency is already exposed on `:backend:ktor`'s `internal` package — the AuthPlugin at line 115 already imports `id.nearyou.app.infra.otel.UserIdHasher` from the same module, confirming the build-classpath wiring is in place from `observability-otel-foundation`. No new module dependencies are added by this change.
- [x] 3.5 Update the now-stale comment at [`AuthPlugin.kt:111`](../../../backend/ktor/src/main/kotlin/id/nearyou/app/auth/AuthPlugin.kt) (which currently says "the `service.account.id` shape is deferred to the `internal-endpoint-auth-otel-attributes` follow-up") — replace with a positive reference to the now-shipped contract, e.g., "the `service.account.id` shape is shipped by the `internal-endpoint-auth-otel-attributes` capability spec at `openspec/specs/internal-endpoint-auth/spec.md` § `service.account.id`". This avoids `git grep` confusion for future readers.

## 4. Plugin integration test

> **Scope note**: the integration test surface is larger than initially estimated. There is NO existing `:backend:ktor` test that uses `KtorServerTelemetry` (verified via `grep -rln "KtorServerTelemetry"` over `:backend:ktor` test sources during round-1 review). This means the new test must construct the OTel SDK + `SpanRecorder` + install `KtorServerTelemetry` itself. Realistic LOC: ~200-350 across the test class and its helpers, not the ~50-100 first-pass estimate. The fixture work is in scope of this task — do NOT defer it as out-of-scope; the test surface is REQUIRED for the spec scenarios to be verifiable.

- [x] 4.1 Create `backend/ktor/src/test/kotlin/id/nearyou/app/internal/InternalEndpointSpanAttributeTest.kt` (or extend the existing `InternalEndpointAuth*Test` if one exists — verify during apply) using `testApplication { ... }` + the existing [`SpanRecorder`](../../../infra/otel/src/testFixtures/kotlin/id/nearyou/app/infra/otel/testing/SpanRecorder.kt) test fixture from `:infra:otel`'s testFixtures source set (the same fixture used by the `fcm-push-dispatch` capability's "Span recording failure does not block dispatch" scenario — verify the testFixtures dependency is wired on `:backend:ktor` test classpath; if not, wire it as part of this task).
- [x] 4.2a Implement Scenario "Successful request produces span with hashed service.account.id" — assert hash value: construct a valid OIDC token (test JWKS-signed) with `claims.sub = "105329845711234567890"` (representative numeric-UID shape per Google OIDC), send `POST /internal/unban-worker` with the token, capture the resulting server span, assert `span.attributes["service.account.id"] == ServiceAccountIdHasher.hash("105329845711234567890")`.
- [x] 4.2b Implement Scenario "Successful request span MUST NOT carry the raw sub claim" — sentinel-string scan: same test setup as 4.2a; assert NO attribute value, attribute key, or span name on the captured server span contains the literal `"105329845711234567890"` (sentinel-scan via `forEach { entry -> entry.value.toString() shouldNotContain "105329845711234567890" }` shape).
- [x] 4.3 Implement Scenario "401-rejected request span behaviour": construct a token with an invalid signature (or expired `exp`), send the request, observe whether `KtorServerTelemetry`'s instrumentation produces a server span for the 401 response (the plugin short-circuits in `CallSetup` via `respondText(...)` BEFORE `OidcSubjectKey` is set, so the writer never executes; whether a span is emitted depends on Ktor instrumentation hook ordering — verify empirically). If a span IS produced, assert it carries NO `service.account.id` AND NO `user.id`. If no span is produced (also acceptable), assert that no span at all references the offending token's hashed form. The negative property (attribute absence) is the contract; whether a span exists is implementation-detail of `KtorServerTelemetry`.
- [x] 4.4 Implement Scenario "Server-span-level mutual exclusion with user.id" — `/internal/*` direction: same as 4.2a but additionally assert `user.id` attribute is absent on the server span (the request never reached UserPrincipal-backed auth; the AuthPlugin's writer never executes on `/internal/*` routes).
- [x] 4.4b Implement Scenario "Converse mutual exclusion — `/api/v1/*` server span never carries `service.account.id`": construct a UserPrincipal-authenticated request to a representative `/api/v1/*` route (e.g., `GET /api/v1/timeline/nearby` or whichever existing route has the simplest test fixture surface); capture the server span; assert `user.id` IS present AND `service.account.id` is ABSENT (the InternalEndpointAuth plugin never ran on this route; the writer's structural gate fails). This locks the converse direction of the mutual-exclusion contract.
- [x] 4.5 Implement Scenario "Best-effort write silently no-ops when OTel SDK uninitialised": construct a `testApplication { ... }` block that mounts `/internal/*` routes WITHOUT calling `OtelBootstrap.start(...)`, send a valid OIDC request, assert the handler dispatches normally AND no exception propagates AND the response is the handler's success body. The mechanism: `Span.current()` returns the no-op span (per OTel SDK contract), whose `setAttribute` is a defensive no-op (no-throw). This locks the SDK-uninitialised silent-fail posture.
- [x] 4.5b Implement Scenario "Best-effort write silently no-ops on helper throw" — REGRESSION test for blank-sub edge case: construct an `OidcTokenVerifier` test fixture (mock or fake) that returns `VerifiedClaims(sub = "", aud = "...", exp = ..., iat = ...)` (mirrors `GoogleOidcTokenVerifier.kt:90`'s null-substitution behaviour). Send a request, observe that the writer's helper invocation throws `IllegalArgumentException` per `require(sub.isNotBlank())`, the catch-all swallows the throw, AND the auth gate proceeds to handler dispatch normally AND the captured server span has NO `service.account.id` attribute (absence is the correct outcome for an unidentifiable principal).
- [x] 4.5c Implement Scenario "Best-effort write silently no-ops on SpanProcessor failure" — REGRESSION test using `FailingSpanProcessor` from [`infra/otel/.../SpanRecorder.kt`](../../../infra/otel/src/testFixtures/kotlin/id/nearyou/app/infra/otel/testing/SpanRecorder.kt) (the same fixture used by the `fcm-push-dispatch` "Span recording failure does not block dispatch" scenario): wire the failing processor into the OTel SDK pipeline, send a valid OIDC request, observe that the writer's `Span.current().setAttribute(...)` invocation triggers the FailingSpanProcessor's throw, the catch-all swallows it, AND the auth gate + handler proceed unblocked. Locks the silent-fail posture against actively-throwing telemetry pipelines (a stronger regression than 4.5's no-op-span path).
- [x] 4.6 Implement Scenario "Vendor-webhook routes opt out and produce no service.account.id": send a request to [`POST /internal/apple/s2s-notifications`](../../../backend/ktor/src/main/kotlin/id/nearyou/app/auth/routes/AppleS2SRoutes.kt) (the canonical existing vendor-webhook `/internal/*` route — confirmed not to install `InternalEndpointAuth`); capture the server span; assert NO `service.account.id` attribute AND NO `user.id` attribute (the InternalEndpointAuth plugin never ran on this route; OidcSubjectKey is never set; no UserPrincipal-backed auth either).

## 5. Lint + Detekt verification

- [x] 5.1 Run `./gradlew detekt` locally — assert the `OtelForbiddenAttributeRule` does NOT fire on the new attribute writer call (`service.account.id` is the SANCTIONED key, not in the rule's forbidden list; the hashed-via-helper consumption pattern mirrors `UserIdHasher.hash(uuid)` → `user.id` and is the canonical sanctioned path).
- [x] 5.2 Run the full pre-push verification per [`CLAUDE.md`](../../../CLAUDE.md) § "Pre-push verification": `./gradlew ktlintCheck detekt :backend:ktor:test :lint:detekt-rules:test` — all four targets MUST pass before pushing.
- [x] 5.3 Verify by inspection that the new helper is NOT added to `ForbiddenAttributeStripper.FORBIDDEN_KEYS` — the runtime stripper defends against accidentally-emitted forbidden keys; the new sanctioned attribute MUST flow through unstripped.

## 6. Spec + docs sync

- [x] 6.1 Run `openspec validate internal-endpoint-auth-otel-attributes --strict` — ensure validation passes.
- [x] 6.2 FOLLOW_UPS.md entry `internal-endpoint-auth-otel-attributes` deleted at archive time (previously at lines 79–96); the file retains its other open entries.
- [x] 6.3 Post-archive grep on `docs/` + `openspec/specs/` is clean — the only pre-archive reference was the deferral prose at `openspec/specs/observability-otel-foundation/spec.md:152`, which the MODIFIED delta replaces during `openspec archive` sync.
- [x] 6.4 Added `service.account.id` to `docs/04-Architecture.md` § Observability Stack "Mandatory attributes" bullet (line 437) — `/internal/*` is now enumerated alongside the UserPrincipal-backed `user.id` attribute. `docs/06-Security-Privacy.md` does not enumerate per-attribute observability surface (verified by grep), so no amendment needed there.

## 7. Pre-archive smoke

- [x] 7.1 Manual staging deploy completed via `gh workflow run deploy-staging.yml --ref internal-endpoint-auth-otel-attributes` ([run 25710059171](https://github.com/aditrioka/nearyou-id/actions/runs/25710059171), conclusion = success at 2026-05-12T02:48Z).
- [x] 7.2 Partial verification: Cloud Scheduler force-run `gcloud scheduler jobs run nearyou-unban-worker-staging --location=asia-southeast1 --project=nearyou-staging` returned 0; Cloud Logging confirms `POST https://api-staging.nearyou.id/internal/unban-worker` → 200 OK at 2026-05-12T02:50:42Z + structured INFO log `event=suspension_unban_applied unbanned_count=0 unbanned_user_ids=[] duration_ms=69` — the new writer code path executes without breaking auth or handler dispatch. **Full Tempo span attribute assertion deferred**: this Claude session does not have `gcx` installed locally; the expected `service.account.id` value for the staging SA is computed as `73f1ab38aff02daa` (= first 16 hex of `SHA-256("116614859945965515867")` where `116614859945965515867` is `unban-scheduler-staging@nearyou-staging.iam.gserviceaccount.com`'s numeric `uniqueId`). User to run a `gcx` Tempo query post-merge to compare the actual span attribute against `73f1ab38aff02daa` (or accept the 9-scenario integration test coverage as proof of writer correctness).
- [x] 7.3 Negative smoke deferred: this Claude session's sandbox denied sending an unauthenticated POST to the staging `/internal/*` endpoint as a non-test write against shared infrastructure. The negative-property contract (NO `service.account.id` on 401-rejected request spans) is exhaustively covered by integration test scenario 4.3 (`InternalEndpointSpanAttributeTest`) which constructs an expired-token request and asserts attribute absence on any captured server span. User to optionally run the staging negative-smoke probe manually with explicit authorization.
