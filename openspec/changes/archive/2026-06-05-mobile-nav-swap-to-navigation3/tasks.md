## 1. Substrate swap + pre-implementation library re-check (MUST run first)

- [x] 1.1 **Pre-implementation library re-check (MUST — new substrate).** Per `openspec/project.md` § "Pre-implementation library re-check", run a fresh dated `WebSearch` (e.g. `"org.jetbrains.androidx.navigation3 latest version <month-year>"`, `"compose multiplatform navigation 3 production <month-year>"`, `"koin-compose-navigation3 version"`). Resolve: (a) the exact `org.jetbrains.androidx.navigation3` version + release channel; (b) whether `navigation3-ui` alone suffices (transitive `navigation3-runtime`/`-common`) or `-runtime` must be declared; (c) the `io.insert-koin:koin-compose-navigation3` version compatible with Koin `4.1.0` + CMP `1.11.1`; (d) any API rename since proposal (e.g. `onBack`→`onBackCompleted`). If the re-check surfaces a materially-better alternative or a blocking incompatibility, STOP and surface to the user (do NOT silently switch substrate). Record the verification date + resolved versions in the first feat commit body.
- [x] 1.2 In `gradle/libs.versions.toml`: remove the `voyager` version + the `voyager-navigator` / `voyager-koin` library entries; add the `navigation3` version + `navigation3-ui` library (and `navigation3-runtime` if 1.1 requires it explicitly) under `org.jetbrains.androidx.navigation3`, plus `koin-compose-navigation3` under `io.insert-koin`. Replace the Voyager pin-rationale comment (lines 45-51) with a Nav3 rationale comment (CMP port; pre-stable status + why accepted; re-check date).
- [x] 1.3 In `mobile/app/build.gradle.kts`: swap the `commonMain` deps `libs.voyager.navigator` + `libs.voyager.koin` for `libs.navigation3.ui` (+ `libs.navigation3.runtime` if needed) + `libs.koin.compose.navigation3`. The `kotlinxSerialization` plugin is **already applied** (`mobile/app/build.gradle.kts:9`, `alias(libs.plugins.kotlinxSerialization)`) — the `@Serializable NavKey` routes rely on it; confirm it remains (no add needed). Keep the Release-variant `*ScreenTest` exclude block intact.
- [x] 1.4 Confirm the project compiles against the new deps with the old screens temporarily un-migrated is NOT expected — Voyager imports break immediately; proceed straight through Sections 2-5 before the first successful build.

## 2. Nav model (routes, serialization, entryProvider, helpers, identity holder) — `screens/routing/`

- [x] 2.1 Add `screens/routing/NavKeys.kt`: a `sealed interface`/marker `NavKey` set — `@Serializable data object RootRoute`, `SignInRoute`, `HomeRoute`, `AgeGateRoute`, `PostCreationRoute`, each implementing the Nav3 `NavKey` marker. `AgeGateRoute` MUST be a parameterless marker (NO `idToken` property — design Decision 4 + the `mobile-age-gate` § "AgeGateRoute declares no identity property" scenario).
- [x] 2.2 Add `screens/routing/AppNavSerialization.kt`: the `SavedStateConfiguration` carrying `SerializersModule { polymorphic(NavKey::class) { subclass(RootRoute::class, RootRoute.serializer()); … all 5 routes … } }` (`mobile-app-scaffold` § "Back stack uses serializable NavKey routes"). Every route MUST be registered (the round-trip test in 7.1 fails on a missing `subclass`).
- [x] 2.3 Add `screens/routing/BackStackExtensions.kt`: `fun NavBackStack.replaceAll(key: NavKey) { clear(); add(key) }` (design Decision 6) for auth-boundary transitions.
- [x] 2.4 Add `screens/routing/PendingSignupIdentity.kt`: an in-memory holder exposing `set(idToken: String)` / `peek(): String?` (non-clearing read — so an in-screen retryable error can re-read the same token) / `clear()`, registered as a Koin `single` in 6.1. Holds the verified Google `id_token` between the sign-in no-account path and the age-gate flow — never persisted, never on a `NavKey` (design Decision 4). `clear()` is called on every **terminal** transition OUT of the age-gate flow (Success, AccountExists, the absent-identity/back re-route to `SignInRoute`) so a verified token does not linger in process memory past the flow; it is NOT cleared on in-screen retryable errors (network/5xx) where the user may resubmit.
- [x] 2.5 Add `screens/routing/AppEntryProvider.kt`: `fun appEntryProvider(backStack: NavBackStack, …)` returning the `entryProvider { entry<RootRoute> { … }; entry<SignInRoute> { … }; entry<HomeRoute> { … }; entry<AgeGateRoute> { … }; entry<PostCreationRoute> { … } }` mapping each key to its screen composable, wiring each screen's narrow navigation lambdas (Decision 6) to back-stack operations.

## 3. App host — `App.kt`

- [x] 3.1 Rewrite `App()` to: `NearYouTheme { val backStack = rememberNavBackStack(navSavedStateConfig, RootRoute); SessionExpiryEffect(backStack); NavDisplay(backStack = backStack, onBack = { backStack.removeLastOrNull() }, entryDecorators = listOf(rememberSaveableStateHolderNavEntryDecorator()), entryProvider = appEntryProvider(backStack, …)) }`. Remove all `cafe.adriel.voyager.*` imports (`Navigator`, `CurrentScreen`). (`mobile-app-scaffold` § Typed navigation host + § NavDisplay entry decorators.)
- [x] 3.2 `entryDecorators` includes BOTH `rememberSaveableStateHolderNavEntryDecorator()` AND `rememberViewModelStoreNavEntryDecorator()` (Decision 5 un-deferred during `/opsx:apply` 9.5 — see § 3.5; `mobile-app-scaffold` § "NavDisplay scopes per-entry saveable state and ViewModels via entry decorators").

## 3.5 Nearby feed reload-on-return fix — VM-scoped entry state (Decision 5 un-deferred)

On-device manual testing (9.5) surfaced the Nav3 "state-loss on back" papercut: opening the composer disposed `HomeRoute`, so returning re-fired the feed's `remember`+`LaunchedEffect` load → the Nearby feed re-fetched every return (perceived-inconsistent due to the 60s/90s `LocationTuning` warm-fix window). Fixed with the canonical Nav3 pattern (verified against the Android save-state doc + `android/nav3-recipes` + the Koin Nav3 doc): scope the feed's load state to a `HomeRoute` ViewModel under `rememberViewModelStoreNavEntryDecorator()`.

- [x] 3.5.1 Add `org.jetbrains.androidx.lifecycle:lifecycle-viewmodel-navigation3` (JetBrains CMP port, `version.ref = androidx-lifecycle` = `2.10.0`) to `gradle/libs.versions.toml` + `mobile/app/build.gradle.kts` `commonMain`.
- [x] 3.5.2 Wire `rememberViewModelStoreNavEntryDecorator()` into `App.kt` (and `TestNavHost`) `entryDecorators` after the saveable-state decorator (canonical order).
- [x] 3.5.3 Add `screens/timeline/NearbyTimelineViewModel.kt` (`androidx.lifecycle.ViewModel`) holding `outcome` / `inFlight` + `reload()`; first load on `init`; load failure → existing `NearbyTimelineOutcome.NetworkError`.
- [x] 3.5.4 Refactor `NearbyFeed` to observe `viewModel { NearbyTimelineViewModel(flow) }` (the `flow` is `koinInject`-ed; androidx `viewModel` — no Koin VM registration, so the screen-test modules are unchanged); remove the `remember`+`LaunchedEffect`+reloadKey load. The `LocationGate` stays `remember`-ed (its `ON_RESUME` re-check is intentional + fast).
- [x] 3.5.5 NEW `commonTest` `NearbyTimelineViewModelTest`: loads once on construction, `reload()` re-fetches, failure → `NetworkError` (runs on Android + iOS).
- [x] 3.5.6 Invert the `VoyagerAbsenceScanTest` VM-decorator guard → assert the decorator IS wired in `App.kt` + the artifact IS in the catalog. The existing Nearby `*ScreenTest` / `*FlowIosTest` are unchanged (the bare-compose `viewModel { }` resolves the test host's `ViewModelStore`; verified green on Robolectric + iOS sim).

## 4. Screen conversions (Voyager `Screen` → plain `@Composable`)

- [x] 4.1 `screens/routing/RootRouterScreen.kt`: convert `class RootRouterScreen : Screen { override fun Content() }` → `@Composable fun RootRouterScreen(onAuthenticated: () -> Unit, onUnauthenticated: () -> Unit)` (or receive the back stack). Replace `LocalNavigator.currentOrThrow` + `navigator.replaceAll(...)` with the injected lambdas calling `backStack.replaceAll(HomeRoute)` / `replaceAll(SignInRoute)` in the `entryProvider`. Splash composition unchanged. (`mobile-auth-signin` § RootRouterScreen routes.)
- [x] 4.2 `screens/auth/SignInScreen.kt`: convert to `@Composable fun SignInScreen(…)`. Success → `backStack.replaceAll(HomeRoute)`; on 404 no-account → `pendingSignupIdentity.set(idToken)` then `backStack.add(AgeGateRoute)`. Remove Voyager imports.
- [x] 4.3 `screens/auth/AgeGateScreen.kt`: convert to `@Composable fun AgeGateScreen(…)`. Read the identity via `pendingSignupIdentity.peek()` (NOT a route param; non-clearing so a network-error resubmit re-reads). On entry with an absent pending identity → one-shot re-route to `SignInRoute` (process-death guard; `mobile-age-gate` § "Restored back stack on AgeGateRoute with absent identity"). Call `pendingSignupIdentity.clear()` on every terminal exit: Success → `clear()` + `backStack.replaceAll(HomeRoute)`; `AccountExists` → `clear()` + `backStack.replaceAll(SignInRoute)`; the absent-identity re-route → `clear()` (idempotent) + `replaceAll(SignInRoute)`. Do NOT clear on retryable in-screen errors (network/5xx).
- [x] 4.4 `screens/home/HomeScreen.kt`: convert to `@Composable fun HomeScreen(…)`. Keep hosting `NearbyTimelineScreen` as the body + the FAB; FAB → `backStack.add(PostCreationRoute)` (replaces the nullable `LocalNavigator` push). (`mobile-nearby-timeline` § HomeScreen hosts + `mobile-post-creation` § FAB.)
- [x] 4.5 `screens/post/PostCreationScreen.kt`: convert to `@Composable fun PostCreationScreen(…)`. `Success` → `backStack.removeLastOrNull()` from a `LaunchedEffect(outcome)` (never mutate the back stack during composition). (`mobile-post-creation` § Successful post returns to Home.)
- [x] 4.6 `screens/timeline/NearbyTimelineScreen.kt`: convert `class … : Screen` → `@Composable fun NearbyTimelineScreen(…)`. It remains navigation-free — add NO back-stack reference, NO FAB (`mobile-post-creation` § "NearbyTimelineScreen remains navigation-free"). This is the only edit to this file.

## 5. Routing effects — `SessionExpiryEffect`

- [x] 5.1 `screens/routing/SessionExpiryEffect.kt`: change the signature from `SessionExpiryEffect(navigator: Navigator)` to `SessionExpiryEffect(backStack: NavBackStack)`; replace `navigator.replaceAll(SignInScreen())` with `backStack.replaceAll(SignInRoute)`. Keep the `collectLatest` on `SessionInvalidator.sessionExpired`. Stays under `screens/routing/**` so `App.kt` is free of auth-flow identifiers.

## 6. DI wiring — `di/MobileModule.kt`

- [x] 6.1 Register `single { PendingSignupIdentity() }` in `mobileModule`. Preserve ALL existing bindings (`AuthFlow`, `NearbyTimelineFlow`, `CreatePostFlow`, `SessionInvalidator`, `LocationPermissionController`, etc.) — the nav swap changes none of them; `koinInject` resolution is unaffected.
- [x] 6.2 Verify the Koin graph still resolves (extend `KoinInitTest` or add a focused check that `PendingSignupIdentity` resolves from the real `mobileModule`).

## 7. Tests (migrate 12 nav-dependent files, confirm 5 pure *UiStateTest untouched, add 2 new)

<!-- Nav-area test inventory (17 existing files): 12 reference the Voyager nav API and migrate/delete — 7 Robolectric *ScreenTest (7.3) + 4 iOS *FlowIosTest (7.5) + 1 commonTest HomeScreenTest (7.9, deleted). 5 pure *UiStateTest have no nav dependency and are confirmed untouched (7.6). Plus 2 new tests (7.1 serialization round-trip, 7.2 id-token-not-serialized). -->


- [x] 7.1 NEW `commonTest` `NavKeySerializationTest`: serialize each `NavKey` route via the polymorphic `SavedStateConfiguration` module and assert it deserializes equal (catches a missing `subclass` registration). (`mobile-app-scaffold` § "Every route key round-trips".)
- [x] 7.2 NEW `commonTest` `PendingSignupIdentityNotSerializedTest`: build `[RootRoute, SignInRoute, AgeGateRoute]` with `PendingSignupIdentity` holding `id_token = "g-id-secret"`, serialize the back stack, assert the serialized output contains NO `"g-id-secret"` substring. (`mobile-age-gate` § "Serialized back stack containing AgeGateRoute carries no id_token".)
- [x] 7.3 Re-wire the 7 Robolectric `*ScreenTest` (`RootRouterScreenTest`, `SignInScreenTest`, `AgeGateScreenTest`, `PostCreationScreenTest`, `HomeScreenFabTest`, `NearbyTimelineScreenTest`, `NearbyLocationGateScreenTest`) from `Navigator(Screen())` to either composing the screen composable directly with a fake back stack + recording nav lambdas, or hosting `appEntryProvider` in a `NavDisplay` where an actual route transition is asserted. Update routing assertions to back-stack checks (e.g. FAB → `PostCreationRoute` appended; RootRouter → `replaceAll(HomeRoute)`/`replaceAll(SignInRoute)`).
- [x] 7.4 Add a Robolectric (or commonTest) case for the new process-death guard: a restored back stack on `AgeGateRoute` with an empty `PendingSignupIdentity` re-routes to `SignInRoute` and does NOT render the signup form. (`mobile-age-gate` § "Restored back stack on AgeGateRoute".)
- [x] 7.5 Re-wire the 4 iOS `*FlowIosTest` (`SignInFlowIosTest`, `AgeGateFlowIosTest`, `RootRouterFlowIosTest`, `NearbyTimelineFlowIosTest`) to the Nav3 harness; verify they pass on `:mobile:app:iosSimulatorArm64Test`. **Extend `AgeGateFlowIosTest`** with a restored-back-stack-on-`AgeGateRoute`-with-absent-identity case (compose the restored entry with an empty `PendingSignupIdentity`, assert the one-shot re-route to `SignInRoute` and that the signup form is NOT rendered) — this is the load-bearing iOS-serialization guard (`mobile-age-gate` § "Restored back stack on AgeGateRoute with absent identity"); the guard logic is composable-testable on the iOS sim, so it gets a real iosTest case, not only the Robolectric (7.4) + manual (9.5d) coverage.
- [x] 7.6 Confirm the **5** pure `*UiStateTest` (`SignInUiStateTest`, `AgeGateUiStateTest`, `PostCreationUiStateTest`, `NearbyTimelineUiStateTest`, `LocationGateUiStateTest`) are untouched (no navigator dependency) and still pass.
- [x] 7.7 Confirm any new Robolectric `*ScreenTest` added in 7.3/7.4 is in the `build.gradle.kts` Release-variant `*ScreenTest` exclude; verify `:mobile:app:testDevReleaseUnitTest` does not attempt them (per the `feedback_mobile_robolectric_release_exclude` rule).
- [x] 7.8 Add a source-scan guard (mirroring `VendorSdkLeakageScanTest`/`PostCreationSourceGuardTest`, stripping comments first per `feedback_source_scan_guard_strip_comments`): assert NO `cafe.adriel.voyager` import remains anywhere in `mobile/app/src/**`.
- [x] 7.9 **Delete `mobile/app/src/commonTest/kotlin/id/nearyou/app/HomeScreenTest.kt`** — it instantiates `HomeScreen()` as a Voyager `Screen` and asserts `screen.key` (lines 14-15, 22-23), which does not compile once `HomeScreen` is a `@Composable fun` (task 4.4). Its render-smoke value is already covered by `NearbyTimelineScreenTest` (per the file's own in-file NOTE) and the FAB behavior by `HomeScreenFabTest`, so the file is removed rather than converted. (This is the 12th nav-dependent file — the `find` glob for `*ScreenTest.kt` skips it because the host class is `HomeScreenTest` in `commonTest`; surfaced by the test-coverage review lens.) If any unique assertion is found at apply time, fold it into the Robolectric `HomeScreenFabTest` instead of resurrecting this file.

## 8. Docs + catalog hygiene

- [x] 8.1 Amend `docs/04-Architecture.md` § Mobile Status (the two "Voyager nav" mentions, ~lines 124 + 146) to "Navigation 3" with a one-line note that the swap shipped via `mobile-nav-swap-to-navigation3`. (This change deliberately supersedes the prior canonical statement — an explicit docs amendment, not stale-doc drift.)
- [x] 8.2 Confirm the `gradle/libs.versions.toml` comment swap from 1.2 reads cleanly (Nav3 rationale replaces the Voyager "re-evaluate when stable" note).
- [x] 8.3 `FOLLOW_UPS.md` hygiene: (a) if any Voyager-tracking entry exists (e.g. a "re-evaluate Voyager stable" note), resolve/remove it (this change IS that re-evaluation); (b) update the existing `mobile-post-creation-refresh-nearby-on-return` entry — its action item (`FOLLOW_UPS.md:57`) says "a shared reload trigger **or a Voyager result**"; change "a Voyager result" to "a Nav3 `ResultEventBus` signal" so the follow-up matches the post-swap mechanism (the post-creation delta's negative guard already forbids the Nav3 result path); (c) add the deferred follow-ups named in the proposal only if not already present: `mobile-nav3-adaptive-scenes`, `mobile-home-tab-host` (the latter may already exist from `mobile-post-creation`).
- [x] 8.4 **At `/opsx:archive` (spec-sync phase):** edited the `## Purpose` of `openspec/specs/mobile-app-scaffold/spec.md` ("typed navigation host (Voyager)" → "(Navigation 3)") and `openspec/specs/mobile-post-creation/spec.md` ("a Voyager `Screen` opened by a home-surface FAB" → "a composable opened by a home-surface FAB") in the archive commit. The 3 remaining "Voyager" mentions in the synced specs are deliberate (the age-gate Purpose explains the *prior* never-serialized back stack; the post-creation scenario explains the `Screen`→composable conversion; the scaffold requirement explicitly *forbids* Voyager API).

## 9. Validation + verification (pre-push gates)

- [x] 9.1 `openspec validate mobile-nav-swap-to-navigation3 --strict` → green.
- [x] 9.2 `./gradlew :mobile:app:assembleDebug` (Android) → exit 0.
- [x] 9.3 `./gradlew :mobile:app:linkPodDebugFrameworkIosSimulatorArm64` (iOS framework link — the load-bearing Nav3-on-iOS target; CI is Linux-only so this MUST be run locally) → exit 0. If ObjC-category imports surface (per `reference_kn_objc_category_members_need_import`), resolve before pushing.
- [x] 9.4 `./gradlew ktlintCheck detekt :mobile:app:testDevDebugUnitTest :mobile:app:testDevReleaseUnitTest :backend:ktor:test :lint:detekt-rules:test` → green (flavor-qualified mobile tasks per `reference_mobile_gate_flavor_qualified_tasks`; both lint frameworks per the CLAUDE.md pre-push gate).
- [~] 9.5 Manual runtime pass — **partial; load-bearing flows verified on-device (Android, staging, Galaxy A17 SM-A176B), residuals automation-backed + flagged as a post-merge spot-check.** Verified manually: (a) cold launch → **token-present → Home** (Nearby feed renders); (c) FAB → composer (PostCreationRoute pushed) → back → pops to Home (the `removeLastOrNull()` pop, same op as the Success-pop) + the composer loading/LocationUnavailable state mappings; **and the reload-on-return fix** (returning from the composer does NOT re-fetch the Nearby feed — content persists). NOT manually exercised, but covered by the automated iOS/Robolectric suites: (a)-no-token → SignIn (`RootRouterScreenTest`/`RootRouterFlowIosTest`), (b) sign-in success → Home, (d) iOS process-death mid-AgeGate → SignIn (`AgeGateFlowIosTest` restored-back-stack case + `AgeGateScreenTest` 7.4), (e) terminal 401 → SignIn (`SessionExpiryEffect` + its wiring). Post-merge manual spot-check of (b)/(d)/(e) recommended on a fresh install. Recipes: `reference_mobile_physical_device_uses_staging` + `reference_ios_sim_verification_recipe`.
