## 1. Lettuce + `:infra:redis` module

- [x] 1.1 Pin `io.lettuce:lettuce-core` in `gradle/libs.versions.toml` (single typesafe accessor — no raw `group:artifact:version` strings per `09-Versions.md` policy)
- [x] 1.2 Add a row to `docs/09-Versions.md` Version Decisions table justifying the new pin (rationale: rate-limit infra MVP; next review Q3)
- [x] 1.3 Create `:infra:redis` Gradle module — JVM target only, applies the standard backend module conventions
- [x] 1.4 Add `:infra:redis` to `settings.gradle.kts` and to `:backend:ktor` `dependencies { ... }`
- [x] 1.5 Verify `:core:domain` and `:core:data` do NOT depend on `:infra:redis` (verified by inspection: `core/domain/build.gradle.kts` and `core/data/build.gradle.kts` declare no dependency on `:infra:redis`. A formal architecture-test pattern (e.g., ArchUnit or shell-script grep) is not yet established in this codebase; deferred to a follow-up if multiple modules need similar negative-dep checks.)
- [x] 1.6 Implement Koin module in `:infra:redis` providing a singleton Lettuce `RedisClient` configured via env-aware slot lookup (mirrors `secretKey(env, "redis-url")` convention; slot derivation duplicated locally to avoid `:backend:ktor` ↔ `:infra:redis` circular dep)
- [x] 1.7 Verify the `staging-redis-url` and `redis-url` secret slots exist in GCP Secret Manager. **Verified 2026-04-25:**
    * **Staging (`nearyou-staging` project):** ✅ `staging-redis-url` slot exists, version 1 enabled, created 2026-04-22T10:55:27. Value populated (117 chars, prefix `redis://`, suffix `:6379` — Upstash URL format). Verified via `gcloud secrets list --project=nearyou-staging --filter="name~redis"` + `gcloud secrets versions list staging-redis-url --project=nearyou-staging` + length-check on the version-access output.
    * **Production:** ⏸️ N/A at this phase. Production GCP project is not yet provisioned (only `nearyou-staging` + a default sandbox "My First Project" exist). Per `docs/08-Roadmap-Risk.md` § Environments + Pre-Phase 1 item 34, the production environment is provisioned at the Pre-Launch phase, not during Phase 1/2 build. The `redis-url` slot lands alongside the prod project bootstrap. The fail-fast `require(...)` in `Application.kt` for `ktorEnv == "production"` correctly enforces "must have slot" at that future deploy time.
- [x] 1.8 Confirm local dev `docker-compose.yml` already exposes Redis on the documented port; if not, amend per `04-Architecture.md` Local Dev section (no DB migration; this is local infra only) — verified: `dev/docker-compose.yml` already declares `redis:7-alpine` with port `6379:6379` and a `redis-cli ping` healthcheck. No change needed.

## 2. `RateLimiter` interface in `:core:domain`

- [x] 2.1 Add package `id.nearyou.app.core.domain.ratelimit` under `core/domain/src/main/kotlin/` (note: `:core:domain` is JVM-only in this repo, not KMP, so the path is `src/main/kotlin/` not `src/commonMain/kotlin/` — minor spec drift, behavior-equivalent)
- [x] 2.2 Define sealed interface `Outcome` with `Allowed(remaining: Int)` and `RateLimited(retryAfterSeconds: Long)` — match V9 `ReportRateLimiter.Outcome` shape byte-for-byte (declared as nested types inside `RateLimiter` interface; same Kotlin shape, namespaced under the interface)
- [x] 2.3 Define interface `RateLimiter` with `tryAcquire(userId: UUID, key: String, capacity: Int, ttl: Duration): Outcome` and `releaseMostRecent(userId: UUID, key: String)`
- [x] 2.4 Verify the interface compiles in `:core:domain` without any Redis or vendor SDK imports — verified by inspection: `RateLimiter.kt` imports only `java.time.Duration` and `java.util.UUID`
- [x] 2.5 Add `RateLimiterTest` (in `core/domain/src/test/kotlin/`, JVM tests not commonTest per the JVM-only module) — interface contract tests using a fake in-process implementation. Covers: `Allowed.remaining` decrement across slots, 11th call returns RateLimited with retryAfterSeconds ≥ 1, releaseMostRecent restores the slot, releaseMostRecent on empty bucket is no-op, different keys are independent, different users on the same key are independent.

## 3. `computeTTLToNextReset(userId)` shared helper

- [x] 3.1 Add `computeTTLToNextReset(userId: UUID, now: Instant = Instant.now()): Duration` in `id.nearyou.app.core.domain.ratelimit`
- [x] 3.2 Implement: `offset_seconds = abs(userId.hashCode().toLong()) % 3600` (`.toLong()` cast prevents `Int.MIN_VALUE.absoluteValue == Int.MIN_VALUE` overflow); compute next 00:00 in `Asia/Jakarta` at-or-after `now` (always tomorrow's midnight, since `todayMidnight ≤ nowZdt` always holds and the spec carve-out forces forward-roll on exact-midnight); return `Duration.between(nowZdt, effectiveReset)`.
- [x] 3.3 Add `ComputeTtlToNextResetTest` covering: same user same offset across calls, pure-function determinism, offset bounded `[0, 3600)` at fixed `now = 2026-01-01T00:00:00Z` (1000 random users), different users different offsets at high probability (≥ 999 of 1000), midnight-crossing rollover (just-before vs just-after WIB midnight), exactly-at-midnight WIB carve-out (TTL = 24h with offset 0), strictly-positive-Duration spot check.
- [x] 3.4 Verify the helper has zero file/network/DB I/O — only `java.time` + the userId argument — verified by inspection: imports are `java.time.Duration`, `java.time.Instant`, `java.time.ZoneId`, `java.util.UUID`, `kotlin.math.absoluteValue`. No I/O.

## 4. Redis-backed `RateLimiter` implementation

- [x] 4.1 Implement `RedisRateLimiter` in `:infra:redis/src/main/kotlin/` against the `RateLimiter` interface
- [x] 4.2 Embed Lua script: `ZREMRANGEBYSCORE` (prune) → `ZCARD` (count) → branch (oldest via `ZRANGE 0 0 WITHSCORES` + `Retry-After` math, OR `ZADD now jti` + `PEXPIRE ttl`)
- [x] 4.3 Wrap the Lua call in `withContext(Dispatchers.IO)` so the Ktor coroutine dispatcher stays unblocked — landed at the call site in `LikeService.like(...)` (the surrounding coroutine context is known there; the `RateLimiter` interface stays non-suspending per V9 contract preservation). Both `tryAcquire` and `releaseMostRecent` are wrapped. Documented in `RedisRateLimiter.kt` KDoc + `LikeService.kt` KDoc.
- [x] 4.4 Use a per-call `UUID.randomUUID().toString()` as the sorted-set member to avoid same-millisecond collisions
- [x] 4.5 Implement `releaseMostRecent` as `ZPOPMAX key 1` (atomic, single-call)
- [x] 4.6 Wire the `RedisRateLimiter` as the production `RateLimiter` Koin binding in `:backend:ktor` Application setup — **shipped as fail-soft**: `Application.kt` resolves `secretKey(env, "redis-url")` and binds `RedisRateLimiter` when the slot is set, otherwise binds `NoOpRateLimiter` for dev/test. In `staging`/`production` envs, missing slot is a hard startup failure (`require(...)` throws). Task 1.7 (GCP slot verification) remains user-blocked but does NOT block dev/test runs. Spec drift documented in `Application.kt` comment + this tasks.md note. The `staging-deploy.yml` workflow already injects `REDIS_URL` from the `staging-redis-url` secret slot, so the staging deploy will fail loudly if the slot is missing.
- [x] 4.7 Add `redis:7-alpine` service container to `.github/workflows/ci.yml` `test` job (mirroring the `postgis/postgis:16-3.4` setup). Also injects `REDIS_URL=redis://localhost:6379` env var into the test step.
- [x] 4.8 Boot Redis once per test JVM via `KotestProjectConfig.beforeProject()` — landed: `KotestProjectConfig.bootstrapRedis()` mirrors the Postgres probe-then-skip pattern. If `REDIS_URL` env-or-system-property is unset, probe `localhost:6379` (500ms TCP timeout) and on success export the system property so `EnvVarSecretResolver` finds it on Application startup. Probe failure → no-op (NoOpRateLimiter binding takes over per task 4.6).
- [x] 4.9 Implement `RedisRateLimiterIntegrationTest` (`:infra:redis/src/test/kotlin/`, tagged `database`) covering all 10 scenarios from `rate-limit-infrastructure/spec.md` § Test coverage: empty-bucket admit-then-reject (capacity 10), Retry-After math within ±5s using a frozen Clock, releaseMostRecent restores slot, releaseMostRecent on empty bucket is no-op, parallel-coroutine concurrent capacity-boundary (10 admit / 5 reject), old-entry pruning, hash-tag CRC16 equivalence via `Lettuce.SlotHash.getSlot`, two same-millisecond inserts both land via random JTI, bucket-over-capacity preserved on cap reduction, V9 contract subsumption (10-succeed / 11th-429 / 409-release-on-no-op). 10/10 scenarios green.

## 5. Hash-tag key format Detekt rules

- [x] 5.1 Implement `RateLimitTtlRule` in `lint/detekt-rules/src/main/kotlin/id/nearyou/lint/detekt/` — fires on `tryAcquire` calls whose key string contains `_day}` or matches `^\{scope:rate_[a-z_]+_day\}` AND whose `ttl` argument is not a `computeTTLToNextReset(...)` call. Looks up `key` and `ttl` arguments by parameter name first, then by positional index (1 and 3). Allowlist via `/src/test/` path + `@AllowDailyTtlOverride` annotation (with non-empty reason).
- [x] 5.2 Implement `RedisHashTagRule` in the same package — visits `KtStringTemplateExpression`; fires on literals starting with `rate:` (legacy) OR starting with `{scope:` but not matching the strict `^\{scope:[^{}]+\}:\{[^{}:]+:[^{}]+\}$` pattern. Allowlist via `/src/test/` path + `@AllowRawRedisKey` annotation (with non-empty reason).
- [x] 5.3 Define `@AllowDailyTtlOverride(reason: String)` and `@AllowRawRedisKey(reason: String)` in `core/domain/src/main/kotlin/id/nearyou/app/core/domain/ratelimit/Annotations.kt`. Both declare `reason` as a non-nullable `String` parameter (no default) so callers cannot syntactically omit it; both Detekt rules additionally reject empty-string and blank reasons (after stripping surrounding `"` or `"""`) to close the silent-bypass loophole.
- [x] 5.4 Register both rules in `NearYouRuleSetProvider` alongside `RawFromPostsRule`, `BlockExclusionJoinRule`, `CoordinateJitterRule`.
- [x] 5.5 `RateLimitTtlRuleTest`: 11 scenarios — daily-key + hardcoded `Duration.ofDays(1)` fires, daily-key (substring `_day}`) + hardcoded ttl fires, daily-key with named arguments + hardcoded ttl fires, daily-key + `computeTTLToNextReset` passes, burst-key + hardcoded ttl passes, non-tryAcquire callee passes, test-path passes, `@AllowDailyTtlOverride("non-empty reason")` passes, `@AllowDailyTtlOverride("")` still fires, synthetic-file harness via `id.nearyou.test.fixtures.*` passes, rule-registered-via-provider. 11/11 green.
- [x] 5.6 `RedisHashTagRuleTest`: 14 scenarios — legacy `rate:user:` fires, legacy `rate:guest_issue:ip:` fires, malformed `{scope:rate_x}:user:id` fires, malformed `{scope:rate_x}` (no second segment) fires, conforming `{scope:rate_like_day}:{user:id}` passes, conforming `{scope:rate_like_burst}:{user:id}` passes, conforming V9 `{scope:rate_report}:{user:id}` passes, unrelated SQL string passes, file path string passes, test-path passes, `@AllowRawRedisKey("non-empty reason")` passes, `@AllowRawRedisKey("")` still fires, synthetic-file harness via `id.nearyou.test.fixtures.*` passes, rule-registered-via-provider. 14/14 green.

**Note on `RateLimitKey` value-class:** an earlier draft of this change proposed a `RateLimitKey(scope, axis, value)` value class in `:core:domain` for caller convenience. Dropped because (a) the Detekt rule enforces the shape directly on string literals, making the value class an unenforced parallel API, and (b) value-class adoption would either require all callers to migrate (out of this change's scope) or remain optional (orphan abstraction). String literals + Detekt enforcement is sufficient.

## 6. Apply rate limit to `POST /api/v1/posts/{post_id}/like`

- [x] 6.1 Added `RateLimiter` + `RemoteConfig` to `LikeService` constructor — DI matches the V9 `ReportService` injection precedent (service-owned, not route-owned). `LikeRoutes.likeRoutes` is unchanged in shape; the route-handler delegates to the service via the new `LikeService.Result` sealed type and maps `Result.RateLimited(retryAfterSeconds)` to 429 + `Retry-After` header.
- [x] 6.1.1 Verified the auth-jwt principal — it did NOT carry `subscription_status`. Added it: (a) `subscriptionStatus: String = "free"` on `UserRow` (default keeps existing constructors compiling), (b) `subscription_status` added to `JdbcUserRepository.baseSelect`, (c) `subscriptionStatus: String` on `UserPrincipal`, populated from `user.subscriptionStatus` in `AuthPlugin.configureUserJwt`. Tier is now read from the principal, never via a fresh DB SELECT in the like handler.
- [x] 6.2 Daily limiter implemented: skips entirely if `subscriptionStatus in ('premium_active', 'premium_billing_retry')`; otherwise resolves cap from `remoteConfig.getLong("premium_like_cap_override")` with default-10 fallback on unset/null/zero/oversized/error; calls `tryAcquire(userId, "{scope:rate_like_day}:{user:$userId}", cap, computeTTLToNextReset(userId))` inside `withContext(Dispatchers.IO)`. On `RateLimited`: returns `Result.RateLimited(retryAfterSeconds)` and the route maps to 429 + `Retry-After`.
- [x] 6.3 Burst limiter implemented: `tryAcquire(userId, "{scope:rate_like_burst}:{user:$userId}", capacity = 500, ttl = Duration.ofHours(1))` inside `withContext(Dispatchers.IO)`. On `RateLimited`: returns `Result.RateLimited(retryAfterSeconds)`.
- [x] 6.4 No-op release path: when `likes.likeInTx(...)` returns false (0 rows affected = re-like), call `releaseMostRecent` on BOTH keys unconditionally inside one `withContext(Dispatchers.IO)`. Daily-key release on Premium is a no-op against the empty bucket per the `RateLimiter` contract; called unconditionally for tier-flip-mid-day safety.
- [x] 6.5 Ordering verified: auth (Ktor `authenticate(AUTH_PROVIDER_USER)`) → UUID validation (`parseUuid`) → daily limiter (Free only) → burst limiter (both) → `likes.resolveVisiblePost` (`Result.NotFound` does NOT release) → `INSERT ... ON CONFLICT DO NOTHING` → release-on-no-op (re-like) → notification emit (V10 strict-coupling: emit + INSERT in one TX; commit synchronous; dispatch fire-and-forget post-commit via `dispatcher.dispatch`). The non-blocking guarantee for FCM is the dispatcher impl's responsibility (the current `NoopNotificationDispatcher` is non-blocking; a future FCM impl will spawn its own coroutine on `Dispatchers.IO`). Wrapping `dispatcher::dispatch` in `withContext(Dispatchers.IO)` at the service-level call site would NOT help — it would block inside the wrap if the dispatcher itself blocked.
- [x] 6.6 Created `id.nearyou.app.config.RemoteConfig` interface + `StubRemoteConfig` default impl (always returns `null`). `LikeService.resolveDailyCap(userId)` resolves the flag, coerces unset/null/non-positive/oversized to default 10, slf4j WARN-logs each fallback path. The "Sentry WARN" target is the slf4j WARN log line — a future Sentry integration will pick those up (no Sentry SDK is yet on the classpath, mirrors the existing `:backend:ktor` log-via-slf4j pattern).
- [x] 6.7 Audit log on override-applied path: `LikeService.resolveDailyCap(userId)` logs `event=remote_config_override_applied key=premium_like_cap_override value=<cap> user_id=<uuid>` at INFO when a non-default cap is honored.
- [x] 6.8 Notification-emit-does-not-block integration test — covered by `LikeRateLimitTest` scenario 20 (section 8). The slow-FCM-stub fixture asserts the like response returns 204 within the request budget regardless of downstream dispatcher latency.

## 7. Port V9 `ReportRateLimiter` to the shared infra

- [x] 7.1 Replaced the `ConcurrentHashMap`-backed body of `ReportRateLimiter`. `tryAcquire` and `releaseMostRecent` now delegate to an injected `RateLimiter` (the shared interface from `:core:domain`). The V9 in-process algorithm itself is preserved — extracted into `InMemoryRateLimiter` in `:core:domain/src/main/kotlin/.../ratelimit/` — and remains the wrapper's default constructor argument so existing `ReportRateLimiter()` calls in tests keep working byte-for-byte.
- [x] 7.2 V9 public surface preserved byte-for-byte: `Outcome.Allowed(remaining)` / `Outcome.RateLimited(retryAfterSeconds)` shape unchanged, `keyFor(userId)` companion returns `{scope:rate_report}:{user:<uuid>}`, `cap = DEFAULT_CAP = 10`, `window = DEFAULT_WINDOW = 1h`. The `tryAcquire(userId): Outcome` and `releaseMostRecent(userId)` signatures are the same. Backward-compatible additive change: the new constructor parameter `rateLimiter: RateLimiter = InMemoryRateLimiter()` defaults to the V9 algorithm so callers that pass no args still get V9 semantics.
- [x] 7.3 KDoc rewritten. The "deferred to a separate change" block is gone; replaced with a description pointing at the `rate-limit-infrastructure` capability and the `like-rate-limit` change. The new KDoc explicitly documents the test gate split (V9's `ReportEndpointsTest` is the byte-for-byte HTTP-level regression gate; Lua-level correctness lives in `:infra:redis`'s `RedisRateLimiterIntegrationTest`).
- [x] 7.4 **Spec drift acknowledged**: V9 had no separate `ReportRateLimiterTest` unit class to re-point — its rate-limit assertions live entirely in `ReportEndpointsTest` (HTTP-level, database-tagged). Instead, the canonical in-memory test double now lives in `:core:domain/src/main/kotlin/.../ratelimit/InMemoryRateLimiter.kt` (extracted from V9's algorithm), used by both `:core:domain`'s `RateLimiterTest` (de-duped — its private inline copy was deleted) AND the wrapper's default constructor. The `:core:domain` `RateLimiterTest` thus also functions as a unit-speed regression gate for the shared sliding-window algorithm.
- [x] 7.5 Verified `ReportEndpointsTest` (database-tagged, runs against Postgres + the new Redis service container per task 4.7) passes byte-for-byte against the ported `ReportRateLimiter`. The wrapper's `tryAcquire(userId): Outcome` shape preservation means no test-side changes were needed — the existing scenarios (rate-limited 429, Retry-After header, 409-release behavior) all pass against the V9 in-process algorithm now living in `InMemoryRateLimiter`.

## 8. Tests for `POST /like` rate limit

- [x] 8.1 Implemented `LikeRateLimitTest` (`backend/ktor/src/test/kotlin/id/nearyou/app/engagement/LikeRateLimitTest.kt`), tagged `database`. **Spec drift acknowledged**: the test runs against an in-memory [`InMemoryRateLimiter`] rather than a real Redis container — per Decision 7, the test gate is split. The Lua sliding-window correctness path is exercised by `:infra:redis`'s `RedisRateLimiterIntegrationTest` (10 scenarios against real Redis); this class is the HTTP-level + service-level gate testing what `LikeService` does *with* the limiter. In-memory is byte-equivalent to Redis (V9 sliding-window algorithm), drastically faster, and deterministic for Retry-After math. The original tasks.md wording ("depends on Redis service container") was updated to reflect this rationale.
- [x] 8.2 Covers all 21 scenarios from the post-likes spec § Integration test coverage requirement, named in `@Test`-equivalent strings 1–21 verbatim. 21/21 green.
- [x] 8.3 Scenario 18 (WIB rollover) uses a frozen `AtomicReference<Instant>` clock injected into `InMemoryRateLimiter`; advances exactly past `computeTTLToNextReset(userId)` + 1 second to verify the per-user reset moment restores the cap. Scenario 3 asserts Retry-After within ±5s of `computeTTLToNextReset(userId, now).seconds`.
- [x] 8.4 Scenario 17 asserts the daily key is exactly `{scope:rate_like_day}:{user:<uuid>}` and burst key is exactly `{scope:rate_like_burst}:{user:<uuid>}` via a `SpyRateLimiter` that captures every `tryAcquire` key.
- [x] 8.5 Pre-push gauntlet green: `./gradlew ktlintCheck detekt :backend:ktor:test :lint:detekt-rules:test :infra:redis:test :core:domain:test -PincludeMobile=false` — BUILD SUCCESSFUL. Aggregate: 511 tests across all modules (backend/ktor 405, core/domain 13, infra/redis 10, lint/detekt-rules 83), 0 failures.

## 9. Pre-push + CI verification

- [x] 9.1 `openspec validate like-rate-limit --strict` — green at HEAD `728b9a8`.
- [x] 9.2 `./gradlew ktlintCheck detekt -PincludeMobile=false` — green. Both new Detekt rules (`RateLimitTtlRule`, `RedisHashTagRule`) registered via `NearYouRuleSetProvider` and produce 0 findings on the codebase. ktlint clean across all modules.
- [x] 9.3 `./gradlew :backend:ktor:test :infra:redis:test :core:domain:test :lint:detekt-rules:test -PincludeMobile=false` — green. Aggregate **511 tests across 4 modules, 0 failures, 0 errors** (backend/ktor: 405 incl. LikeRateLimitTest 21/21 + ReportEndpointsTest 23/23, core/domain: 13, infra/redis: 10, lint/detekt-rules: 83 incl. RateLimitTtlRuleTest 11/11 + RedisHashTagRuleTest 14/14).
- [x] 9.4 V9 suite green against ported impl. **Spec-drift documented**: the original task referenced Gradle's `--tests` flag, but Kotest spec discovery in this repo uses the `-Dkotest.filter.specs=*<SpecName>*` system property (wired in each module's `tasks.withType<Test>` config). The equivalent invocation is `./gradlew :backend:ktor:test --rerun-tasks "-Dkotest.filter.specs=*ReportEndpointsTest"` → 23/23 green. There is no `ReportRateLimiterTest` class — see task 7.4 for why (V9 had no separate unit test class to re-point; HTTP-level assertions live in ReportEndpointsTest).
- [x] 9.5 Git tree clean: only the proposal directory + the section-1-through-8 implementation files staged across the 9 commits. No `.gradle/`, no `.idea/`, no debug logs. Untracked `.claude/worktrees/` is local-only infrastructure (worktree of PR #38), correctly out of the index.
- [x] 9.6 CI green on HEAD `43f320c`: `lint` SUCCESS, `build` SUCCESS, `test` SUCCESS (Redis + Postgres service containers both healthy throughout), `migrate-supabase-parity` SUCCESS. All 4 jobs COMPLETED — confirmed via `gh pr view 37 --json statusCheckRollup` post-monitor.
- [ ] 9.7 Wait for staging deploy (`deploy-staging.yml`) green; smoke test: hit 10 likes from a Free synthetic account, expect 11th to return 429 + `Retry-After`. **Task 1.7 unblocked** (staging-redis-url slot verified 2026-04-25). The staging deploy fires automatically on squash-merge to `main`; smoke test is executed by the user (requires a JWT for a seeded Free synthetic account in the staging DB). Will tick after smoke test confirms 429 + `Retry-After` on the 11th like.

## 10. Doc sync (in archive PR, not feat PR)

- [x] 10.1 Updated `docs/08-Roadmap-Risk.md` Phase 1 item 24 — appended a "Layer 2 infrastructure shipped" cross-reference noting Lettuce-backed `RedisRateLimiter`, `RateLimiter` interface, `computeTTLToNextReset` helper, hash-tag key standard, both Detekt rules, and the V9 `ReportRateLimiter` port. Layers 1 / 3 / 4 remain explicitly deferred.
- [x] 10.2 Updated `docs/08-Roadmap-Risk.md` Phase 2 item 3 — appended "Like rate limit shipped" cross-reference covering the 10/day Free + 500/hour burst cap, `premium_like_cap_override` flag, and the auto-hide trigger (already shipped in V9). Reply rate limit (20/day Free) explicitly flagged as deferred to a separate future change.
- [x] 10.3 Updated `docs/05-Implementation.md` § Rate Limiting Implementation — added a "shipped" callout block above Layer 1 pointing at `openspec/specs/rate-limit-infrastructure/spec.md` for the canonical contract, summarizing the production binding (`RedisRateLimiter` + Lua + hash-tag keys + `withContext(Dispatchers.IO)` at the call site), the `:core:domain` interface placement, the V9 `ReportRateLimiter` port, and the Detekt-rule enforcement.
- [x] 10.4 No edit needed for `docs/01-Business.md` — verified the like-cap row at line 55 (`Like | 10/day | Unlimited | 500/hour (both tiers, anti-bot)`) and the cross-row at line 17 (`Likes per day | 10 (cap 500/hour burst) | Unlimited (cap 500/hour burst) | Month 1`) already match what shipped. The cost-forecast row + the Phase-2 cross-reference paragraph at line 76 also match.
- [x] 10.5 Ran `openspec archive like-rate-limit --yes`. Output: `+ 14 added`, `~ 1 modified`, `- 0 removed`, `→ 0 renamed` across `openspec/specs/post-likes/spec.md` (+6 ADDED), `openspec/specs/rate-limit-infrastructure/spec.md` (+8 ADDED — new capability), `openspec/specs/reports/spec.md` (~1 MODIFIED). Change directory moved to `openspec/changes/archive/2026-04-25-like-rate-limit/`. Note: per the **one-PR-per-change convention** codified in PR #38, this archive commit lands on the same `like-rate-limit` branch as the proposal + feat commits — NOT a separate archive PR (the original task wording predates the convention update).
