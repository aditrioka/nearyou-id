## 1. `:shared:distance` multiplatform-ization (coordinate-jitter delta)

- [x] 1.1 Pre-implementation re-check (per `openspec/project.md` § Apply-phase / pre-impl re-check): fresh dated WebSearch confirming the canonical Kotlin/Native iOS HMAC-SHA256 pattern (CommonCrypto `CCHmac` via `platform.CoreCrypto` + `usePinned`) is still current; drop a one-line evidence note in the first feat commit body. (No new `libs.versions.toml` pin enters, so this is a confirm-only check.)
- [x] 1.2 Make `shared/distance/build.gradle.kts` multiplatform-consumable (mirror `shared/resources/build.gradle.kts`): apply `alias(libs.plugins.androidLibrary)`; add `androidTarget { compilerOptions { jvmTarget JVM_11 } }` + `iosArm64()` + `iosSimulatorArm64()` alongside the existing `jvm()` (NO `binaries.framework` block — `:shared:distance` is a transitive dependency of `:mobile:app`, not directly Xcode-consumed); add an `android { namespace = "id.nearyou.distance"; compileSdk; defaultConfig { minSdk }; compileOptions JVM_11 }` block. Remove the now-satisfied "deferred to the mobile change" comment.
- [x] 1.3 Add `androidMain` `actual fun hmacSha256` (`javax.crypto.Mac` "HmacSHA256") + `actual fun unixMillis` (`System.currentTimeMillis()`) — share with `jvmMain` via an intermediate source set OR duplicate the ~6 lines (build-structure choice; verify against the convention plugin).
- [x] 1.4 Add `iosMain` `actual fun hmacSha256` via CommonCrypto `CCHmac(kCCHmacAlgSHA256, …)` (`platform.CoreCrypto`, `usePinned` for key/msg/32-byte digest, `@OptIn(ExperimentalForeignApi::class)`, handle the empty-array pinning edge) + `actual fun unixMillis` via Foundation `NSDate` epoch.
- [x] 1.5 Add a `commonTest` HMAC known-answer test using **`kotlin.test`** (`@Test` + `assertContentEquals`, NOT Kotest `StringSpec` — Kotest runs via JUnit5/`useJUnitPlatform()` = JVM/Android only and would NOT execute on Native, leaving the iOS CommonCrypto actual unverified). Pin it to a published **RFC 4231 HMAC-SHA-256 vector** (e.g., Test Case 2: key=`"Jefe"`, data=`"what do ya want for nothing?"`, expected `5bdcc146bf60754e6a042426089575c75a003f089d2739839dec58b964ec3843`) so all three actuals check against a known-correct value, not just each other.
- [x] 1.6 Add `implementation(projects.shared.distance)` to `mobile/app/build.gradle.kts` `commonMain.dependencies`.
- [x] 1.7 Verify on all targets — `./gradlew :shared:distance:iosSimulatorArm64Test :shared:distance:testDebugUnitTest :shared:distance:jvmTest` (the iOS sim test MUST run, not just `:build` which compiles-without-running Native tests; this is what actually exercises the CommonCrypto `hmacSha256` actual against the RFC 4231 vector). Confirm `:backend:ktor` still compiles (it only ever used the JVM target — `JitterEngine`/`UuidV7` unchanged in `commonMain`).

## 2. Bahasa Indonesia strings (shared-resources delta)

- [x] 2.1 Add to `shared/resources/src/commonMain/composeResources/values/strings.xml`: `timeline_nearby_title` = "Post dari lokasi ini", `timeline_loading` = "Sedang memuat postingan…", `timeline_empty_nearby` = "Area kamu belum ramai. Sementara lihat dari seluruh Indonesia dulu?" (all three byte-identical to the cited docs). 
- [x] 2.2 Add the derived rate-limit strings `timeline_limit_hard` + `timeline_limit_soft` (Bahasa Indonesia, consistent with the Mobile #3/#4 register; flagged for UX review in design Open Questions).
- [x] 2.3 Confirm `home_placeholder_title` / `home_placeholder_version` remain in `strings.xml` (retained, no longer rendered) and no earlier string text is altered. Verify the generated `Res.string.*` accessors compile for the new keys.

## 3. Networking DTOs + `NearbyTimelineApiClient`

- [x] 3.1 Define `@Serializable` response DTOs in `mobile/app/src/commonMain/.../timeline/`, **mirroring the SHIPPED wire in `backend/.../timeline/TimelineRoutes.kt` (`NearbyPostDto`/`NearbyResponse`) + `Upsell.kt` EXACTLY — regenerate field names from that source, NOT from the `nearby-timeline` spec's snake_case JSON example** (the shipped wire is mixed-case; see design D10). Per-post: bare camelCase `id`, `authorUserId`, `content`, `latitude: Double`, `longitude: Double`, `distanceM: Double`, `createdAt: String` (NO `@SerialName`), plus `@SerialName("city_name") cityName: String`, `@SerialName("liked_by_viewer") likedByViewer: Boolean`, `@SerialName("reply_count") replyCount: Int`. Top-level: `posts: List<…>`, bare `nextCursor: String? = null`, `upsell: UpsellDto? = null`. `UpsellDto(soft: Boolean = false, hard: Boolean = false)` (bare). `nextCursor`/`upsell` tolerate absence/null (`ignoreUnknownKeys` + `explicitNulls = false` already on the shared `Json`). MockEngine test fixtures MUST use the mixed-case wire keys.
- [x] 3.2 Implement `NearbyTimelineApiClient.fetchNearby(lat, lng, radiusM, sessionId, cursor: String? = null)` → `client.get("/api/v1/timeline/nearby") { parameter("lat",…); parameter("lng",…); parameter("radius_m",…); cursor?.let { parameter("cursor", it) }; header("X-Session-Id", sessionId) }`; return a status-tagged result type (mirror `AuthApiClient`'s `*ApiResult`), NOT a thrown exception, for non-2xx.
- [x] 3.3 Define `NEARBY_RADIUS_M = 20000` as a single named constant (the Free-tier fixed radius); no magic literal at the call site.

## 4. Repository, outcome mapping, providers

- [x] 4.1 Define the sealed `NearbyTimelineOutcome` (`Loaded(posts, nextCursor, upsell)`, `NetworkError`, `Error`) and the `NearbyTimelineFlow` interface (`suspend fun loadFirstPage(): NearbyTimelineOutcome`).
- [x] 4.2 Implement `NearbyTimelineRepository : NearbyTimelineFlow` mapping HTTP **status** → outcome with no generic fallthrough: 200 → `Loaded` (carrying parsed `upsell`); 400 → retryable `Error` + logged diagnostic; 5xx / IO → `NetworkError`; 401 delegated to the shipped `Auth` plugin (NOT reimplemented). Reuse `LocationProvider` + `SessionIdProvider` + `NEARBY_RADIUS_M`.
- [x] 4.3 Define the commonMain `LocationProvider` interface (`suspend fun current(): LatLng`, reusing `id.nearyou.distance.LatLng`) and `StubLocationProvider` returning `LatLng(-6.2, 106.8)`. Do NOT reference any platform location/permission API.
- [x] 4.4 Define `SessionIdProvider` whose session id is computed EXACTLY ONCE and captured in a `val`/field at construction (`val id = Uuid.random().toString()`, `@OptIn(ExperimentalUuidApi::class)`) — NOT recomputed per access (a fresh UUID per call opens a new soft-cap Redis bucket every request, so the 50/session soft cap never engages). Registered as a Koin `single`. Confirm the value matches `^[A-Za-z0-9-]{1,64}$`.

## 5. Pure UI state + projection

- [x] 5.1 Define the Compose-free `NearbyTimelineUiState` (loading / content+posts / empty / hard-limit / soft-limit+posts / error) and a pure `nearbyTimelineUiState(outcome, inFlight)` projection (mirror `AgeGateUiState`). Carry NO PII (no `author_user_id`, no coordinates) into the state.
- [x] 5.2 Map the rate-limit presentation from the parsed `upsell` on a `Loaded` outcome (empty + `hard` → hard-limit; non-empty + `soft` → soft-limit+banner), distinct from the genuinely-empty (`Loaded` empty, no upsell) state.

## 6. `NearbyTimelineScreen` + `HomeScreen` host

- [x] 6.1 Implement `NearbyTimelineScreen` (`mobile/app/src/commonMain/.../screens/timeline/NearbyTimelineScreen.kt`): inject `NearbyTimelineFlow` via `koinInject`; hold outcome + inFlight via `remember`/`LaunchedEffect`; render under `NearYouTheme`; top bar `timeline_nearby_title`; `LazyColumn` of post cards inside a Material 3 `PullToRefreshBox`. Every string via `stringResource` (zero literals).
- [x] 6.2 Implement the read-only post card (D9 visual pattern): `content`, a metadata row (`city_name` + coral location-pin glyph + `DistanceRenderer.render(distance_m)` + relative `created_at`), and a read-only counts row (`liked_by_viewer` heart state + `reply_count`). Render NO `author_user_id`, NO raw lat/lng. Use `MaterialTheme.colorScheme.*` + `locationPin` + `NearYouTypography`.
- [x] 6.3 Render the six states per the screen-state-mapping spec: loading skeleton + `timeline_loading`; content list; empty + `timeline_empty_nearby`; error + `signin_error_network` + `cta_retry` button (re-invokes load); hard-limit + `timeline_limit_hard` (no cards); soft-limit banner + `timeline_limit_soft` (above cards).
- [x] 6.4 Wire pull-to-refresh to re-invoke `loadFirstPage()`. Parse + retain `next_cursor` on `Loaded` but do NOT wire load-more (deferred).
- [x] 6.5 Repurpose `HomeScreen.Content()` to render `NearbyTimelineScreen` (drop the placeholder logo/title/version block); stop referencing `home_placeholder_*`. Do NOT edit `RootRouterScreen` routing.

## 7. Koin wiring

- [x] 7.1 Register in `MobileModule.kt`: `single { NearbyTimelineApiClient(get()) }`, `single { NearbyTimelineRepository(get(), get(), get()) }`, `single<NearbyTimelineFlow> { get<NearbyTimelineRepository>() }`, `single<LocationProvider> { StubLocationProvider() }`, `single { SessionIdProvider() }`.

## 8. Tests

- [x] 8.1 `NearbyTimelineUiStateTest` (commonTest): the pure projection maps each of in-flight / loaded-non-empty / loaded-empty-no-upsell / loaded-empty-hard / loaded-non-empty-soft / NetworkError to its state, deterministically; AND assert the projected state carries no PII (no `authorUserId`, no raw lat/lng).
- [x] 8.2 MockEngine `NearbyTimelineApiClientTest` / `NearbyTimelineRepositoryTest` (commonTest): assert the request path `/api/v1/timeline/nearby` + `lat`/`lng`/`radius_m=20000` params + no first-page `cursor`; the `X-Session-Id` header is sent and matches the regex. **Parse against the SHIPPED mixed-case wire** (fixtures use `authorUserId`/`distanceM`/`createdAt`/`nextCursor` camelCase + `city_name`/`liked_by_viewer`/`reply_count` snake — see 3.1); assert all 10 post fields + `nextCursor` (present AND a null/absent case) + optional `upsell` (present soft, present hard, absent) parse; add a regression case that a snake_case-only `author_user_id`/`distance_m`/`created_at`/`next_cursor` body does NOT populate those fields (guards the stale-spec assumption); `city_name = ""` parses to empty string. Status→outcome mapping (200 Loaded incl. hard-cap empty+upsell.hard, 400 retryable+log, 5xx/IO NetworkError) with no generic fallthrough.
- [x] 8.3 `NearbyTimelineScreenTest` (androidUnitTest, Robolectric `runComposeUiTest`, `KoinContext` + `FakeNearbyTimelineFlow`): initial render shows `timeline_nearby_title`; each of the six states renders its asserted node(s); error retry is clickable; a card with `distanceM = 7600.0` renders a "8km" node (the shared `DistanceRenderer` output, asserted at card level); a post with `city_name = ""` renders NO city label and no literal `""`; `author_user_id` + raw coordinates are NOT in the rendered tree.
- [x] 8.4 Add `**/NearbyTimelineScreenTest*` (and `**/HomeScreenTest*` if 8.7 makes it a Robolectric UI test) to the `mobile/app/build.gradle.kts` `tasks.withType<Test>()` Release-variant exclude block (per the established `*ScreenTest` convention). Verify `:mobile:app:testDevReleaseUnitTest` passes (not just Debug).
- [x] 8.5 Add/confirm a `FakeNearbyTimelineFlow` (commonTest) returning fixed outcomes + counting invocations (mirror `FakeAuthFlow`); a `SessionIdProvider` test asserting two reads return the identical id (capture-once per 4.4).
- [x] 8.6 Full local gate: `./gradlew ktlintCheck detekt :mobile:app:testDebugUnitTest :mobile:app:testDevReleaseUnitTest :shared:distance:iosSimulatorArm64Test :shared:distance:testDebugUnitTest`.
- [x] 8.7 `HomeScreen` host coverage: the existing `HomeScreenTest` (commonTest) currently only smoke-checks `HomeScreen().key` — it survives the repurpose but no longer asserts behavior. Either fold a host-delegation assertion into it (composes `HomeScreen().Content()` → `timeline_nearby_title` node present, `home_placeholder_title` node absent) or add that assertion to `NearbyTimelineScreenTest`. Do NOT leave it silently stale.

## 9. No-hardcoded-strings verification

- [x] 9.1 Run the documented grep step (per `shared-resources` spec § negative-requirement grep) over `mobile/app/src/commonMain`, `androidMain`, `iosMain` for the new timeline source; confirm zero hardcoded UI string literals (every UI string via `stringResource(Res.string.X)`).

## 10. Pre-archive staging smoke (runtime-impacting → smoke before archive)

- [ ] 10.1 Build + run the app against `dev`/`staging` (per `dev/docs` runbook + the local-run memory): sign in → land on `HomeScreen`/Nearby → confirm posts render with distance + city + counts (or the empty/error state) against a real backend.
- [x] 10.2 (N/A — mobile-only, no new backend route; mobile client runs against existing staging API) If a staging branch deploy is used: `gh workflow run deploy-staging.yml --ref mobile-nearby-timeline-screen` → poll → smoke. (Mobile-only change with no new backend route — staging smoke is mobile-client-against-existing-staging-API; mark backend-deploy tasks N/A.)

## 11. FOLLOW_UPS + docs

- [x] 11.1 Add `FOLLOW_UPS.md` entries: `mobile-location-permission-flow` (real GPS + permission + UU-PDP consent modal + denial fallback; cite `docs/03-UX-Design.md` § Location Permission / § Permission Denial Fallback), `mobile-nearby-radius-slider` (10/20/50/100 km slider + Free-bounce/Premium-pick), `mobile-nearby-timeline-infinite-scroll` (cursor pagination / load-more), `mobile-timeline-empty-global-cta` (empty-state switch-to-Global affordance once a Global screen exists), `timeline-response-dto-casing-drift` (backend `NearbyPostDto`/`FollowingPostDto`/`GlobalPostDto` + their `*Response` emit camelCase `authorUserId`/`distanceM`/`createdAt`/`nextCursor` while the `nearby-timeline`/`following-timeline`/`global-timeline` specs' Response-shape JSON examples show snake_case — backend owner to reconcile: add `@SerialName` to match the specs + coordinate a mobile DTO update, OR amend the specs' JSON examples). **FOLLOW_UPS.md is already at its 30-entry soft cap — run `/triage-follow-ups` as part of this step (prune resolved entries) before/while adding these 5, per the file's header rule.**
- [x] 11.2 No new module added → README/`module-descriptions.txt` sync not required; confirm `dev/scripts/sync-readme.sh --check` stays clean (the `:shared:distance` target additions don't change the module list).
- [x] 11.3 `openspec validate mobile-nearby-timeline-screen --strict` green; PR body updated to reflect implementation scope at the first feat commit (retitle to `feat(mobile): mobile-nearby-timeline-screen`).
