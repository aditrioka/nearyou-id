## 1. Library + secret-slot prep

- [x] 1.1 Pre-implementation library re-check (2026-05-28): both `com.password4j:password4j 1.8.4` and `dev.samstevens.totp:totp 1.7.1` remain the best-in-class choices per the fresh dated WebSearch — Password4j has OWASP-aligned recommended params, JVM-native, single coordinate; samstevens-totp has the QR + NTP-sync feature surface needed for future enrollment UI. No ecosystem shift since proposal-time direction. Proceeding with the design-time call.
- [x] 1.2 Added `password4j = "1.8.4"` + `samstevens-totp = "1.7.1"` to `gradle/libs.versions.toml` [versions] (alphabetical); library entries `password4j = { ... }` + `samstevens-totp = { ... }` in [libraries] near `auth0-*` group with rationale comments.
- [x] 1.3 Added `ktor-serverSessions = { module = "io.ktor:ktor-server-sessions-jvm", version.ref = "ktor" }` (BOM-resolved shape, same precedent as `ktor-serverPebble`).
- [x] 1.4 Wired all 3 catalog accessors (`libs.ktor.serverSessions`, `libs.password4j`, `libs.samstevens.totp`) into `backend/ktor/build.gradle.kts` dependencies.
- [~] 1.5 OPERATOR ACTION (not runnable from the dev session — needs `gcloud` auth + `nearyou-staging` access). Provisioning script authored (1.6); deploy workflow wired (`ADMIN_TOTP_SECRET_AES_KEY=staging-admin-totp-secret-aes-key:latest` in `deploy-staging.yml`). Operator runs `dev/scripts/admin-totp-key-bootstrap.sh` to create the slot + grant the runtime SA. Tracked in `docs/10-Setup-Checklist.md` § 4. Original task text: Provision GCP Secret Manager slot `staging-admin-totp-secret-aes-key` for staging: `gcloud secrets create staging-admin-totp-secret-aes-key --project=nearyou-staging --replication-policy=automatic` + add a 256-bit AES key (`openssl rand -base64 32 | gcloud secrets versions add staging-admin-totp-secret-aes-key --data-file=-`) + grant `roles/secretmanager.secretAccessor` to the staging Cloud Run runtime SA (per the `provision-admin-app-staging.sh` precedent).
- [x] 1.6 Shipped `dev/scripts/admin-totp-key-bootstrap.sh` — idempotent (create-if-absent, add-key-only-if-no-enabled-version so re-runs don't rotate + orphan ciphertexts, grant secretAccessor). `PROJECT_OVERRIDE`/`SLOT_OVERRIDE`/`RUNTIME_SA_OVERRIDE` env vars for prod. Never echoes key material (openssl piped straight to gcloud). Original task: Document the production slot provisioning procedure in `dev/scripts/admin-totp-key-bootstrap.sh` (idempotent script for both staging + prod with `PROJECT_OVERRIDE` env var per the `provision-admin-app-staging.sh` precedent). DO NOT execute against production in this change — production provisioning is deferred to the production-bootstrap milestone.
- [x] 1.7 Shipped the bootstrap CLI as `backend/ktor/src/main/kotlin/id/nearyou/app/dev/AdminBootstrapMain.kt` + Gradle JavaExec task `adminBootstrap` + wrapper `dev/scripts/admin-bootstrap/admin-bootstrap.sh` + `README.md` (matches the `mintDevJwt` precedent — a standalone .kt outside the source tree couldn't use AesGcmCipher/PasswordHasher). Accepts `--email/--display-name/--role`, reads AES key from `ADMIN_TOTP_AES_KEY_BASE64`, hashes password (Argon2id), generates+base32-encodes+AES-GCM-encrypts (admin-UUID AAD) the TOTP secret, prints the SQL INSERT + DO-NOT-SAVE warning. README documents the shell-history mitigation (leading space + HISTCONTROL=ignorespace). Original task: Document the manual first-admin bootstrap procedure (per `design.md` D11) in `dev/scripts/admin-bootstrap/README.md` and ship the script at `dev/scripts/admin-bootstrap/Main.kt` (Kotlin one-shot CLI). Script accepts `--email`, `--display-name`, `--role`, reads the AES key from `ADMIN_TOTP_AES_KEY_BASE64` env var (set via `gcloud secrets versions access ...`), generates Argon2id hash for an interactively-entered password, generates a SecureRandom 160-bit TOTP secret + base32-encodes it for authenticator-app provisioning + AES-256-GCM-encrypts it with the admin UUID bound as AAD (per design.md D1 + spec Req "TOTP secret decryption uses AES-256-GCM…") for `totp_secret_encrypted`, prints the SQL INSERT statement to stdout with a "DO NOT save this output to a file" warning. Script does NOT log plaintext password / TOTP secret / AES key. The README SHALL document the **shell-history mitigation:** preface the `gcloud secrets versions access` invocation with a leading space (and recommend `export HISTCONTROL=ignorespace` or equivalent for the operator's shell) so the AES key plaintext does not land in `~/.zsh_history` / `~/.bash_history`.
- [~] 1.8 OPERATOR ACTION (gated on 1.5 + a staging deploy). Run `dev/scripts/admin-bootstrap/admin-bootstrap.sh` against the staging DB; store the staging-test email/password/base32-TOTP in the operator password manager (NOT this PR). Tracked in `docs/10-Setup-Checklist.md` § 4.
- [x] 1.9 Shipped `backend/ktor/src/test/kotlin/id/nearyou/app/dev/AdminBootstrapMainTest.kt` (runs in PR CI — no DB/subprocess; drives the pure `buildAdminBootstrap`/`operatorOutput` core). Asserts: SQL INSERT + DO-NOT-SAVE warning present; plaintext password absent (Argon2id hash present); AES key absent; base32 secret appears exactly once; raw secret bytes never as hex/base64; encrypted secret round-trips via AesGcmCipher.decrypt; parseArgs role validation. Original task: Test the bootstrap script's secret-leakage invariants: Kotest spec at `dev/scripts/admin-bootstrap/Test/MainTest.kt` (or whichever Kotlin test source path matches the script's build setup) drives the script with synthetic input (a fixture password + fixture TOTP secret + fixture AES key), captures stdout + stderr + any temporary files created, asserts: (a) stdout contains the SQL INSERT line + the `DO NOT save this output to a file` warning substring; (b) stdout + stderr do NOT contain the plaintext password substring; (c) stdout + stderr do NOT contain the plaintext TOTP secret bytes (raw or base32-encoded — the base32 encoding IS expected to appear in the manual authenticator-app provisioning step's output, but the test asserts that any base32 string emitted is the ONE intended for the operator's authenticator app, not duplicated elsewhere); (d) stdout + stderr do NOT contain the AES key substring; (e) no temporary file under `${TMPDIR}` or the script's CWD contains any of (b)/(c)/(d) substrings.

## 2. AES-256-GCM helper

- [x] 2.1 Created `AesGcmCipher.kt` with `encrypt(plaintext, key, aad)` / `decrypt(ciphertext, key, aad)` / `adminUuidAsBytes(UUID)` per design.md D1; format `nonce (12) || ciphertext || auth_tag (16)`.
- [x] 2.2 Wired JCA `AES/GCM/NoPadding` with `GCMParameterSpec(128, nonce)` + `updateAAD(adminUuidBytes)` on both encrypt and decrypt.
- [x] 2.3 Unit tests at `AesGcmCipherTest.kt` covering: round-trip with AAD match, AAD swap → AEADBadTagException, tampered tag/ciphertext/nonce, wrong-length key, ciphertext-format length, UUID-to-bytes determinism.

## 3. Argon2id verifier

- [x] 3.1 Created `PasswordHasher.kt` exposing `verify(plaintext, hash)` / `hash(plaintext)` / `verifyAgainstSentinel(plaintext)` + tuned constants `MEMORY_KIB=65536`, `ITERATIONS=3`, `PARALLELISM=1`, `HASH_OUTPUT_BYTES=32`.
- [x] 3.2 Authored `PasswordHasherBenchmarkTest.kt` tagged `@Tag("benchmark")`; asserts mean wall time within [300, 2000] ms across n=10 verifies (loose bounds to tolerate JIT/CI variance per task 11.5).
- [ ] 3.3 ⚠ DEFERRED — actual benchmark tuning requires local execution. Starting params are Password4j's "minimum production" preset (64 MiB / 3 iter / 1 parallelism); benchmark spec will validate when run locally. Adjust the `@benchmark` comment + constants after first apply-phase local benchmark run.
- [x] 3.4 Sentinel hash implemented via lazy-init pattern (D9 + D15) — `sentinelHash` is computed once at module bootstrap from a fixed plaintext + the same `argon2Function` used for production. Sentinel-params == production-params by construction (single source of truth); no separate regeneration step needed when params re-tune.
- [x] 3.5 Unit tests at `PasswordHasherTest.kt` covering: OWASP-floor assertions, hash round-trip, random salt produces different hashes, sentinel hash parses + is deterministic + embeds same params as production hashes (D15 invariant).

## 4. TOTP verifier

- [x] 4.1 Created `TotpVerifier.kt` wrapping samstevens java-totp's `DefaultCodeVerifier` with SHA-1 / 30s / 6 digits / ±1 step skew per design.md D3. Internally base32-encodes raw secret bytes via commons-codec (samstevens transitive dep).
- [x] 4.2 Sentinel TOTP secret implemented as a fixed 20-byte source constant (`SENTINEL_SECRET_BYTES`); `verifyAgainstSentinel(code)` used by the login handler on the wrong-password / inactive-admin / missing-secret paths for timing equalization.
- [x] 4.3 Unit tests at `TotpVerifierTest.kt` covering: parameter-constants assertions, full skew matrix (T / ±30s / ±60s), malformed codes (non-digit, wrong length, leading/trailing whitespace, empty), sentinel verify never spuriously succeeds.

## 5. Session middleware

- [x] 5.1 Created `AdminPrincipal.kt` — `data class` with `adminId`, `sessionId`, `csrfTokenHash`, `role`, `expiresAt`, `lastActiveAt`. Implements `io.ktor.server.auth.Principal`.
- [x] 5.2 Created `SessionRepository.kt` exposing `insert(...) → UUID`, `findBySessionHash(...) → SessionRow?`, `refreshLastActive(...)`, `revoke(...)`. All queries via PreparedStatement. Active-predicate (revoked/expires/idle) applied at the application layer (in `SessionRow.isActive(...)`) per design.md D8 — keeps the test ergonomics clean (no Postgres-NOW vs JVM-clock drift). Also `AdminUserRepository.kt` with `findActiveByEmail(...)` + `findById(...)`.
- [x] 5.3 Created `AdminAuthPlugin.kt` — custom Ktor `AuthenticationProvider`. Reads `__Host-admin_session` cookie via `call.request.cookies`; SHA-256-hex hash via `HashUtil`; calls `SessionRepository.findBySessionHash(...)`; validates active predicate + admin still active; refreshes `last_active_at`; populates `AdminPrincipal`. Fail-CLOSED on DB exception (try/catch around the lookup → 302 + WARN log) per spec scenario "Session middleware fails CLOSED on DB exception" + design.md D16. DSL helper `adminAuth(name)` for `install(Authentication) { adminAuth("admin") { ... } }`.
- [x] 5.4 DSL wired; actual install will happen in S8 (`Application.admin()` cleanup).
- [x] 5.x-bonus Created `HashUtil.kt` — SHA-256/hex helpers using JDK 17+ `HexFormat`, `constantTimeEqualHex(a, b)` via `MessageDigest.isEqual` per spec Req "All admin auth comparisons are constant-time".

## 5.5. AdminAuditLogger (precedes its consumers in Sections 6 + 7)

- [x] 5.5.1 Created `AdminAuditLogger.kt` with all 5 methods: `logSuccess` / `logFailure` / `logEmailNotFoundAttempt` / `logLogout` / `logCsrfViolation`. Failure-reason enums `LoginFailureReason` (4 values, omits `email_not_found` per D14) + `CsrfViolationReason` (3 values). Email-not-found path emits structured INFO log only — no DB write. `sha256Base64Url(email)` helper for the `email_hash` field.
- [x] 5.5.2 Type-safe invariants: the method signatures DO NOT accept `password` / `totp` / `submittedToken` parameters — only `reason` from the enumerated set. `after_state` JSON is built from non-secret session metadata only (session_id + expires_at + last_active_at).
- [ ] 5.5.3 DI wiring deferred to S8 (`Application.admin()` cleanup) — `AdminAuditLogger` will be wired into Koin or constructed directly in the admin module's bootstrap.

## 6. CSRF middleware

- [x] 6.1 Created `AdminCsrfPlugin.kt` — `AdminCsrfGate.validateCsrf(call, auditLogger)` returns Boolean (true=proceed, false=rejected with 403 + audit). Skips idempotent methods (GET/HEAD/OPTIONS). Reads `X-CSRF-Token` header first; falls back to `_csrf` form field ONLY when content-type is `application/x-www-form-urlencoded` or `multipart/form-data` (avoids the body-consuming gotcha for JSON requests). SHA-256-hex of submitted token + constant-time hex compare via `HashUtil.constantTimeEqualHex` against the principal's `csrfTokenHash`. On mismatch / missing: `AdminAuditLogger.logCsrfViolation(...)` with appropriate reason, `respond(403)`.
- [x] 6.2 Wiring happens in S8 (interceptor inside the `authenticate("admin") { ... }` block calls `validateCsrf` and `finish()`s the pipeline on rejection).

## 7. Login + logout routes

- [ ] 7.1 Create `backend/ktor/src/main/resources/templates/admin/login.peb` — Pebble template extending the base layout. Login form with `<input type="email" name="email" required>`, `<input type="password" name="password" required>`, `<input type="text" name="totp" pattern="\d{6}" maxlength="6" required>` fields. `<form action="/admin/login" method="POST">`. Conditional `<p class="error">{{ errorMessage }}</p>` block when `errorMessage` is in the rendering context. Page MUST NOT include the CSRF meta tag (no session yet) — the base layout should already make that conditional per task 8.2.
- [ ] 7.2 Modify `backend/ktor/src/main/resources/templates/admin/` base layout (whichever filename Admin #2 chose) to conditionally render `<meta name="csrf-token" content="{{ csrfToken }}">` in the `<head>` ONLY when `csrfToken` is defined in the rendering context. Add the inline `<script>` block from design.md D6 (HTMX configRequest listener) also conditional on `csrfToken` being defined.
- [ ] 7.3 Create `backend/ktor/src/main/kotlin/id/nearyou/app/admin/auth/AdminUserRepository.kt` exposing `fun findActiveByEmail(email: String): AdminUserRow?` — SELECT `id, email, password_hash, totp_secret_encrypted, role, is_active` FROM `admin_users` WHERE `email = $1 AND is_active = TRUE`. Parameterized query.
- [ ] 7.4 Create `backend/ktor/src/main/kotlin/id/nearyou/app/admin/auth/LoginRoute.kt` exposing `fun Routing.adminLogin(...)` extension that wires:
   - `get("/admin/login")` → if authenticated (call.principal != null) → 302 to `/admin/`. Else render `login.peb` with no `errorMessage`.
   - `post("/admin/login")` → call `processLogin(...)`. Body of `processLogin`:
     1. Parse form params: `email`, `password`, `totp`.
     2. SELECT admin_users row via AdminUserRepository.findActiveByEmail(email).
     3. If row is NULL: run sentinel PasswordHasher.verify(password, SENTINEL_HASH) + sentinel TotpVerifier.verify(SENTINEL_SECRET, totp); call `AdminAuditLogger.logEmailNotFoundAttempt(emailHash = SHA-256-base64url(email), ip, userAgent)` per design.md D14 (structured INFO log at application logger, NOT `admin_actions_log` — the V16 schema's `admin_id NOT NULL` invariant forbids unowned audit rows); render `login.peb` with the generic error message; status 200.
     4. If row's `is_active = FALSE`: same sentinel verifies; log audit row with `admin_id = row.id`, `reason = 'inactive_admin'`; same generic-error render.
     5. If row's `totp_secret_encrypted IS NULL`: same sentinel verifies; log audit row with `reason = 'totp_secret_missing'`; same generic-error render.
     6. Run `PasswordHasher.verify(password, row.password_hash)`. If FALSE: run sentinel TotpVerifier.verify; log audit row with `reason = 'password_mismatch'`; same generic-error render.
     7. AesGcmCipher.decrypt(row.totp_secret_encrypted, aesKey). Run TotpVerifier.verify(decryptedSecret, totp). If FALSE: log audit row with `reason = 'totp_mismatch'`; same generic-error render.
     8. Success path: SecureRandom 32-byte session token + 32-byte CSRF token (base64url-encode both); SHA-256 each. SessionRepository.insert(...) returning sessionId. Set `__Host-admin_session` cookie with the plaintext session token value + the attributes per spec admin-login Req 5. AdminAuditLogger.logSuccess(adminId = row.id, sessionId, ip, userAgent). Respond 200 with `HX-Redirect: /admin/` header. (Alternative: a non-HTMX-aware browser will get 200 with empty body — that's fine for the HTMX-driven admin UI.)
- [ ] 7.5 Create `backend/ktor/src/main/kotlin/id/nearyou/app/admin/auth/LogoutRoute.kt` exposing `fun Routing.adminLogout(...)` wiring `post("/admin/logout")` (inside the `authenticate("admin")` block + after the CSRF plugin): SessionRepository.revoke(sessionId from call.principal); set `__Host-admin_session=` (empty value) with Max-Age=0 + same other attributes; AdminAuditLogger.logLogout(adminId); respondRedirect("/admin/login", permanent=false).

## 8. `Application.admin()` cleanup

- [ ] 8.1 In `backend/ktor/src/main/kotlin/id/nearyou/app/admin/AdminModule.kt`: remove the `if (ktorEnv == "production") return` block (lines ~63-71 in current shape) + its bootstrap WARN log line. Remove the `ktorEnv` variable read if it has no other use after the guard removal.
- [ ] 8.2 Remove the bootstrap "admin module mounted: unauthenticated scaffold" WARN log (lines ~73-82 in current shape).
- [ ] 8.3 Remove the per-request WARN interceptor inside `route("/admin")` (lines ~95-122 in current shape — the one that emits `event=admin_request_unauthenticated_scaffold` per `/admin/*` hit).
- [ ] 8.4 Update the head-of-file comment block (lines 14-39): replace the "unauthenticated scaffold + admin-login-argon2-totp will close it" framing with a brief description of the auth gate ("Argon2id password + TOTP + opaque-token session + CSRF" per design.md D10), reference Admin #3 (`admin-login-argon2-totp`) as the lifecycle origin, reference the `admin-login` capability spec for the canonical behavior.
- [ ] 8.5 Verify the `install(Authentication) { admin { ... } }` block from task 5.4 is in place inside `Application.admin()` (it was wired during session-middleware setup; this step is the cleanup-phase verification, not a re-install).
- [ ] 8.6 Restructure the `routing { route("/admin") { ... } }` block:
   - Move the `staticResources("/static", "admin/static")` mount to the outer scope (no auth, no CSRF — public static assets).
   - Add `Routing.adminLogin(...)` extension to wire the login GET + POST routes (unauthenticated; CSRF-exempt for POST).
   - Wrap the authenticated subtree in `authenticate("admin") { install(AdminCsrfPlugin); adminIndex(); adminLogout(...); /* future admin routes */ }`.
- [ ] 8.7 Verify Detekt + ktlint stay green after the restructure: `./gradlew ktlintCheck detekt`.

## 9. `admin-panel-scaffold` spec amendments (post-archive sync)

- [ ] 9.1 At archive time, the `openspec archive` tool MERGES the `admin-panel-scaffold/spec.md` delta from `openspec/changes/admin-login-argon2-totp/specs/admin-panel-scaffold/spec.md` INTO `openspec/specs/admin-panel-scaffold/spec.md` (renaming the canonical to reflect the MODIFIED + REMOVED operations). Verify via `openspec validate --specs admin-panel-scaffold --strict` after the merge.
- [ ] 9.2 Verify the canonical spec post-merge does NOT contain the removed requirements (Req 4 "Admin subtree does NOT require authentication in this change", Req 5 "Production mount guard via KTOR_ENV check") nor the in-source-comment references to them in [`AdminModule.kt`](../../../backend/ktor/src/main/kotlin/id/nearyou/app/admin/AdminModule.kt) post-task 8.4.

## 10. Audit log call-site verification (AdminAuditLogger implemented in Section 5.5)

- [ ] 10.1 Verify the four call sites are wired: LoginRoute (success path → `logSuccess`; password_mismatch / totp_mismatch / inactive_admin / totp_secret_missing failure paths → `logFailure`; email_not_found path → `logEmailNotFoundAttempt`), LogoutRoute (`logLogout`), AdminCsrfPlugin (`logCsrfViolation`).
- [ ] 10.2 Cross-check spec scenarios in `specs/admin-login/spec.md` Requirements "Login POST writes audit row when an admin actor is identified" + "CSRF mismatch writes admin_csrf_violation audit row" against the implemented call sites — each scenario MUST have a corresponding test in Section 11.

## 11. Tests

> Citations below use spec requirement HEADER TEXT (not ordinals) — durable against future requirement insertions per N1 (OpenSpec lens) of round-1 review.

- [ ] 11.1 **Login form GET test** (`admin-login` Req "Login form GET renders the unauthenticated login page"). `testApplication { application { admin() } }` → `client.get("/admin/login")` SHALL return 200; body contains the three `<input>` fields (email, password, totp); body contains `<form action="/admin/login" method="POST">`; body does NOT contain `<meta name="csrf-token"`.
- [ ] 11.2 **Authenticated GET /admin/login redirect** (same requirement). Seed a valid session + cookie; `client.get("/admin/login")` → 302; `Location: /admin/`.
- [ ] 11.3 **Login POST happy path** (`admin-login` Req "Login POST verifies Argon2id password and TOTP and creates a session on success"). Seed an `admin_users` row via test fixture (Argon2id hash of `"correct-pw"`, AES-GCM-encrypted TOTP secret WITH the admin's UUID bound as AAD, `is_active = TRUE`); compute current TOTP code; `client.submitForm(...)`. Assertions: status 200; header `HX-Redirect = /admin/`; `Set-Cookie` header for `__Host-admin_session`; cookie value matches regex `^[A-Za-z0-9_-]{43}$`; new `admin_sessions` row exists with correct `admin_id`, `session_token_hash = SHA-256(cookieValue)`, non-null `csrf_token_hash`, `expires_at` ≈ NOW() + 8h, `revoked_at IS NULL`.
- [ ] 11.4 **Login POST failure scenarios** (same Req + `admin-login` Req "Login POST returns no-enumeration response on every failure path"). Five sub-tests, each asserting identical response shape: (a) wrong password → 200 + no cookie + no session row + body contains "Email, password, or code is incorrect."; (b) wrong TOTP → same; (c) is_active = FALSE → same; (d) totp_secret_encrypted = NULL → same; (e) email not found → same. Cross-test byte-identity: response bodies of all five sub-tests SHALL be byte-equal.
- [ ] 11.5 **Login POST timing equalization** (`admin-login` Req "Login POST returns no-enumeration response on every failure path", scenario "Timing equalization runs Argon2id verify on email-not-found"). Statistical test: run 30 "email not found" attempts + 30 "wrong password" attempts; assert MEDIAN wall time difference ≤ 200ms (raised from the round-1 100ms+10-sample-mean draft to mitigate CI flakiness per B6 of the test-coverage lens; median is robust to JIT-warmup outliers). **Tag the test `@Tag("benchmark")`** matching task 3.2's precedent so it is excluded from the PR-time CI lane (CI runs everything except `@Tag("benchmark")` per project convention); the benchmark lane runs locally during apply-phase tuning + manually pre-archive.
- [ ] 11.6 **Login POST audit row matrix** (`admin-login` Req "Login POST writes audit row when an admin actor is identified" — all scenarios). For each of: success / password_mismatch / totp_mismatch / inactive_admin / totp_secret_missing: SELECT * FROM admin_actions_log ORDER BY created_at DESC LIMIT 1 after the POST; assert `action_type`, `admin_id`, `reason`, `ip`, `user_agent` per the spec scenarios. Cross-assertions: (a) no `admin_actions_log` column value contains the substring of the submitted password or TOTP (negative substring scan including SHA-256/MD5/Argon2id hashes of the submitted plaintexts); (b) `after_state` JSON does NOT contain the cookie value, the SHA-256 of the cookie value, or the CSRF token value; (c) for the success path, `user_agent` is recorded correctly OR is NULL when the request omitted `User-Agent`; (d) for any request with `CF-Connecting-IP: 1.2.3.4`, the audit row's `ip` is `1.2.3.4`.
- [ ] 11.6.1 **Email-not-found does NOT write to admin_actions_log** (`admin-login` Req "Login POST writes audit row when an admin actor is identified", scenario "Email-not-found does NOT write to admin_actions_log"). POST `/admin/login` with a phantom email + assert no new `admin_actions_log` row exists after the POST.
- [ ] 11.6.2 **Email-not-found writes structured INFO log** (same Req, scenario "Email-not-found writes a structured INFO log line at the application logger"). Use Logback `ListAppender` (or equivalent) to capture logs during a phantom-email POST; assert an INFO-level line contains `event=admin_login_attempt_email_not_found` + `email_hash=<SHA-256-base64url-of-phantom-email>`; assert NEITHER the plaintext email NOR the submitted password / TOTP appears in any captured log line.
- [ ] 11.7 **Cookie format** (`admin-login` Req "__Host-admin_session cookie format meets security invariants"). Parse the `Set-Cookie` header from a successful login: assert `Secure`, `HttpOnly`, `SameSite=Strict`, `Path=/` attributes present; assert no `Domain=` attribute; assert `Max-Age` within ±60s of 28800.
- [ ] 11.8 **Session validation matrix** (`admin-login` Req "Session middleware validates the cookie on every authenticated request"). Sub-tests against `client.get("/admin/")`: (a) no cookie → 302 to `/admin/login`; (b) valid session → 200 + `admin_sessions.last_active_at` refreshed; (c) expired session (`expires_at < NOW()` via direct DB update) → 302; (d) idle session (`last_active_at` 31min ago via direct DB update) → 302; (e) revoked session (`revoked_at = NOW()`) → 302; (f) **forged cookie** (well-formed 43-char base64url whose SHA-256 does NOT match any `admin_sessions` row) → 302 + no session row update; (g) **idle-on-threshold** (`last_active_at` at exactly `NOW() - INTERVAL '30 minutes 1 second'`) → 302 + `last_active_at` NOT refreshed (the dead session must not be revived).
- [ ] 11.8.1 **Session middleware fails CLOSED on DB exception** (`admin-login` Req "Session middleware validates the cookie on every authenticated request", scenario "Session middleware fails CLOSED on DB exception"). Use a connection-error-injecting test harness (e.g., wrap the `SessionRepository` with a throwing decorator); `client.get("/admin/")` with valid cookie → 302 (or 500); the principal is NOT populated; a WARN-level log line is captured documenting the DB failure.
- [ ] 11.9 **CSRF validation matrix** (`admin-login` Req "CSRF middleware validates X-CSRF-Token on state-changing requests" + Req "CSRF mismatch writes admin_csrf_violation audit row"). Sub-tests against `client.post("/admin/logout")` with valid session cookie: (a) no CSRF header/form → 403 + `admin_csrf_violation` row with reason `missing_token`; (b) wrong `X-CSRF-Token` header → 403 + `header_mismatch`; (c) correct `X-CSRF-Token` header → 302 to `/admin/login`; (d) correct `_csrf` form field (no header) → 302; (e) header takes precedence over form field (correct header + wrong form → 302); (f) submitted CSRF token value never appears in the audit row's columns.
- [ ] 11.9.1 **Concurrent CSRF token reuse across multiple state-changing requests** (`admin-login` Req "CSRF middleware validates X-CSRF-Token on state-changing requests", scenario "CSRF token does NOT rotate per request"). Wire a synthetic test-only POST route (e.g., `/admin/__test__/state-change`) that does not terminate the session; issue TWO sequential POSTs with the SAME `X-CSRF-Token`; assert both pass + `admin_sessions.csrf_token_hash` does NOT change between requests + no `admin_csrf_violation` audit row.
- [ ] 11.10 **CSRF exemption for /admin/login POST** (`admin-login` Req "CSRF middleware validates X-CSRF-Token on state-changing requests", scenario "Login POST is exempt from CSRF middleware"). POST `/admin/login` with no `X-CSRF-Token` header → not rejected by CSRF middleware (reaches login handler; produces the normal credentials-based response).
- [ ] 11.11 **GET is not gated by CSRF** (same Req, scenario "GET requests are not gated by CSRF middleware"). GET `/admin/` with valid session + no `X-CSRF-Token` header → 200.
- [ ] 11.12 **Logout** (`admin-login` Req "Logout POST revokes session, clears cookie, and writes audit row"). Valid session + valid CSRF → POST `/admin/logout` → 302 to `/admin/login` + `Set-Cookie: __Host-admin_session=` (empty) with `Max-Age=0`; `admin_sessions.revoked_at` ≈ NOW(); `admin_actions_log` row with `action_type = 'admin_logout'`.
- [ ] 11.13 **Logout idempotency** (same Req, scenario "Logout is idempotent against already-revoked sessions"). Pre-revoke a session in DB; POST `/admin/logout` with that session's cookie → 302 to `/admin/login` from the session middleware (logout handler not reached; no second audit row).
- [ ] 11.14 **AES-256-GCM helper** (`admin-login` Req "TOTP secret decryption uses AES-256-GCM with key from GCP Secret Manager and admin-UUID AAD binding"). (a) AAD-bound round-trip on fixed secret + key + admin UUID returns original; (b) tampered tag → AEADBadTagException; (c) tampered ciphertext → AEADBadTagException; (d) AAD-bound decrypt with WRONG admin UUID → AEADBadTagException; (e) wrong-length key (16-byte AES-128 instead of 32-byte AES-256) → InvalidKeyException; (f) ciphertext length ≥ 28 + plaintext length.
- [ ] 11.15 **Authenticated layout CSRF meta + JS hook** (`admin-login` Req "Authenticated layout includes CSRF meta tag and HTMX configRequest hook" + `admin-panel-scaffold` MODIFIED Req "Shared base layout template exists and is extended by admin pages"). Authenticated `client.get("/admin/")` body contains `<meta name="csrf-token" content="...">`; the `content` attribute value's SHA-256 equals the session's `csrf_token_hash`; body contains an `htmx:configRequest` listener with literal substring `evt.detail.headers['X-CSRF-Token']` OR `evt.detail.headers["X-CSRF-Token"]`. Unauthenticated `client.get("/admin/login")` body contains NEITHER.
- [ ] 11.16 **Constant-time compare audit** (`admin-login` Req "All admin auth comparisons are constant-time"). Source-code scan: grep `backend/ktor/src/main/kotlin/id/nearyou/app/admin/auth/` for: (a) `Arrays.equals(` (NOT constant-time); (b) `.equals(` on locals named `*token*` / `*hash*` / `*csrf*` (case-insensitive); (c) `==` operator on locals/parameters whose declared type is `ByteArray` / `String?` AND whose name matches `*token*` / `*hash*` / `*csrf*` (operator-form equality). Assert all findings are either in test fixtures (`/test/`) or the canonical `MessageDigest.isEqual` pattern.
- [ ] 11.17 **Argon2id benchmark + parameter floor + sentinel-params discipline** (`admin-login` Req "Argon2id verification uses tuned parameters with documented benchmark" + design.md D15). Mean verify time within [300, 2000] ms; constants meet OWASP floor (memory ≥ 15 MiB, iter ≥ 2, parallelism = 1); **sentinel hash's embedded params SHALL match the production hash's embedded params** (parse both via Password4j; assert memory/iterations/parallelism equality). Tag this test `@Tag("benchmark")` per task 3.2's precedent.
- [ ] 11.18 **TOTP verifier RFC 6238** (`admin-login` Req "TOTP verification uses RFC 6238 with SHA-1 / 30s / 6 digits / ±1 step skew"). Parameter constants assertion + the four skew scenarios (current step / -30s / +30s / -60s) + a fifth: malformed code (non-digit characters, wrong length) returns FALSE without throwing.
- [ ] 11.19 **Login input handling** (`admin-login` Req "Login input handling tolerates Unicode and rejects malformed shapes safely"). Sub-tests: (a) Unicode email + Unicode password round-trip succeeds; (b) TOTP code with leading/trailing whitespace → totp_mismatch failure (not silently stripped); (c) 10MB email field → 413 or 400 at parse layer + no admin_users SELECT + no audit row; (d) Pebble template escapes `errorMessage = '<script>alert(1)</script>'` → rendered HTML contains entity-escaped form, NOT raw `<script>`.
- [ ] 11.20 **admin-panel-scaffold MODIFIED Req "Admin routes mount under /admin/* namespace" — auth redirect.** Unauthenticated `GET /admin/` → 302 to `/admin/login`; authenticated → 200.
- [ ] 11.21 **admin-panel-scaffold MODIFIED Req "Admin routes mount under /admin/* namespace" — 404 on unmapped routes.** `GET /admin/nonexistent-page` (with and without session) → 404.
- [ ] 11.22 **admin-panel-scaffold MODIFIED Req "Admin routes mount under /admin/* namespace" — 405 on POST to bare index.** `POST /admin/` → 405 Method Not Allowed.
- [ ] 11.23 **admin-panel-scaffold MODIFIED Req "Shared base layout template exists and is extended by admin pages" — login page extends base layout without CSRF block.** Unauthenticated `GET /admin/login` body contains header / nav-stub / footer markup; does NOT contain `<meta name="csrf-token"` or `htmx:configRequest`.
- [ ] 11.24 **admin-panel-scaffold REMOVED Req "Admin subtree does NOT require authentication in this change" — no per-request WARN log.** Test using log-capture utility: `GET /admin/` (authenticated) does NOT emit any WARN log line containing the substring `unauthenticated scaffold`.
- [ ] 11.25 **admin-panel-scaffold REMOVED Req "Production mount guard via KTOR_ENV check" — production mount no longer gated.** Test with `MapApplicationConfig("ktor.environment" to "production")`: `GET /admin/login` SHALL return 200 (route mounted in production, just behind the auth gate); the previous expectation of 404 in production no longer holds.

## 12. Documentation

- [ ] 12.1 Update the head-of-file comment in `AdminModule.kt` (covered by task 8.4 — listed here for the documentation-trail).
- [x] 12.2 Updated `docs/10-Setup-Checklist.md` § 4 — added the `staging-admin-totp-secret-aes-key` slot (with deploy-wiring note + key-rotation warning), the staging-test admin row item, and the production slot as a deferred item gated on prod bootstrap.
- [x] 12.3 Added Version Pinning Decisions Log rows to `docs/09-Versions.md` for `com.password4j:password4j 1.8.4` + `dev.samstevens.totp:totp 1.7.1` (pinned-on 2026-05-29, rationale + re-check outcome + chosen-over-alternative + next review 2026-Q3).
- [x] 12.4 Reconciliation pass: implementation surfaced ONE spec-internal tension (is_active-filter vs inactive_admin audit reason) — resolved in-code via the two-step lookup (documented in the S11 commit + AdminUserRepository KDoc), NOT a doc divergence. Cookie format / session timeout / audit action types / library substrate all matched docs/04/05/07 + admin-schema spec as implemented. No silent divergence.
- [x] 12.5 No stale-doc divergence surfaced (the tension was spec-internal, fixed in-code) — no FOLLOW_UPS entry needed.

## 13. Pre-archive smoke

- [x] 13.1 Shipped `dev/scripts/smoke-admin-login-argon2-totp.sh` — covers (a) GET /admin/login 200 + form, (b) POST valid creds + oathtool TOTP → 200 + HX-Redirect + Set-Cookie, (b.1) wire-level cookie attrs (Secure/HttpOnly/SameSite=Strict/Path=/, no Domain), (c) extract CSRF from /admin/ → POST /admin/logout → 302, (d) revoked-cookie GET /admin/ → 302, (e) no-enumeration body byte-equality. Original task: Author `dev/scripts/smoke-admin-login-argon2-totp.sh` — bash script that:
   - **(a)** hits `GET https://api-staging.nearyou.id/admin/login` with `curl -i` and asserts 200 + login form HTML in response;
   - **(b)** POSTs valid credentials + current TOTP code (sourced from a local `oathtool --totp -b <STAGING_TEST_TOTP_SECRET>` invocation) and asserts 200 + `HX-Redirect: /admin/` header + `Set-Cookie: __Host-admin_session` header;
   - **(b.1) wire-level cookie attribute check:** the captured `Set-Cookie` header line SHALL contain the substrings `Secure`, `HttpOnly`, `SameSite=Strict`, `Path=/`, AND SHALL NOT contain the substring `Domain=`. This catches cookie-attribute mutation by the Cloudflare → Google Frontend → Cloud Run proxy chain that in-process integration tests cannot exercise;
   - **(c)** extracts the session cookie + the CSRF token from the next `GET /admin/` response (HTML parse), then POSTs `/admin/logout` with cookie + CSRF header, asserts 302 to `/admin/login`;
   - **(d)** GETs `/admin/` with the now-revoked cookie, asserts 302 to `/admin/login`;
   - **(e) no-enumeration response body equality** — POSTs two failing login attempts (a wrong-password attempt against the staging test admin + a phantom-email attempt with `phantom-${RANDOM}@nearyou.id`), captures the response bodies, asserts they are byte-equal. This catches a future UX-improvement that drops the no-enumeration contract or returns distinct messages per failure mode.
- [x] 1.5 DONE (2026-05-29) — provisioned `staging-admin-totp-secret-aes-key` + `staging-admin-csrf-hmac-key` (both v1 + runtime-SA grant) via `dev/scripts/admin-totp-key-bootstrap.sh`.
- [x] 13.2 DONE (2026-05-29) — provisioned the staging-test admin row via `dev/scripts/admin-bootstrap/admin-bootstrap.sh` (Argon2id hash + AES-GCM-encrypted TOTP secret, `INSERT 0 1` via the admin_app connection). Creds stored at `~/.nearyou-staging-test-admin.env` (chmod 600, outside the repo). NOTE: the direct Supabase host turned out reachable from this machine via IPv6 (the FOLLOW_UPS "IPv4-only workstation" caveat didn't apply here); `admin_app` has INSERT on `admin_users`.
- [x] 13.3 DONE — deployed the branch to staging twice via `gh workflow run deploy-staging.yml --ref admin-login-argon2-totp`. Run 1 (8ed5c47): success, but the smoke caught an Argon2 OOM (see 13.5). Run 2 (8c422e1): success with the 19 MiB Argon2 fix.
- [x] 13.4 DONE — full smoke green against the live staging revision through the real Cloudflare→GFE→Cloud Run chain: ✓ GET /admin/login 200 + form + no CSRF meta; ✓ login success → 200 + HX-Redirect + Set-Cookie; ✓ wire-level cookie attrs (Secure/HttpOnly/SameSite=Strict/Path=/, no Domain); ✓ authed GET /admin/ → CSRF extract → logout 302; ✓ revoked-cookie GET → 302; ✓ no-enumeration body byte-equality. Exercises the full success path (real password verify + AES TOTP decrypt + HMAC CSRF derivation + session lifecycle).
- [x] 13.5 DONE — the smoke CAUGHT a real deploy-runtime bug: HTTP 500 "Java heap space" on the first login POST. Root cause: Argon2 128 MiB on-heap matrix vs the ~128 MiB Cloud Run JVM heap (`--memory=512Mi`). Fixed by sizing Argon2 to 19 MiB (commit 8c422e1) + re-deploy + re-smoke green. Exactly the one-way-door bug the pre-archive smoke exists to catch.
- [x] 13.6 DONE — deploy + full-smoke outcomes recorded in the PR body. Pre-archive smoke gate SATISFIED (all 6 steps green on staging). Ready for `gh pr ready` + `/review` + `/opsx:archive`.
