## 1. Annotation scaffolding

- [x] 1.1 Locate the existing project annotation file. The canonical location for `@AllowDailyTtlOverride` / `@AllowRawRedisKey` / `@AllowForbiddenSpanAttribute` annotations should be either `core/domain/src/main/kotlin/id/nearyou/app/core/domain/ratelimit/Annotations.kt` (or the equivalent per-domain file) OR a centralised location in `:lint:detekt-rules`. Verify by running `grep -rln "AllowDailyTtlOverride\|AllowRawRedisKey" core/ lint/ infra/`. Pick the canonical location. — **Verified**: two storage locations exist; the more recent precedent is `backend/ktor/src/main/kotlin/id/nearyou/app/lint/` (one file per annotation, currently `AllowMissingBlockJoin.kt` + `AllowRawPostsRead.kt`). New annotation placed there.
- [x] 1.2 Add `@AllowContentWriteWithoutModeration` annotation. The annotation accepts a single `String` argument named `reason`. Allowed reasons are documented in KDoc as one of the enum-string values: `"tombstone"`, `"admin_redaction"`, `"seed"`. Empty-string reasons MUST fail the rule per the rule-side enforcement (Section 2). Annotation target: `@Target(AnnotationTarget.FUNCTION)`. Retention: `@Retention(AnnotationRetention.SOURCE)` (the rule reads it via PSI, not at runtime).

## 2. Rule scaffolding

- [x] 2.1 Create `lint/detekt-rules/src/main/kotlin/id/nearyou/lint/detekt/ContentWriteRequiresModerationRule.kt` extending Detekt's `Rule` base class. Mirror the structural shape of [`OtelForbiddenAttributeRule.kt`](../../../lint/detekt-rules/src/main/kotlin/id/nearyou/lint/detekt/OtelForbiddenAttributeRule.kt) — same package, same `Issue` declaration shape, same `visit*` override pattern.
- [x] 2.2 Implement the dual allowlist: short-circuit if (a) `containingKtFile.virtualFilePath?.replace('\\', '/')?.contains("/src/test/") == true` OR (b) the `KtFile`'s `packageFqName.asString() == "id.nearyou.test.fixtures"` OR `startsWith("id.nearyou.test.fixtures.")`. Mirror the canonical 3-rule precedent — `RedisHashTagRule.kt:115-120`, `RateLimitTtlRule.kt:92-100`, `BlockExclusionJoinRule.kt:176-186`.
- [x] 2.3 Implement the SQL-string-literal candidate detection (Decision 2 arm a): override `visitStringTemplateExpression`; test the flat textual content against the regex `(?i)\\bINSERT\\s+INTO\\s+(posts|post_replies|chat_messages)\\b[^)]*\\bcontent\\b`. If matched, the candidate write call's PSI element is the `KtStringTemplateExpression` itself; proceed to the moderator-ancestry check (step 2.5).
- [x] 2.4 Implement the service-method candidate detection (Decision 2 arm b): override `visitCallExpression`; check that the callee's short-name is one of `create` / `post` / `sendMessage` AND the receiver-expression's text matches the canonical short-name set (`postRepository`/`PostRepository`, `replyService`/`ReplyService`, `chatRepository`/`ChatRepository`). Mirror the receiver-name heuristic from `BlockExclusionJoinRule`. Verify the canonical service-method names against actual production code at apply time — if a name differs (e.g., `insert` instead of `create`), update the rule's match set to match reality. — **Production-verified match-set adopted from spec delta**: `(posts, create)` / `(replies, insertInTx)` / `(repository, sendMessage)`. The proposal's draft set `create / post / sendMessage` was incorrect; the spec delta (lines 11–13) corrected it after production-source verification. `ReplyService.post(...)` is the route-layer wrapper, not the Repository write — the actual write is `replies.insertInTx(conn, ...)` at `ReplyService.kt:155`.
- [x] 2.5 Implement the moderator-ancestry PSI walk: from the candidate write-call PSI element, walk UP via `getParentOfType<KtNamedFunction>(true)` (or `KtFunctionLiteral` for lambda contexts). Then walk DOWN the function body via `collectDescendantsOfType<KtCallExpression>()`, filter to those whose callee short-name is `moderate` AND whose receiver-expression text matches `textModerator` / `TextModerator`, AND whose source-position offset PSI-precedes the candidate write call's offset. If any qualifying call exists, the rule passes; else proceed to the annotation-bypass check (step 2.6).
- [x] 2.6 Implement the annotation-bypass: walk UP from the candidate write-call to the enclosing `KtNamedFunction`; check `function.annotationEntries` for any entry whose `shortName?.asString() == "AllowContentWriteWithoutModeration"`. If found, extract the first value-argument's text via `entry.valueArguments.firstOrNull()?.getArgumentExpression()?.text`. Validate: (a) the text is a string literal (after `removeSurrounding("\"\"\"")` or `removeSurrounding("\"")`); (b) the literal is non-blank; (c) the literal is one of `tombstone` / `admin_redaction` / `seed`. If all 3 conditions pass, the rule passes; else fire with the appropriate error message (empty-reason vs non-enumerated-reason — the message names the allowed reasons).
- [x] 2.7 If both moderator-ancestry (step 2.5) AND annotation-bypass (step 2.6) fail, fire via `report(CodeSmell(issue, Entity.from(<candidate-write-PSI-element>), <recommended-fix message>))`. The error message MUST recommend the canonical fix: "Invoke `TextModerator.moderate(content)` BEFORE the content write per `openspec/specs/content-moderation-keyword-lists/spec.md` § \"Moderator runs after length guard, before INSERT\"; OR annotate the function with `@AllowContentWriteWithoutModeration(\"<reason>\")` for documented carve-outs (allowed reasons: `tombstone`, `admin_redaction`, `seed`)."
- [x] 2.8 Add KDoc covering: rule purpose, candidate-write surface (Decision 2), detection mechanism (Decision 1), allowlist mechanism (Decision 5), annotation-bypass (Decision 4), reference back to `openspec/specs/content-moderation-keyword-lists/spec.md` § "ContentWriteRequiresModerationRule fences content-write call sites".

## 3. Provider registration

- [x] 3.1 Register `ContentWriteRequiresModerationRule` in [`NearYouRuleSetProvider.kt`](../../../lint/detekt-rules/src/main/kotlin/id/nearyou/lint/detekt/NearYouRuleSetProvider.kt) — add it to the `RuleSet` returned by `instance(config)`. The current registration list contains 7 rules (verified at NearYouRuleSetProvider.kt:14-21). Append the new rule. NOTE per `design.md` § Decision 7: PR [#100](https://github.com/aditrioka/nearyou-id/pull/100) is also adding an 8th rule — whichever change merges to `main` second will need to rebase its addition to coexist as the 9th. Trivial 1-line conflict. — **Resolved**: PR #100 (`IpAxisMustUseTryAcquireByKeyRule`) merged first; rebased onto `origin/main` and appended `ContentWriteRequiresModerationRule(config)` as the **9th** rule.
- [x] 3.2 No edit to [`backend/ktor/config/detekt/detekt.yml`](../../../backend/ktor/config/detekt/detekt.yml) is required. Verified: the config currently enumerates only `RawFromPostsRule` + `BlockExclusionJoinRule` explicitly; the other 5 existing rules are active by default after `NearYouRuleSetProvider` registration. The new rule follows the same default-active pattern.

## 4. Lint test fixtures

- [x] 4.1 Create `lint/detekt-rules/src/test/kotlin/id/nearyou/lint/detekt/ContentWriteRequiresModerationLintTest.kt` extending the project's existing Detekt-rule test base (mirror `OtelForbiddenAttributeLintTest`).
- [x] 4.2 Implement positive-case fixtures (1, 2, 3 per spec scenario list): SQL-literal INSERTs into `posts` / `post_replies` / `chat_messages` content columns without preceding `moderate` call. Each → exactly one finding.
- [x] 4.3 Implement positive-case service-method fixtures (4, 5, 6): `postRepository.create(content, ...)`, `replyService.post(content, ...)`, `chatRepository.sendMessage(content, ...)` without preceding `moderate` call. Each → exactly one finding. — Adopted production-verified shapes from the spec delta: `posts.create(...)` / `replies.insertInTx(...)` / `repository.sendMessage(...)`.
- [x] 4.4 Implement negative-case fixture (7): write WITH preceding `textModerator.moderate(content)` call → zero findings.
- [x] 4.5 Implement annotation-bypass positive cases (8, 9, 10): each annotation reason (`tombstone` / `admin_redaction` / `seed`) on a function with no-moderate-write → zero findings.
- [x] 4.6 Implement annotation-bypass empty-reason fail (11): `@AllowContentWriteWithoutModeration("")` on a function with no-moderate-write → exactly one finding.
- [x] 4.7 Implement annotation-bypass non-enumerated-reason fail (12): `@AllowContentWriteWithoutModeration("custom_reason")` → exactly one finding AND the error message contains the allowed-reasons list.
- [x] 4.8 Implement allowlist fixtures (13, 14): test-source-path AND package-FQN-fallback → zero findings each.
- [x] 4.9 Implement non-content-table negative case (15): `INSERT INTO follows (...)` → zero findings.
- [x] 4.10 Implement content-table-without-content-column negative case (16): `INSERT INTO posts (id, view_count) VALUES (...)` → zero findings.
- [x] 4.11 Implement composition with `BlockExclusionJoinRule` (17): a fixture that triggers BOTH rules; assert both finding-IDs in the captured list, no cross-suppression.
- [x] 4.12 Implement provider registration block (18): co-located Kotest block `"rule registered in NearYouRuleSetProvider"` inside `ContentWriteRequiresModerationLintTest.kt` (mirror precedent at `OtelForbiddenAttributeLintTest.kt:807-812`). Pattern: `val provider = NearYouRuleSetProvider(); val ruleSet = provider.instance(io.gitlab.arturbosch.detekt.api.Config.empty); val rules = ruleSet.rules.map { it::class.simpleName }; rules.contains("ContentWriteRequiresModerationRule") shouldBe true`.

## 5. Project Detekt run validation

- [x] 5.1 Run `./gradlew :lint:detekt-rules:test` locally — assert all new tests pass. — **Verified**: 18 / 18 tests pass for `ContentWriteRequiresModerationLintTest`; full `:lint:detekt-rules:test` suite green.
- [x] 5.2 Run `./gradlew detekt` locally — assert the rule is active AND every existing content-write call site in `:backend:ktor` either (a) invokes `TextModerator.moderate(...)` first OR (b) is annotated with `@AllowContentWriteWithoutModeration("<reason>")`. The 3 canonical write paths (`PostCreationRoute` / `ReplyService` / `ChatRepository.sendMessage`) already invoke moderate per the existing 3 integration tests' shape — they should pass without changes. Any other content-write site found firing the rule MUST be fixed (preferred: add the moderate call) OR annotated (with documented reason) BEFORE proceeding. — **Verified**: `./gradlew detekt` reports `0 number of total code smells` across 75 Kotlin files in `:backend:ktor`; the canonical 3 paths (`CreatePostService.create` → `posts.create`, `ReplyService.post` → `replies.insertInTx`, `ChatService.send` → `repository.sendMessage`) all invoke `textModerator.moderate(content)` before the write call and pass the rule.
- [x] 5.3 Run the full pre-push verification per [`CLAUDE.md`](../../../CLAUDE.md) § "Pre-push verification": `./gradlew ktlintCheck detekt :backend:ktor:test :lint:detekt-rules:test` — all four targets MUST pass before pushing. — **Verified**: all four targets BUILD SUCCESSFUL.

## 6. Spec + docs sync

- [x] 6.1 Run `openspec validate content-write-moderation-detekt-rule --strict` — ensure validation passes. — **Verified**: `Change 'content-write-moderation-detekt-rule' is valid`.
- [ ] 6.2 At `/opsx:archive` time: delete the [`FOLLOW_UPS.md`](../../../FOLLOW_UPS.md) entry `content-write-moderation-detekt-rule` (lines ~594–615) — its scope is fully fulfilled by this change. If `FOLLOW_UPS.md` becomes empty after this deletion (unlikely given the other open entries), also delete the file itself per its intro-blurb convention. — **Deferred to `/opsx:archive`** (explicitly tied to the archive commit per the task description).
- [ ] 6.3 Verify by grep that no other `docs/**` or `openspec/specs/**` cross-reference points at the deleted FOLLOW_UPS entry: `grep -rn "content-write-moderation-detekt-rule" docs/ openspec/specs/` post-archive. — **Pre-archive check (informational)**: `grep -rn "content-write-moderation-detekt-rule" docs/ openspec/specs/` returns zero matches today; the only in-tree references are in `openspec/changes/content-write-moderation-detekt-rule/` (the change folder itself) and `FOLLOW_UPS.md` (the entry being deleted by 6.2). Re-verify at archive time.

## 7. Pre-archive smoke

- [x] 7.1 N/A — this is a docs + lint-rule change with no runtime impact (per the Pre-archive smoke convention in [`openspec/project.md`](../../../openspec/project.md) § Staging deploy timing: "For docs-only / refactor-only changes, skip step 2-3 and go straight to archive"). Mark Section 7 N/A in the archive commit body. — **Acknowledged N/A**: no Cloud Run deploy + no smoke. The archive commit body MUST note "Section 7 pre-archive smoke: N/A (docs + lint-rule only — no runtime impact)".
