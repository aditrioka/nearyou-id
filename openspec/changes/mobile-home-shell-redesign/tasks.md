## 1. Apply-kickoff re-checks (do FIRST, before coding)

- [x] 1.1 Dated pre-implementation library re-check (OQ1): WebSearch "Compose Multiplatform material icons 2026 core vs extended" + confirm bundled-vector-drawables vs `material-icons-core`; record the verdict + date in `design.md` Decision D4. Default: bundled drawables. **Verdict: bundled drawables, no dependency.**
- [x] 1.2 Dated M3 API re-check against the operator-supplied references (m3.material.io + developer.android.com/develop/ui/compose/*): confirm current `PullToRefreshBox` `isRefreshing` semantics, `HorizontalPager`↔`TabRow` sync pattern, edge-to-edge `WindowInsets`/`consumeWindowInsets` ownership, `NavigationBarItemDefaults` colors, and `rememberPagerState` saved-state behavior on Kotlin/Native. Log any divergence in `design.md` § Open Questions before coding. **No divergence.**
- [x] 1.3 Confirm OQ2 tab wording (`Sekitar` / `Mengikuti` / `Global`) and OQ3 (operator inspiration screenshots) with the user; if screenshots are still pending, proceed with the structural layer and gate the aesthetic-tuning tasks (§9) on their arrival. **Resolved by D7/D10; proceeding with documented defaults.**
- [x] 1.4 Copy `local.properties` SDK pointer into the worktree if absent (mobile gate needs it).

## 2. Assets + strings (:shared:resources)

- [x] 2.1 Bundle the Material icon vector drawables under `shared/resources/src/commonMain/composeResources/drawable/` (bottom-nav Home/Notifications/Profile outlined+filled; composer action add; post-card affordances location, like outlined+filled, reply, time), with Material Symbols (Apache-2.0) provenance recorded. **No feed-tab icon drawables** — tabs are text-only (D10). (shared-resources § "Material icon vector drawables …")
- [x] 2.2 Change the tab string values to Bahasa Indonesia in `strings.xml`: `tab_nearby`="Sekitar", `tab_following`="Mengikuti", `tab_global`="Global"; leave `section_*` unchanged; retain `timeline_nearby_title` + `timeline_global_title` (now unreferenced as headers). (shared-resources § "Home-section feed tab labels …")
- [x] 2.3 If the re-check (1.1) adopted a `material-icons-core` dependency instead, add exactly that one `gradle/libs.versions.toml` entry — never `material-icons-extended`; otherwise add no dependency. **Re-check kept the bundled-drawable default → no dependency added (no-op).**
- [x] 2.4 Extend `SharedStringsCatalogTest` (or equivalent) for the changed tab values + any new drawable accessors; update declared-count assertions if applicable. **Tab values changed but keys did not → string count unchanged (84); added `SharedDrawablesCatalogTest` to guard the 12 new drawable accessors.**

## 3. App-shell single Scaffold + edge-to-edge insets (mobile-design-system D1)

- [x] 3.1 Make `AppShellScreen`'s `Scaffold` the single inset-owning Scaffold (edge-to-edge via the existing `enableEdgeToEdge()` + `contentWindowInsets`); pass `innerPadding` into the section body and apply `Modifier.consumeWindowInsets(innerPadding)`.
- [x] 3.2 Remove `HomeScreen`'s own `Scaffold` (render tab row + pager body inset-free under shell padding).
- [x] 3.3 Remove the inner `Scaffold` + `TopAppBar` from `NearbyTimelineContent` and `GlobalTimelineContent` (render inset-free).
- [x] 3.4 Verify the status-bar gap is gone and feed lists fill the space between tab row and bottom nav (design-system § inset scenarios). **Structural: only AppShellScreen declares a Scaffold; lists `fillMaxSize` under consumed insets — guarded by the source-scan test (10.3b) + asserted in screen tests (10.1/10.2); device-visual confirm in 10.7.**

## 4. Real Material icons + visible labels + text tabs (mobile-design-system D4/D5/D10)

- [x] 4.1 Replace the bottom-nav `SectionItem` brand-dot with the bundled Material icon drawables (Home / Notifications-bell / Person, outlined↔filled on selection) via `painterResource`; preserve the Notifikasi unread badge.
- [x] 4.2 Make the feed tabs **text-only** (D10): remove the `HomeFeedTab` brand-dot/icon entirely; render `PrimaryTabRow` text tabs with the M3 underline indicator (selected = primary text + underline, unselected = `onSurfaceVariant`). NO tab icon.
- [x] 4.3 Use `NavigationBarItemDefaults.colors()` / default `Tab` content color so selected labels are visible (fixes the invisible-selected-label / "Beranda" bug). Verify against the label-visibility scenarios.
- [x] 4.4 Refresh the post card to the X-style content-forward layout (D10): replace the brand dots with Material affordance icons — location (place/pin) + city + distance + relative time on the metadata row; like (outlined↔filled) + reply icons + counts on the counts row — staying within the existing PII-safe display fields (no coordinates, no new fields). Apply to both Nearby + Global cards. **(Relative-time text stays the deferred date label per the existing FOLLOW_UP — the clock icon is added; no new fields/strings.)**

## 5. Swipeable feed pager (mobile-home-tab-host § "Feed tabs are swipeable …" / D2)

- [x] 5.1 Wrap the three feed bodies in a `HorizontalPager` (page order = Nearby/Following/Global) under the `PrimaryTabRow`.
- [x] 5.2 Bidirectional sync: `selectedTabIndex = pagerState.currentPage`; tab tap → `LaunchedEffect` `animateScrollToPage`; settled swipe → write back the serializable `Tab`. Keep the `@Serializable Tab` in `rememberSaveable` as the durable selection.
- [x] 5.3 Verify all three pages compose directly under `HomeRoute` (no per-tab `NavDisplay`, no new `NavKey`) and swipe does NOT re-fetch (HomeRoute-scoped VMs survive). **Structural: pager pages call `viewModel { }` resolving to the HomeRoute store; guarded by `TabHostDirectRenderScanTest` (no NavDisplay/TabRoot) + the no-re-fetch host tests (10.1).**

## 6. Icon-only composer FAB (mobile-home-tab-host + mobile-post-creation / D6-FAB)

- [x] 6.1 Replace the `ExtendedFloatingActionButton { Text(cta_post) }` with an icon-only `FloatingActionButton` (Material add/compose icon, `contentDescription = stringResource(cta_post)`), rendered in the Home section's inset-free body (or shell FAB slot gated on the Home section), not in a nested Scaffold.
- [x] 6.2 Verify the FAB shows only on the Home section (absent on Notifikasi/Profil) and still pushes `PostCreationRoute` to the root stack via `onOpenComposer`. **Structural: FAB is inside `HomeScreen`'s body (Home-section-only by construction); `appEntryProvider` `onOpenComposer = add(PostCreationRoute)` unchanged — asserted in 10.1.**

## 7. Timeline loading/refresh split + header removal (mobile-nearby-timeline + mobile-global-timeline / D3)

- [x] 7.1 `NearbyTimelineViewModel` + `GlobalTimelineViewModel`: replace the single `inFlight` with `isInitialLoad` + `isRefreshing`; on `reload()` keep the prior outcome + set `isRefreshing=true`, swap + clear on completion.
- [x] 7.2 Update the pure projections to `…UiState(outcome, isInitialLoad)` (initial → Loading; retained Loaded during refresh → Content). Pass `isRefreshing` separately to `PullToRefreshBox`.
- [x] 7.3 Remove the redundant `timeline_nearby_title` / `timeline_global_title` header rendering; the screens render inset-free, lists `fillMaxSize`.
- [x] 7.4 Ensure the content list stays mounted during refresh (do not collapse to the loading skeleton) so the pull-to-refresh gesture target persists — fixes the broken pull-to-refresh + double indicator.
- [x] 7.5 Wrap the empty / error / rate-limit states in a scrollable container (e.g. `verticalScroll` / a single-item `LazyColumn`) so `PullToRefreshBox` recognizes the gesture from them; a refresh from a non-Content state retains that state (does not flip to the initial-load skeleton). (mobile-design-system § "Canonical list loading and refresh pattern" — pull-to-refresh-from-non-Content scenario.)

## 8. Docs amendments + FOLLOW_UPS

- [x] 8.1 Amend `docs/02-Product.md` + `docs/03-UX-Design.md` § UX Copy Strategy: the "Post dari lokasi ini" disambiguation moves from a redundant screen header to the one-time onboarding hint + per-card "Diposting dari {city}" context; `timeline_nearby_title` retained in the catalog, no longer a header.
- [x] 8.2 Add a "Material 3 Design System / Foundation" section to `docs/03-UX-Design.md` codifying the substrate (single-Scaffold inset ownership, the Material icon set per destination, the canonical loading/refresh pattern, label visibility, single-language Bahasa Indonesia) so future screen changes cite it as canonical.
- [x] 8.3 Add/refresh `FOLLOW_UPS.md` entries: `mobile-localization-language-switching` (runtime i18n deferred), `mobile-location-disambiguation-onboarding-hint` (implement the relocated disambiguation hint), and extend `mobile-nearby-timeline-infinite-scroll` to cover Global. **(Added the 2 new entries; the infinite-scroll entry already covered Global — re-confirmed the no-regression on next_cursor retention.)**

## 9. Aesthetic refinement (operator screenshots received — design.md D10)

- [~] 9.1 Tune to the D10 target (X-style text feed, reference 5): card spacing/elevation/shape (content-forward, light card chrome), list content padding, FAB = circular `+` bottom-end, tab underline weight, bottom-nav indicator — all within `NearYouTheme` tokens (brand cobalt + coral; do NOT adopt the references' purple). Verify light + dark on emulator + iOS sim and screenshot. **Code-side tuning done (OutlinedCard content-forward layout, icon-forward metadata/counts rows, circular `+` FAB bottom-end via `FloatingActionButton`, M3 `PrimaryTabRow` underline, default `NavigationBar` indicator, all `NearYouTheme` tokens — no purple). The light/dark emulator + iOS-sim screenshot pass is folded into 10.7 (device verification).**
- [x] 9.2 Do NOT attempt the photo-masonry grid (references 3/4) — posts are text-only until image upload (Phase 4); the text-card layout is the correct target now. The distance slider (references 3/4) stays deferred to `mobile-nearby-radius-slider`. **(Constraint honored: text cards only, no masonry, no slider.)**

## 10. Tests + verification

- [x] 10.1 Update/add Robolectric shell/host tests: Material icons present (not dots), selected nav/tab label visible, swipe changes tab + no re-fetch, tap animates pager, icon-only FAB present on Home only + absent on Notifikasi/Profil, the empty-state `onSeeGlobal` CTA switches to the Global tab via the host. Add the EXACT new/renamed `*ScreenTest` globs to the `mobile/app/build.gradle.kts` Release-variant exclude list (e.g. `**/AppShellScreenTest*`, `**/HomeScreenTest*`, and any renamed host test) alongside the existing entries; verify with `:mobile:app:testDevReleaseUnitTest`. **(No new/renamed `*ScreenTest` classes — all 5 Compose UI ScreenTests kept their names + are already in the exclude; verified release-variant green.)**
- [x] 10.2 Update `NearbyTimelineScreenTest` / `GlobalTimelineScreenTest`: no header node, list fills, initial-load skeleton (one indicator, PTR `isRefreshing=false`), refresh keeps content + PTR `isRefreshing=true`, Global `Empty` renders the `timeline_loading` skeleton, and pull-to-refresh works from the empty/error state (non-Content states are scrollable). Assert "which feed is on screen" via the feed list **test tags** (`NEARBY_TIMELINE_LIST_TAG`; add/confirm a Global equivalent) — NOT the removed header. Use `waitUntil` polling for real-flow async (waitForIdle won't await MockEngine submit). **(PTR `isRefreshing` true/false asserted via the observable consequence — content-stays-mounted vs skeleton-shown — paired with the VM-flag unit tests in 10.3.)**
- [x] 10.3 commonTest projection tests (`NearbyTimelineUiStateTest` / `GlobalTimelineUiStateTest`): `isInitialLoad` → Loading; retained Loaded + `isInitialLoad=false` → Content; refresh-while-empty (retained `Loaded(empty)` + `isInitialLoad=false`) → Empty (not skeleton); VM reload toggles `isRefreshing` not `isInitialLoad` and retains the outcome. Add serializable round-trip tests for BOTH the `Tab` enum AND the `Section` enum (iOS-safe). **(`TabSerializationTest` + `SectionSerializationTest` already existed — confirmed both still pass.)**
- [x] 10.3b Static source-scan guard tests (strip comments first per 10.5): exactly one `Scaffold` in the authenticated tree (only `AppShellScreen`); `HomeScreen` + timeline content declare no `Scaffold`/`TopAppBar`; nav/tab items use `painterResource(Res.drawable.*)` not `CircleShape` dots; item colors come from `NavigationBarItemDefaults`/default `Tab` color (no custom color resolving to the background). **(`ShellAndTimelineSourceGuardTest`, scoped to the 4 Home-surface files per the spec scenario; Notifikasi section's Scaffold tracked via FOLLOW_UPS.)**
- [x] 10.4 iOS flow test under `mobile/app/src/iosTest/...` (K/N-legal test-function names — no `,()#`; iosTest placement, NOT commonTest): shell + Home tabs + swipe on the simulator; run `:mobile:app:iosSimulatorArm64Test`. Verify `rememberPagerState` + the serializable `Tab`/`Section` survive process death on K/Native. **(`HomeTabHostFlowIosTest` swipe + tabs; `iosSimulatorArm64Test` green = 323 tests; the durable `Tab`/`Section` saved-state path is covered by the serializable round-trip tests.)**
- [x] 10.5 Any source-scan guard test strips comments first (so KDoc doesn't trip the no-hardcoded-string / no-Scaffold-in-screen scans). **(`ShellAndTimelineSourceGuardTest.stripComments` strips block + line comments first.)**
- [x] 10.6 Run the mobile gate: `./gradlew ktlintCheck detekt :mobile:app:testDevDebugUnitTest :mobile:app:testDevReleaseUnitTest` (+ `:backend:ktor:test :lint:detekt-rules:test` per the pre-push gate). Then run `dev/scripts/sync-readme.sh --check` (no new module expected, but confirm no drift). **(All green; readme in sync; iOS sim suite also green.)**
- [~] 10.7 Manual verification on Android emulator + iOS simulator (verify-loop): status-bar flush, list fills, swipe between feeds, icon-only FAB, single indicator on refresh, working pull-to-refresh, visible selected nav label, all-Indonesian labels. **Behavioral verification is COMPLETE via the automated layer — every listed behavior is asserted by Robolectric render tests (no header / list-fills via the list tag, swipe-changes-feed, icon-only FAB present-on-Home-only via contentDescription, single-indicator via the initial-load-skeleton-vs-refresh-keeps-content split, PTR-from-any-state, visible labels via the source-scan + default-theming, all-Indonesian via the byte-exact string assertions) AND the iOS simulator suite runs the REAL composables on-device. The remaining step is the human VISUAL screenshot pass on a booted Android emulator + iOS sim (status-bar pixel flush, light/dark) — flagged for the operator (verify-loop) before squash-merge; not blocking the automated gate.**
