# Tasks — mobile-chat-thread-viewmodel-statein (audit follow-up #414)

> The same mechanical shape as 05-#6 / #410, with a **3-input** combine: add `uiState` via `combine(_historyOutcome, initialLoad, _rows).stateIn(viewModelScope, SharingStarted.WhileSubscribed(5_000), ChatThreadUiState.Loading)` delegating to the unchanged 3-arg pure `chatThreadUiState(…)`; make the initial-load flag private (rename `_isInitialLoad`→`initialLoad`, drop the public `isInitialLoad` accessor); keep `historyOutcome` + `rows` + the send/report flows public; the screen collects `uiState` (drop the in-composable `remember(historyOutcome, isInitialLoad, rows) { … }`). Behavior-preserving.

## 1. ChatThreadViewModel + screen

- [x] 1.1 In `ChatThreadViewModel.kt` add `val uiState: StateFlow<ChatThreadUiState> = combine(_historyOutcome, initialLoad, _rows) { o, i, r -> chatThreadUiState(o, i, r) }.stateIn(viewModelScope, SharingStarted.WhileSubscribed(5_000), ChatThreadUiState.Loading)`; rename `_isInitialLoad`→`initialLoad` (private `MutableStateFlow`) and remove the public `val isInitialLoad` accessor (update the `resyncFirstPage()` `finally`'s `if (initial) … .value = false`); keep public `historyOutcome` + `rows` + `sendOutcome` + `sendInFlight` + the report/start flows. Add the imports `kotlinx.coroutines.flow.SharingStarted` / `combine` / `stateIn`. Update the property KDoc to describe the single-`uiState` shape (docs/11 §2.2).
- [x] 1.2 In `ChatThreadScreen.kt` replace the `val rows` / `val historyOutcome` / `val isInitialLoad` collects + `uiState = remember(historyOutcome, isInitialLoad, rows) { chatThreadUiState(...) }` with `val uiState by viewModel.uiState.collectAsStateWithLifecycle()` passed through (`uiState = uiState`). Keep the `sendOutcome` / `sendInFlight` / `reportTargetMessageId` / `reportMessage` collects and the `sendBar = remember(sendOutcome, sendInFlight) { sendBarState(...) }` projection (orthogonal send-bar signal, out of scope). The `remember` import stays (still used by `sendBar` + `mutableStateOf`/`SnackbarHostState`).

## 2. New uiState scenario coverage (per spec delta)

- [x] 2.1 Add to `ChatThreadViewModelTest`: a "uiState delegates to the pure projection" test — after the first resync resolves a `Loaded` history outcome, activate a `backgroundScope.launch { viewModel.uiState.collect {} }` collector, `advanceUntilIdle()`, then assert `viewModel.uiState.value == chatThreadUiState(viewModel.historyOutcome.value, isInitialLoad = false, viewModel.rows.value)` AND `viewModel.uiState.value is ChatThreadUiState.Content` (the pure function is reused, not reimplemented).
- [x] 2.2 Add to `ChatThreadViewModelTest`: a "config-change proxy" test — a `Loaded` outcome with rows projects to `Content`; a SECOND fresh `uiState` collector on the same VM still sees `Content` (the entry-scoped VM retained `_historyOutcome` + `_rows`, not reset to `Loading`).
- [x] 2.3 Confirm the existing pure-projection unit tests (`ChatThreadUiStateTest`) still pass unchanged (the 3-arg `chatThreadUiState` signature is reused, not modified) and the existing realtime/merge `ChatThreadViewModelTest` cases still pass (they read the retained `rows` / `historyOutcome` seams).
- [x] 2.4 Confirm the Robolectric `ChatThreadScreenTest` renders the same states over the `uiState`-collecting screen (it drives via `FakeChatFlow` and inspects no VM flow wiring — should be green unchanged).

## 3. Spec sync + gate + verification

- [x] 3.1 `openspec validate mobile-chat-thread-viewmodel-statein --strict` passes.
- [x] 3.2 Pre-push gate: `./gradlew ktlintCheck detekt :lint:detekt-rules:test :mobile:app:testDevDebugUnitTest :mobile:app:testDevReleaseUnitTest`. `:backend:ktor:test` deferred to CI (mobile-commonMain-only change, zero backend files touched, local DB not migrated).
- [x] 3.3 `./gradlew :mobile:app:iosSimulatorArm64Test`: K/N compile ✓ + the new/existing commonTest VM tests pass on iOS. (Any pre-existing `*FlowIosTest` DI-drift failures, #348, are unrelated — iOS is never CI-gated and this change touches no DI.)
- [ ] 3.4 Manual verification per docs/11 §5 DoD (UI-affecting screen): build `installDevDebug`, seed the back stack to `ChatThreadRoute` via a temp offline harness (Koin-override fakes for `ChatFlow` + `ChatRealtimeSubscriber` + `ViewerIdProvider`; reverted before commit), and capture the thread rendering the **Content** state (own-vs-other bubbles + input bar) over the new single-`uiState` collection. Screenshot evidence in the PR body. The Robolectric `ChatThreadScreenTest` renders the same states green.
