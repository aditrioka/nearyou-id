## 1. Schema (V23 migration)

- [x] 1.1 Add `V23__referral_tickets.sql` creating `referral_tickets` (`id UUID PK DEFAULT gen_random_uuid()`, `inviter_user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE`, `invitee_user_id UUID NOT NULL UNIQUE REFERENCES users(id) ON DELETE CASCADE`, `status VARCHAR(32) NOT NULL CHECK (status IN ('pending_activity','granted','expired'))`, `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`, `expires_at TIMESTAMPTZ NOT NULL`) with a leading comment header in the V9/V6 style.
- [x] 1.2 Add the future-worker partial index `CREATE INDEX referral_tickets_pending_idx ON referral_tickets (expires_at) WHERE status = 'pending_activity';` — verify the `WHERE` predicate is the CONSTANT status only (NO `NOW()`/volatile expression, partial-index invariant).
- [x] 1.3 Add the per-inviter counter index `CREATE INDEX referral_tickets_inviter_status_idx ON referral_tickets (inviter_user_id, status);`.
- [x] 1.4 Confirm the migration number is the next free one at apply time and rebase if another in-flight change landed it first (the V23 slot is contended — several proposal PRs are open concurrently; the exact number resolves at squash-merge/rebase time). Verify `flyway validate` passes on a fresh DB. *(As-built: V23 is free on origin/main; verified by a clean Flyway V1→V23 migrate on a fresh disposable postgis container. The SHARED dev DB on :5433 carries a foreign V23 from a concurrent branch — verify against a fresh container, not the shared dev DB.)*

## 2. Referral feature package (`id.nearyou.app.referral`)

- [x] 2.1 Add `ReferralTicketCreator` interface (single best-effort method `suspend fun createTicketIfInvited(inviteCode, inviteeUserId, inviteeDeviceFingerprintHash)`) + a `NoopReferralTicketCreator` default — the narrow seam `SignupService` depends on (docs/11 §3.1 cross-feature-via-interface).
- [x] 2.2 Add `ReferralRepository` owning the `referral_tickets` INSERT (`status='pending_activity'`, `expires_at = NOW() + INTERVAL '14 days'`). *(As-built deviation per docs/11 §3.1: the inviter reverse lookup lives in `UserRepository.findInviterByInviteCodePrefix` — the user feature owns the `users` table — NOT in `ReferralRepository`. `ReferralService` calls both.)* Blocking JDBC; the caller wraps in `withContext(dbDispatcher)`.
- [x] 2.2a Raw `FROM users` inviter-lookup read is covered by the EXISTING class-level `@AllowMissingBlockJoin` on `JdbcUserRepository` (its reason string already names "invite-code prefix" auth-plane reads) — no new annotation needed; `RawFromPostsRule` does not apply. Verified by `detekt` passing.
- [x] 2.3 Implement `ReferralService` (implements `ReferralTicketCreator`): resolve code → inviter checks (live-by-query / `is_banned=FALSE` / `created_at < NOW() - 30d` / `inviter_id != inviteeUserId`) → invitee device-fingerprint non-collision (both non-null) → burst-rate gate → INSERT. Swallow every failure; structured-log every attempt (created / rejected+reason) per the `signup.blocked` precedent.
- [x] 2.4 Wire the per-inviter burst limiter via the shipped `RateLimiter` **axis-agnostic** `tryAcquireByKey` (NOT per-user `tryAcquire`), 7-day window, max 3, key `{scope:rate_referral_ticket}:{inviter:<inviter_id>}` *(as-built: `rate_` scope prefix for convention coherence with `FollowRateLimiter`; still satisfies `RedisHashTagRule.STRICT_HASH_TAG_PATTERN`; scope does not end in `_day` → sliding window)*. LAST gate before INSERT; the shipped `RedisRateLimiter` is fail-soft (Redis down → `Allowed`, so the burst cap is best-effort — signup unaffected either way; design.md Risks). Not `computeTTLToNextReset`.
- [x] 2.5 Wire `ReferralRepository` + `ReferralService` into `SignupService` in `Application.kt` (the backend's manual-DI pattern — direct construction, reusing the shared `RateLimiter`/`DataSource`/`dbDispatchers.db`), no new secret (`invite-code-secret` already wired). *(As-built: passed to `SignupService` via the `referral` constructor param; no standalone Koin `single` needed since nothing else injects `ReferralTicketCreator`.)*

## 3. Signup wiring

- [x] 3.1 Add `@SerialName("invite_code") val inviteCode: String? = null` to `SignupRequestDto` (`SignupRoutes.kt`) and thread it onto the internal `SignupRequest` data class (default null).
- [x] 3.2 In `SignupService`, add the `ReferralTicketCreator` constructor dependency (default `NoopReferralTicketCreator`) and call it best-effort AFTER the `users` INSERT commits (step 5.5) — wrapped so any throwable is logged and never propagates. Signup result + status code unchanged.
- [x] 3.3 Confirm the success response and all five existing signup error codes are unchanged (referral introduces no new code — `auth-signup` Error-taxonomy-stability stays satisfied; verified by `AuthWireFormatTest` + the 13 existing `SignupFlowTest` cases still green).

## 4. Tests

- [x] 4.1 `ReferralServiceTest` service-container tests (`@Tags("database")`): valid code → `pending_activity` ticket with `expires_at = created_at + 14d`; absent/blank/unresolvable code → no ticket; soft-deleted / banned / <30d inviter → no ticket; self-invite → no ticket; device-fingerprint collision → no ticket; null invitee fingerprint → not a collision; burst boundary (3rd created, 4th rejected); rolling-window expiry (4th allowed after 7d); duplicate `invitee_user_id` → swallowed; hard-delete of inviter or invitee → cascade. **16/16 green** against a fresh postgis container. *(As-built: burst tests use the `InMemoryRateLimiter` double with an injectable clock.)*
- [x] 4.2 Signup integration (in `SignupFlowTest`): valid seeded inviter code → 201 + ticket; garbage code → 201 + no ticket; no code → 201 + no ticket. **16/16 green** (3 new + 13 existing, no regression).
- [x] 4.3 Extend `AuthWireFormatTest` to pin the `invite_code` wire field (snake_case) + optionality (absent → null). **8/8 green.**
- [x] 4.4 Partial-index migration applies on a fresh DB — covered by the clean V1→V23 Flyway migrate + exercised by `ReferralServiceTest` (cascade + EXPLAIN tests touch the table/indexes).
- [x] 4.5 `EXPLAIN` index-scan test (docs/08:329): asserts an `Index` plan (not `Seq Scan`) for the inviter resolution query, with `SET LOCAL enable_seqscan=off` (planner picks Seq Scan on tiny tables otherwise). Collision-retry half of docs/08:329 already covered by `InviteCodePrefixTest`.

## 5. Conformance, docs, verification

- [x] 5.1 Reconcile docs/05-Implementation.md § Referral System: narrowed DESIGN → PARTIAL (ticket creation shipped; worker / `granted_entitlements` / grants / IP+identifier anti-collision legs remain DESIGN); added the docs/08-#22-vs-docs/01-§3 anti-collision reconciliation note. docs/01/08 narrative intact.
- [x] 5.2 Full pre-push gate green: `ktlintCheck` ✓, `detekt` ✓, `:lint:detekt-rules:test` ✓, CI-equiv `:backend:ktor:test -Dkotest.tags='!database'` ✓ (+ the three DB specs above against a fresh container).
- [ ] 5.3 Pre-archive staging smoke (docs/11 §5 DoD). NOTE: this change adds NO new secret, env var, config, or Gradle module — only a `users`-feature method, a `referral` package, a `referral_tickets` table, and one optional signup field. The deploy-config drift surface a staging smoke exists to catch (the `like-rate-limit` Redis-TLS class of bug) is therefore essentially nil, and the runtime behavior is already verified against a real Postgres + the real migration locally. Pending an explicit decision: run the staging branch-deploy smoke anyway, or accept local verification + proceed to archive.
- [x] 5.4 Update the PR title/body at the phase boundary (`feat(backend): referral ticket creation at signup (V23)`) and post `/review` per the apply workflow.
