## 1. SignInViewModel + SignInScreen

- [x] 1.1 Add `SignInViewModel` (commonMain, `screens/auth/SignInViewModel.kt`) modeled on `UsernameCustomizationViewModel`/`AppealViewModel`: private `VmState(outcome, inFlight, sessionExpired, navigation: SignInNavTarget?)`, single `uiState: StateFlow<SignInUiState>` via `state.map { it.toUiState() }.stateIn(viewModelScope, WhileSubscribed(5_000), …)` delegating to the unchanged `signInUiState(outcome, inFlight)`; constructor deps `AuthFlow`, `PendingSignupIdentity`, `PendingReturnDestination`. Capture `sessionExpired = pendingReturnDestination.wasInvoluntary()` once in `init`.
- [x] 1.2 Implement `onSignInClick()` (single in-flight; `signInWithGoogle()` on `viewModelScope` with CE-rethrow + `ensureActive()`), the `NoAccount` path (`pendingSignupIdentity.set(idToken)` → raise `navigation = AgeGate` → clear consumed `outcome`), the `Success` path (raise `navigation = Home`), and `onNavigationHandled()` (clears the nav one-shot). Expose the banned-state + appeal-entry flags through `uiState`.
- [x] 1.3 Rewrite `SignInScreen` to resolve `viewModel { SignInViewModel(...) }`, read `uiState` via `collectAsStateWithLifecycle()`, render from it, drive the CTA via `viewModel.onSignInClick()`, and consume the nav one-shot in `LaunchedEffect(ui.navigation) { … invoke onSignedIn/onNoAccount; viewModel.onNavigationHandled() }`. Remove all `remember`/`rememberCoroutineScope` state; keep every string via `stringResource`.
- [x] 1.4 Add `SignInViewModelTest` (commonTest, `runTest`, fake `AuthFlow`/holders) covering: single-`uiState`-equals-`signInUiState`; outcome retention across a fresh `uiState` read (config-change proxy); double-tap → one `signInWithGoogle()`; NoAccount sets identity-before-nav + clears outcome after `onNavigationHandled()`.

## 2. AgeGateViewModel + AgeGateScreen

- [x] 2.1 Add `AgeGateViewModel` (`screens/auth/AgeGateViewModel.kt`): private `VmState(selectedDobMillis, showPicker, outcome, inFlight, navigation, identityAbsent)`, single `uiState` via `stateIn` delegating to `ageGateUiState(outcome, isDobSubmittable(selectedDob, today), inFlight)` (+ surface `selectedDob`/`showPicker`/`identityAbsent`); deps `AuthFlow`, `PendingSignupIdentity`, `today: LocalDate = systemToday()`. `init` runs the absent-identity guard: if `pendingSignupIdentity.peek() == null` → `clear()` + raise the sign-in re-route one-shot + set `identityAbsent`.
- [x] 2.2 Implement `onDobPicked(millis)`, `onShowPicker(Boolean)`, `onCreateAccountClick()` (single in-flight `signUpWithGoogle(idToken, dob)` on `viewModelScope`, CE-rethrow + `ensureActive()`), terminal handling via the reused pure `handleAgeGateTerminalOutcome` (clear holder + raise the matching nav one-shot; retryable does NOT clear), and `onNavigationHandled()`.
- [x] 2.3 Rewrite `AgeGateScreen` to resolve `viewModel { AgeGateViewModel(...) }`, collect `uiState` with lifecycle, drive the DOB picker + CTA through VM methods, early-return on `identityAbsent`, and consume the nav one-shot in a `LaunchedEffect`. Remove all `remember`/`rememberCoroutineScope` state; keep the `DobPickerDialog`/`NonFutureSelectableDates` composables and all `stringResource`s.
- [x] 2.4 Add `AgeGateViewModelTest` (commonTest): single-`uiState` shape; picked-DOB retention AND `showPicker` retention across a fresh `uiState` read (D5: rotation keeps the picker open — assert both, not manual-only); terminal `Success`/`AccountExists` clear the holder + raise nav while `RetryableError` does not; absent-identity-on-init raises the sign-in re-route + clears + signals `identityAbsent` in the initial state (form never renders a frame); double-tap → one `signUpWithGoogle`.

## 3. ConsentViewModel + ConsentScreen

- [x] 3.1 Add `ConsentViewModel` (`screens/consent/ConsentViewModel.kt`): private `VmState(analytics, crash, ads, outcome, inFlight, done)` seeded from `initialAnalytics`/`initialCrash`/`initialAdsPersonalization` ctor params (defaults false/true/false); single `uiState` via `stateIn` delegating to `consentUiState(outcome, inFlight)` and surfacing the three toggle booleans; deps `ConsentFlow`, `ConsentSnapshotStore`.
- [x] 3.2 Implement `onAnalyticsChange`/`onCrashChange`/`onAdsChange`, `onContinueClick()` (single in-flight `submitConsent(analytics, crash, ads)` on `viewModelScope`; on `Success` write `snapshotStore.write(ConsentSnapshot(...))` BEFORE raising the `done` one-shot — mirroring `handleConsentTerminalOutcome`), and `onDoneHandled()`.
- [x] 3.3 Rewrite `ConsentScreen` to resolve `viewModel { ConsentViewModel(...) }`, collect `uiState`, drive the three `Switch`es + CTA + post-failure skip through VM methods, consume the `done` one-shot in a `LaunchedEffect`. Remove all `remember`/`rememberCoroutineScope` state; keep the `ConsentToggleRow` composable and all `stringResource`s.
- [x] 3.4 Add `ConsentViewModelTest` (commonTest): single-`uiState` shape; toggle retention across a fresh `uiState` read; double-tap → one `submitConsent`; snapshot written exactly once on `Success` before the `done` one-shot; `RetryableError` writes no snapshot and surfaces the skip.

## 4. Existing-test reconciliation + gates

- [x] 4.1 Verified `SignInScreenTest` / `AgeGateScreenTest` / `ConsentScreenTest` (androidUnitTest) pass **unchanged** against the VM-based screens — no `waitUntil` adjustment was needed (the screen-test fakes return synchronously on `Dispatchers.Main.immediate`, so the now-`viewModelScope` submit drains under `waitForIdle`; `ConsentScreenTest` already polled with `waitUntil` from a prior change). The pure `*UiStateTest` / `*OutcomeHandlerTest` (commonTest) are untouched and green.
- [x] 4.2 Run the mobile gate: `:mobile:app:testDevDebugUnitTest` + `:mobile:app:testDevReleaseUnitTest` (new `*ScreenTest`s must be in the Release-variant exclude if any added); ensure all new `commonTest` VM tests pass.
- [x] 4.3 Ran `:mobile:app:iosSimulatorArm64Test` (filtered) — iOS compile green; the 3 new VM tests + `AgeGateFlowIosTest` PASS on iOS (proving `viewModel { }` resolves on K/N). The 4 fails (`SignInFlowIosTest.initialRender` stale title assertion + `ConsentFlowIosTest` `NoDefinitionFoundException: ConsentSnapshotStore`) are **pre-existing #348 reds** NOT introduced here: the iOS test files are untouched by this diff, the title was removed by `mobile-mockup-visual-conformance` (Android test asserts it absent), and the original `ConsentScreen` already `koinInject`'d `ConsentSnapshotStore` (#198) — identical throw point. iOS is not CI-gated.
- [x] 4.4 Lint gate green locally: `:mobile:app:ktlintCheck` + `:mobile:app:detekt` + `:lint:detekt-rules:test` PASS. `:backend:ktor:test` NOT run locally — this diff touches **zero** backend/shared/infra files (provably mobile-only), so the backend suite is byte-identical to green `main`; running it against the 14h dev DB risks false-positive pollution failures (docs/13 §5 caveat). Covered by CI on fresh containers. (No `:backend:ktor:run` was alive during the gate.)

## 5. Manual verification (verify-loop §B) + backlog close-out

- [ ] 5.1 Android device/emulator verify-loop §B: walk SignIn → AgeGate → Consent → Home; explicitly **rotate mid-flow** (pick a DOB → rotate → DOB retained; flip a consent toggle → rotate → toggles retained; CTA never sticks on "loading" across rotation). Capture screenshot evidence for the PR body (docs/11 §5 DoD).
- [ ] 5.2 Update the audit backlog: edit `dev/audits/2026-06-10-holistic-audit/PROGRESS.md` § "Remaining after wave 7" to mark 05-#5 shipped for SignIn/AgeGate/Consent (PostCreation/PostDetail already done), and to re-scope 05-#6 (timeline-VM retrofit only) + 05-#7 (`koinViewModel` global conversion) as the distinct remaining items; update the `/audit-burndown` skill menu row for 05-#5 to the stale-sketch reality.
- [ ] 5.3 PR title/body kept current at each phase boundary; squash-merge one commit per change.
