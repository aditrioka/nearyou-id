## 1. Discovery & infra verification

- [ ] 1.1 Verify (during reconciliation) Free 50/day, Premium unlimited, no burst — consistent across `02-Product.md:318` and `05-Implementation.md` Layer 2 table. Confirm `chat-foundation` proposal (PR #61) explicitly named `chat-rate-limit` as a follow-up consumer of `rate-limit-infrastructure`.
- [ ] 1.2 Verify `openspec/specs/rate-limit-infrastructure/spec.md` confirms the `RateLimiter` interface contract (`tryAcquire(userId, key, capacity, ttl): Outcome`, `releaseMostRecent(userId, key)`) and `computeTTLToNextReset` placement in `id.nearyou.app.core.domain.ratelimit`.
- [ ] 1.3 Verify chat-foundation send-message ordering by reading the chat-foundation send route + service: current sequence is auth → UUID → conversation existence + active-participant check → bidirectional block check → JSON parse + content guard → INSERT + `last_message_at` UPDATE in TX → 201. New ordering inserts the rate limiter BETWEEN UUID validation AND the conversation-existence check.
- [ ] 1.4 Verify: `:infra:redis` module exists (per like-rate-limit shipped). CI workflow runs `redis:7-alpine` service container with `REDIS_URL=redis://localhost:6379`. No new CI infra needed.
- [ ] 1.5 Verify: `UserPrincipal.subscriptionStatus: String` exists at `backend/ktor/src/main/kotlin/id/nearyou/app/auth/AuthPlugin.kt:34`. RevenueCat webhook handler does not yet exist in the backend (Phase 4 work per `docs/08-Roadmap-Risk.md`); the JWT TTL window concern raised during like / reply review remains moot until Phase 4 lands the handler. Confirm the existing `auth-jwt-spec-debt-userprincipal-subscription-status` `FOLLOW_UPS.md` line item still tracks this — extend its scope only if needed.
- [ ] 1.6 **Defer to manual user verification.** Mobile chat-compose 429 handling cannot be reliably checked from this scope (Compose Multiplatform UI code). The backend 429 contract is correct regardless; the generic timeline rate-limit screen catches uncaught 429s. File in commit body as a known unverified-at-merge item.

## 2. `premium_chat_send_cap_override` Remote Config flag plumbing

- [ ] 2.1 Add `PREMIUM_CHAT_SEND_CAP_OVERRIDE_KEY = "premium_chat_send_cap_override"` constant to the chat send service companion object (mirrors `LikeService.PREMIUM_LIKE_CAP_OVERRIDE_KEY` and `ReplyService.PREMIUM_REPLY_CAP_OVERRIDE_KEY` placement; the `RemoteConfig` interface itself is key-agnostic — `getLong(key)` accepts any string, so no interface-level change is needed).
- [ ] 2.2 Verify: `RemoteConfig.getLong("premium_chat_send_cap_override")` resolves through the same code path as the like and reply flags. `StubRemoteConfig` returns null for any key, so the unset-fallback path is exercised by default in dev/test.
- [ ] 2.3 Document mirror semantics in the `resolveDailyCap` KDoc on the chat send service (a one-line note pointing at `LikeService.resolveDailyCap` / `ReplyService.resolveDailyCap` is sufficient — no separate per-key comments on `RemoteConfig`).

## 3. Apply rate limit to `POST /api/v1/chat/{conversation_id}/messages`

- [ ] 3.1 Inject `RateLimiter` + `RemoteConfig` (+ optional `clock` for tests) into the chat send service. Reuse the `RateLimitOutcome` sealed type already in `:core:domain` (extracted by like-rate-limit; same shape as the reply path). The rate-limit gate is exposed as a separate `suspend fun tryRateLimit(userId, subscriptionStatus)` method (rather than embedded in `send()`) so the route can run the limiter BEFORE the participant lookup / block check / content guard / INSERT — this satisfies the spec's "limiter BEFORE all DB reads" ordering. The `send()` method itself stays unchanged in shape from chat-foundation.
- [ ] 3.2 `resolveDailyCap(userId)` reads `remoteConfig.getLong(PREMIUM_CHAT_SEND_CAP_OVERRIDE_KEY)` with try/catch. Unset / null / ≤ 0 / > 10,000 (anti-typo clamp per spec) / SDK error all coerce to default 50. WARN logs on each fallback; INFO log on the override-applied happy path. Mirrors `ReplyService.resolveDailyCap` byte-for-byte (just default 50 instead of 20).
- [ ] 3.3 Daily-limiter call lives in the chat send service's `tryRateLimit`: skips entirely if `subscriptionStatus in PREMIUM_STATES`. Otherwise calls `tryAcquire(userId, "{scope:rate_chat_send_day}:{user:$userId}", cap, computeTTLToNextReset(userId, clock()))` inside `withContext(Dispatchers.IO)`. Returns `RateLimitOutcome.RateLimited` (mapped to 429 + `Retry-After` by the route) or `Allowed`.
- [ ] 3.4 Verify chat send route ordering: auth (`call.principal<UserPrincipal>()`) → UUID validation on `conversation_id` (`parseUuid`) → `service.tryRateLimit(userId, principal.subscriptionStatus)` → 429 short-circuit on `RateLimited` → conversation existence + active-participant check → bidirectional block check → `call.receive<ChatMessageCreateRequest>()` JSON parse → `contentGuard.enforce(CHAT_CONTENT_KEY, body.content)` → `service.send()` (which does INSERT + `last_message_at` UPDATE in one TX) → 201. The limiter MUST run before any DB read AND before JSON parsing / content guard — confirm by reading the route file.
- [ ] 3.5 Verify the chat send service does NOT call `RateLimiter.releaseMostRecent` anywhere. Add explicit KDoc note documenting the asymmetry with `LikeService.like()` (no `INSERT ... ON CONFLICT DO NOTHING` no-op equivalent on `chat_messages` since every successful POST is a real new row). Mirror the same KDoc note already in `ReplyService.post()`.
- [ ] 3.6 Audit-log line emitted at INFO from `resolveDailyCap` when cap is a non-default value: `event=remote_config_override_applied key=premium_chat_send_cap_override value=<cap> user_id=<uuid>`. Match the like-flag and reply-flag audit-log shape byte-for-byte.
- [ ] 3.7 Run `./gradlew :backend:ktor:detekt :lint:detekt-rules:test -PincludeMobile=false` — verify both `RateLimitTtlRule` and `RedisHashTagRule` pass on the new call site (the `_day}` substring in `{scope:rate_chat_send_day}:{user:$userId}` matches `RateLimitTtlRule`'s daily-key pattern, and the strict hash-tag shape passes `RedisHashTagRule`). 0 detekt findings expected on the touched backend kotlin files.
- [ ] 3.8 Chat-foundation send-endpoint contracts preserved: response shapes (POST 201 with `ChatMessageDto`, the `last_message_at` UPDATE atomicity, the bidirectional block 403, the shadow-banned-sender persists, the embedded-field silent-ignore + WARN log, the redacted-message NULL-content rendering) all unchanged at the route level. The chat-foundation `send()` body stays byte-for-byte unchanged. Update the chat-foundation route test class only to pass the new constructor args (`NoOpRateLimiter()` / existing `noopLimiter`); `./gradlew :backend:ktor:compileTestKotlin` green. Runtime verification (test pass byte-for-byte) lands at section 5.3 (CI run).

## 4. Tests for `POST /chat/{id}/messages` rate limit

- [ ] 4.1 Implement `ChatSendRateLimitTest` at `backend/ktor/src/test/kotlin/id/nearyou/app/chat/ChatSendRateLimitTest.kt` (or wherever the chat module lives), tagged `database`. Back it with `InMemoryRateLimiter` (extracted in `like-rate-limit` task 7.1). Test fixtures (`SpyRateLimiterChat`, `FixedRemoteConfigChat`, `ThrowingRemoteConfigChat`, `NullRemoteConfigChat`) follow the like / reply rate-limit precedents with `Chat` suffix to avoid top-level-class redeclaration collisions across the three test files.
- [ ] 4.2 Implement all 30 scenarios from `specs/chat-conversations/spec.md` § "Integration test coverage — chat send rate limit" as Kotest `StringSpec` strings named "scenario 1 — …" through "scenario 30 — …". Run `./gradlew :backend:ktor:test --rerun-tasks "-Dkotest.filter.specs=*ChatSendRateLimitTest"` — expect 30/30 green.
- [ ] 4.3 Baseline preserved: chat-foundation route test class (and any other touched chat tests) green against the modified chat send service. The new rate-limit scenarios live in `ChatSendRateLimitTest`; chat-foundation tests are updated only to pass the new `rateLimiter` + `remoteConfig` constructor args (using `NoOpRateLimiter` + `StubRemoteConfig` test doubles).
- [ ] 4.4 Pre-push gauntlet: `./gradlew ktlintCheck detekt :backend:ktor:test --rerun-tasks "-Dkotest.filter.specs=*ChatSendRateLimitTest|*ChatFoundation*" :lint:detekt-rules:test :infra:redis:test :core:domain:test -PincludeMobile=false` — all green expected. (Targeted Kotest filter rather than the full `:backend:ktor:test` to keep local cycle time ≤2 min; full suite runs on CI.)

## 5. Pre-push + CI verification

- [ ] 5.1 `openspec validate chat-rate-limit --strict` — GREEN throughout (proposal phase + any review fixes; no spec edits in feat phase except in response to review).
- [ ] 5.2 `./gradlew ktlintCheck detekt -PincludeMobile=false` — GREEN. Both `RateLimitTtlRule` and `RedisHashTagRule` produce 0 findings on the new chat send call site (the `_day}` substring + `{scope:rate_chat_send_day}:{user:<uuid>}` shape align with the rules' patterns).
- [ ] 5.3 Local Kotest-filtered run: `ChatSendRateLimitTest` 30/30, chat-foundation route test class baseline preserved — all green.
- [ ] 5.4 CI green on the change branch HEAD: `lint` SUCCESS, `test` SUCCESS (30 new ChatSendRateLimitTest scenarios + chat-foundation baselines all passing against the CI Postgres + Redis service containers), `migrate-supabase-parity` SUCCESS.

## 6. Smoke test against staging (post-CI-green)

- [ ] 6.1 Create `dev/scripts/smoke-chat-send-rate-limit.sh` — adapted from the like / reply precedents (`smoke-like-rate-limit.sh` + `smoke-reply-rate-limit.sh`). Mint dev JWT via `mint-dev-jwt.sh`, create one conversation between two synthetic users via `POST /api/v1/conversations`, POST 51 chat messages with JSON body `{"content":"smoke chat send <timestamp> #N"}`, assert first 50 = 201 + 51st = 429 with `Retry-After` ≥ 60s + `error.code = "rate_limited"`. The ≥60s Retry-After lower-bound guards against fail-soft per task 6.4. Set executable bit; verify `bash -n` syntax check green.
- [ ] 6.2 Seed (or reuse) two synthetic users on staging: a Free sender + an unblocked recipient with no `user_blocks` row in either direction. The chat-foundation create-or-return endpoint will produce the conversation; reuse if one already exists from chat-foundation smoke testing.
- [ ] 6.3 Run smoke against `https://api-staging.nearyou.id` for synthetic Free user. Expect: 50 chat sends return 201, 51st returns 429 + `Retry-After` in expected WIB-staggered window (next WIB midnight + `hash(userId) % 3600` per-user offset → 0–24h+1h band) + `error.code = "rate_limited"`. **Limiter is genuinely firing — NOT fail-softing.** Record the staging deploy run URL + the actual Retry-After value in a comment update on the change PR.
- [ ] 6.4 Verify the limiter actually fired (not fail-soft): the smoke script's ≥60s lower-bound check on Retry-After must pass. The like-rate-limit-task-9.7 deployment-config issues (TLS scheme, secret slot value, eager-connect crash) all stayed fixed across the like and reply branch deploys — same `RedisRateLimiter` plumbing inherited from PR #37/#43/#44 should hold here.

## 7. Doc sync (in the archive commit, not the feat commit)

- [ ] 7.1 Update `docs/08-Roadmap-Risk.md` Phase 2 chat item: append "Chat send rate limit shipped" callout covering the 50/day Free cap, `premium_chat_send_cap_override` flag, the explicit "no burst" decision (canonical asymmetry per `02-Product.md:318`), the oversized-flag clamp at 10,000, and the no-`releaseMostRecent` design choice. Remove any "Chat rate limit (50/day Free) remains deferred" stale notes if present.
- [ ] 7.2 Extend `docs/05-Implementation.md` § Rate Limiting Implementation callout with the chat-send-cap entry pointing at `openspec/specs/chat-conversations/spec.md` § "Daily send-rate cap". Layer 1 / Layer 3 / Layer 4 still flagged as future work.
- [ ] 7.3 Confirm canonical "Free 50/day, Premium unlimited" line is unchanged at `02-Product.md:318` and `05-Implementation.md` Layer 2 table. No edits needed — what shipped should match what was canonical from the start.
- [ ] 7.4 Run `openspec archive chat-rate-limit --yes` — see archive commit for the spec-merge output. Per the one-PR-per-change convention, the archive commit lands on this same `chat-rate-limit` branch.
