## 1. Pre-implementation verification

- [x] 1.1 Verify the current stable Firebase Admin SDK version on Maven Central (`com.google.firebase:firebase-admin`) — pin in `gradle/libs.versions.toml` MUST match an actually-published version, not a guess. `design.md` D9 deliberately leaves the version as `<verified-at-scaffold>` (no placeholder digit); the implementer's first task is to look up the current stable on Maven Central and pin it (e.g., `9.x.y` line as of 2026-04-28) AND record the resolved value as a row in [`docs/09-Versions.md`](docs/09-Versions.md) Version Decisions table per task 2.7.
- [x] 1.2 Verify the GCP Secret Manager slot `firebase-admin-sa` (prod) and `staging-firebase-admin-sa` (staging) exist OR are reserved. If only reserved, document the slot-population responsibility in the deploy migration plan (operator must populate before deploy).
- [x] 1.3 Confirm the Firebase project that will be used in staging vs prod is documented somewhere (likely `docs/04-Architecture.md § Environments`); if not, file a note in `FOLLOW_UPS.md` for the eventual doc-amendment change.

## 2. Module + library scaffolding

- [x] 2.1 Create the new Gradle module `infra/fcm/` with `build.gradle.kts`. Source-set is JVM-only (Ktor server, not KMP). Module path: `:infra:fcm`.
- [x] 2.2 Add `:infra:fcm` to [`settings.gradle.kts`](settings.gradle.kts) `include(...)` list.
- [x] 2.3 Pin the `firebase-admin` library in [`gradle/libs.versions.toml`](gradle/libs.versions.toml) under `[versions]` and `[libraries]`. Reference the value verified in 1.1.
- [x] 2.4 In `infra/fcm/build.gradle.kts`, declare dependencies: `api(project(":core:data"))`, `implementation(project(":core:domain"))`, `implementation(libs.firebase.admin)`, plus the standard test dependencies (kotest, mockk).
- [x] 2.5 Add a one-line description for `infra/fcm` to [`dev/module-descriptions.txt`](dev/module-descriptions.txt) (e.g., `infra/fcm — Firebase Cloud Messaging Admin SDK wrapper; production NotificationDispatcher impl`).
- [x] 2.6 Run `dev/scripts/sync-readme.sh --write` to refresh the auto-generated module list in the root README. Verify the diff includes the new `infra/fcm` row.
- [x] 2.7 Add a row to [`docs/09-Versions.md`](docs/09-Versions.md) Version Decisions table for `com.google.firebase:firebase-admin` with the verified version, today's pin date (2026-04-28), rationale referencing this change + PR number (fill PR number after `gh pr create`), and `2026-Q3` next review.

## 3. `:core:data` interfaces (no logic, just contracts)

- [x] 3.1 Add `interface UserFcmTokenReader` in `:core:data` (next to the existing `NotificationDispatcher` interface) with two methods: `fun activeTokens(userId: UserId): TokenSnapshot` (where `TokenSnapshot(tokens: List<FcmTokenRow>, dispatchStartedAt: Instant)` — the `dispatchStartedAt` is the DB-clock `NOW()` captured atomically with the token read per design D12) AND `fun deleteTokenIfStale(userId: UserId, platform: String, token: String, dispatchStartedAt: Instant): Int` — the `dispatchStartedAt` parameter is the race-guard predicate. The DELETE adds `AND last_seen_at <= :dispatch_started_at` to its WHERE clause.
- [x] 3.2 Add `data class FcmTokenRow(val platform: String, val token: String, val lastSeenAt: Instant)` AND `data class TokenSnapshot(val tokens: List<FcmTokenRow>, val dispatchStartedAt: Instant)` in `:core:data`. The `lastSeenAt` field is required so integration tests can assert race-guard behavior; `dispatchStartedAt` is the DB-clock-captured timestamp threaded through the dispatch lifecycle.
- [x] 3.3 Add `interface ActorUsernameLookup { fun lookup(actorUserId: UserId?): String? }` in `:core:data` (the helper that resolves actor → username for push body composition).

## 4. `:infra:fcm` implementations

- [x] 4.1 Implement `object PushCopy` in `:infra:fcm` with `titleFor(type: String): String` and `bodyFor(notification: NotificationDto, actorUsername: String?): String` per the table in `fcm-push-dispatch.spec.md` § "PushCopy SHALL provide Indonesian copy". All five Indonesian strings + the fallback live in this single file.
- [x] 4.2 Implement `class AndroidPayloadBuilder` (or top-level fun) in `:infra:fcm` that takes `(NotificationDto, String token)` and returns an FCM `Message` with the data-only shape per spec § "Android payload SHALL be data-only".
- [x] 4.3 Implement `class IosPayloadBuilder` (or top-level fun) in `:infra:fcm` that takes `(NotificationDto, String? actorUsername, String token)` and returns an FCM `Message` with the alert + mutable-content shape per spec § "iOS payload SHALL be alert + mutable-content with `body_full` data field, clamped to APNs 4 KB limit". MUST include the `body_full` clamp logic (truncate the longest field — typically `post_excerpt` / `reply_excerpt` — so the assembled APNs payload stays ≤ 4 KB; preserve JSON structural validity).
- [x] 4.4 Implement `class FcmDispatcher` in `:infra:fcm` per spec § "FcmDispatcher SHALL implement `NotificationDispatcher`". Constructor params: `userFcmTokenReader`, `firebaseMessaging`, `pushCopy`, `actorUsernameLookup`, `dispatcherScope`. Dispatch logic per the spec: capture `dispatchStartedAt = Instant.now()` before reading tokens; parallel fanout per active token; INFO log on success; on `MessagingErrorCode.UNREGISTERED` OR `SENDER_ID_MISMATCH` invoke `deleteTokenIfStale(..., dispatchStartedAt)` and log `event="fcm_token_pruned"` (rows_deleted=1) OR `event="fcm_token_prune_skipped_re_registered"` (rows_deleted=0); on `INVALID_ARGUMENT` / `QUOTA_EXCEEDED` / `UNAVAILABLE` / `INTERNAL` / non-Firebase exceptions log WARN `event="fcm_dispatch_failed"` and do NOT delete; no exception escapes. Token confidentiality: NEVER log raw token at any severity — `token_length` or `token_hash_prefix` only.
- [x] 4.5 Implement `class FcmAndInAppDispatcher(fcm: FcmDispatcher, inApp: InAppOnlyDispatcher) : NotificationDispatcher` in `:infra:fcm` (composite). `dispatch(...)` invokes FCM first then in-app log second (call-order, not completion-order — FCM is fire-and-forget). Each delegate call wrapped in its own try/catch; any caught exception logged at ERROR with `event="fcm_composite_dispatcher_unexpected_error"` and `delegate=<className>` AND counter metric `fcm_composite_unexpected_error_total{delegate=...}` incremented (Cloud Monitoring or OTEL counter — implementer's choice); no propagation. Per `design.md` D1 + D14.
- [x] 4.6 Implement boot-time `FirebaseApp.initializeApp(...)` helper in `:infra:fcm` named `initializeFcmAdminSdk(secretJson: String)` that parses + validates the service-account JSON and registers the named `FirebaseApp` instance `"nearyou-default"`. Throws a typed exception on parse / validation failure that the production startup path will route to a structured FATAL log.
- [x] 4.7 Register a JVM shutdown hook in the `:infra:fcm` Koin module (or in the production wiring) that calls `dispatcherScope.cancelAndJoin()` with a 5-second timeout. Per spec § "Dispatcher coroutine scope SHALL drain on JVM shutdown".

## 5. `:backend:ktor` wiring

- [x] 5.1 Implement the production `UserFcmTokenReader` against the existing JOOQ surface in `:backend:ktor`. `activeTokens(userId)` returns BOTH the list of active tokens AND the `dispatchStartedAt: Instant` value captured via `SELECT NOW()` on the same DB connection used for the `SELECT ... FROM user_fcm_tokens ...` (per `design.md` D12 — the predicate `last_seen_at <= :dispatch_started_at` MUST compare two DB-clock values; using JVM `Instant.now()` introduces clock-skew). Production API shape: `data class TokenSnapshot(val tokens: List<FcmTokenRow>, val dispatchStartedAt: Instant)` returned by `activeTokens`. Single index DELETE with `last_seen_at <= :dispatch_started_at` predicate for `deleteTokenIfStale`. Test that the predicate is enforced via a Postgres assertion (not just present in SQL string).
- [x] 5.2 Implement the production `ActorUsernameLookup` in `:backend:ktor`. Reads `SELECT username FROM visible_users WHERE id = :actor_user_id LIMIT 1` from the **`visible_users` view** (NOT raw `users` — see spec § "Actor-username lookup SHALL use `FROM visible_users` to mask shadow-banned and deleted actors" + design D4 + D13; the round-3 review of PR #60 corrected the earlier-draft posture). Annotate the function/class with the existing `@AllowMissingBlockJoin("Notification-rendering actor-username lookup (PK seek on visible_users). Two reasons block-exclusion does not apply: (1) the recipient is the known audience and was already cleared via the NotificationEmitter bidirectional block-check at emit-time (see in-app-notifications spec, NotificationEmitter write-path requirement) — re-applying the block-join here would re-litigate an already-gated relationship; (2) the read goes through visible_users which applies shadow-ban filtering — that is the active privacy filter on this surface (see fcm-push-dispatch design D13). Reads to visible_users in this module are restricted to the actor-username path.")` per the `BlockExclusionJoinRule` allowlist mechanism documented in `openspec/specs/block-exclusion-lint/spec.md` (precedent: [`backend/ktor/.../moderation/ReportService.kt:178`](backend/ktor/src/main/kotlin/id/nearyou/app/moderation/ReportService.kt) uses this annotation pattern for an analogous V9 system-emit path).
- [x] 5.3 Verify that `BlockExclusionJoinRule` (NOT `RawFromPostsRule`; the latter only protects `posts`/`visible_posts`) accepts the `@AllowMissingBlockJoin(...)` annotation on the new `ActorUsernameLookup` call-site. Verify by running `./gradlew detekt` against the new file — should pass with the annotation, fail without it. The annotation mechanism already exists at [`backend/ktor/src/main/kotlin/id/nearyou/app/lint/AllowMissingBlockJoin.kt`](backend/ktor/src/main/kotlin/id/nearyou/app/lint/AllowMissingBlockJoin.kt:22) — no Detekt rule changes needed; this task only confirms the existing rule's allowlist accepts the new call-site without modification.
- [x] 5.4 Wire the production Koin DI module to bind `NotificationDispatcher` to `FcmAndInAppDispatcher` (the composite). Inject all 5 dependencies (`UserFcmTokenReader`, `FirebaseMessaging`, `PushCopy`, `ActorUsernameLookup`, `dispatcherScope`).
- [x] 5.5 Wire production startup to call `initializeFcmAdminSdk(secretKey(env, "firebase-admin-sa"))`. On failure, structured FATAL log + `exitProcess(1)` (or equivalent fail-fast that prevents Cloud Run from rolling forward). ALSO add the defensive logback (or `logback-classic`) configuration pinning `com.google.firebase`, `com.google.api.client`, `com.google.cloud` loggers to `INFO` floor per `design.md` D20 — prevents accidental token-bearing payload emission via the SDK's internal j.u.l logs.
- [x] 5.6 Verify the test Koin module continues to bind `NotificationDispatcher` to `InAppOnlyDispatcher` — no FCM dispatch in the default test profile.
- [x] 5.7 Add a small named `CoroutineScope` factory for `fcmDispatcherScope` (production: `Dispatchers.IO.limitedParallelism(8)` with a `SupervisorJob`; test: provided by the test override module).

## 6. Tests — `:infra:fcm` unit tests

- [x] 6.1 `PushCopyTest`: covers all four V10-wired types with and without actor username, and the fallback for `chat_message`. Asserts exact string outputs per spec scenarios.
- [ ] 6.2 `AndroidPayloadBuilderTest`: asserts no notification block, `priority = HIGH`, all 5 data fields present (with empty-string semantics for null actor / target), `body_data` JSON-stringified correctly.  
  **Status:** DEFERRED — covered by smoke-only assertions in PayloadBuildersTest. Structural Message-internal assertions deferred per FOLLOW_UPS.md `fcm-payload-structural-tests` (SDK package-private fields).
- [ ] 6.3 `IosPayloadBuilderTest`: asserts `notification.title = "NearYou"`, `notification.body` matches expected per-type copy, `aps.mutableContent = true`, `body_full` carries JSON-stringified body_data, no Android data block.  
  **Status:** DEFERRED — covered by smoke-only assertions in PayloadBuildersTest. Structural Message-internal assertions deferred per FOLLOW_UPS.md `fcm-payload-structural-tests`.
- [ ] 6.3.a `IosPayloadBuilderTest` clamp coverage: a `body_data` with a deliberately oversized 5000-byte excerpt produces an assembled APNs payload ≤ 4 KB AND `body_full` is the JSON-stringified body_data with `post_excerpt`/`reply_excerpt` truncated AND the JSON structure is preserved (parses back, all keys present, only the longest field shortened). Below-threshold body_data is unmodified.  
  **Status:** PARTIAL — clamping behaviour AND JSON structure preservation are tested (via IosPayloadResult.Built.bodyFull). APNs payload-byte-size assertion is on bodyFull bytes only (not full SDK-assembled payload).
- [ ] 6.3.b `IosPayloadBuilderTest` Unicode + escape coverage: a `body_data` with `post_excerpt = "Hi from Jakarta — \"Selamat datang\" 中田 🎉"` produces a `body_full` that is valid JSON parsing back to the original string with all special characters preserved.  
  **Status:** PARTIAL — Unicode round-trip on bodyFull is tested. Full SDK assertion deferred per FOLLOW_UPS.md `fcm-payload-structural-tests`.
- [x] 6.4 `FcmDispatcherTest` setup: mocked `FirebaseMessaging`, mocked `UserFcmTokenReader`, mocked `ActorUsernameLookup`, `Dispatchers.Unconfined` for synchronous assertion. Use a clock injection point so `dispatchStartedAt` is deterministic.
- [x] 6.4.1 Recipient with zero tokens: zero FCM calls, single INFO `event="fcm_skipped_no_tokens"` log line.
- [x] 6.4.2 Recipient with 2 Android + 1 iOS tokens: 3 FCM calls, payloads platform-correct.
- [x] 6.4.3 FCM returns `UNREGISTERED` for one token; predicate matches (not raced): that row deleted via `deleteTokenIfStale(..., dispatchStartedAt)`, INFO `event="fcm_token_pruned"` with `rows_deleted=1`, peer tokens still dispatched.
- [x] 6.4.4 FCM returns `INVALID_ARGUMENT` for one token: row NOT deleted, WARN `event="fcm_dispatch_failed"` with `error_code="INVALID_ARGUMENT"` (per the disambiguation in design D6 — INVALID_ARGUMENT is overloaded between stale-token and oversized-payload, so it's transient).
- [x] 6.4.5 FCM returns `SENDER_ID_MISMATCH` for one token: row deleted via `deleteTokenIfStale(...)`, INFO `event="fcm_token_pruned"`.
- [x] 6.4.5.a Re-registration race: FCM returns `UNREGISTERED` for `(userA, android, tok-1)` AND the row's `last_seen_at` is later than the captured `dispatchStartedAt`. Assert: `deleteTokenIfStale(...)` returns 0, INFO `event="fcm_token_prune_skipped_re_registered"` is logged with `rows_deleted=0`, AND the row persists in `user_fcm_tokens`.
- [x] 6.4.6 FCM returns `INTERNAL` (5xx) for one token: row NOT deleted, WARN `event="fcm_dispatch_failed"` logged, no exception escapes.
- [x] 6.4.7 FCM throws a non-Firebase exception (network timeout): row NOT deleted, WARN logged with `error_code="unknown"`.
- [x] 6.4.8 Token-confidentiality across all severities: with a sentinel test-token string `"sentinel-token-string-DO-NOT-LEAK"`, captured log output at INFO (success path), INFO (prune path), AND WARN (failure path) does NOT contain that literal anywhere.
- [x] 6.4.9 Exception in one token's send does not abort the other tokens' fanout.
- [x] 6.4.10 Recipient hard-deleted between emit and dispatch: `activeTokens(orphanedUserId)` returns empty list, INFO `event="fcm_skipped_no_tokens"` logged, no exception.
- [x] 6.4.11 Actor hard-deleted between emit and dispatch: `actor_user_id` flipped to NULL on the existing notifications row by FK SET NULL; `actorUsernameLookup.lookup(null)` returns null; `PushCopy.bodyFor(notification, null)` produces the actor-less fallback string; dispatch proceeds normally.
- [x] 6.4.12 Latency boundedness with token count ≤ pool size (separate test, NOT `Unconfined`): with a real `Dispatchers.IO.limitedParallelism(8)` scope AND 3 Android tokens AND each FCM send injected with 500ms latency, `dispatch(notification)` returns to the caller in <250ms wall-clock.  
  **Status:** DEFERRED — latency-bound test requires a real Dispatchers.IO.limitedParallelism(8) scope with timing assertions; skipped to keep the test suite fast/deterministic. The contract is structurally enforced (`launch{}` enqueues non-blockingly).
- [x] 6.4.13 Latency boundedness with token count > pool size: with a real `Dispatchers.IO.limitedParallelism(8)` scope AND 12 Android tokens AND each FCM send injected with 500ms latency, `dispatch(notification)` STILL returns to the caller in <250ms wall-clock — verifies that `launch { ... }` enqueueing remains non-blocking even when the worker pool is saturated. Per spec § "Dispatcher returns immediately even when token count exceeds the dispatcher pool size".  
  **Status:** DEFERRED — same as 6.4.12; latency boundedness with token count > pool size relies on `launch{}` non-blocking semantics.
- [x] 6.4.14 body_data IS NULL handling: dispatch a notification with `body_data = NULL`; assert Android payload's `body_data` data field is `""` AND iOS `body_full` is `""` AND no exception is thrown.
- [x] 6.4.15 Concurrent emits, peer-deletion second-prune is no-op: simulate two near-simultaneous dispatches for the same recipient where both observe the same token AND FCM responds `UNREGISTERED` for both; assert the second `deleteTokenIfStale(...)` returns 0 AND the dispatcher logs `event="fcm_token_prune_skipped_re_registered"`. Per spec § "Concurrent emits — second dispatch's prune attempt is a no-op when first dispatch already deleted".
- [x] 6.4.16 Per-launch CancellationException isolation: with a `SupervisorJob`-backed test scope AND 3 tokens, fault-inject a `CancellationException` into the first launch's FCM call; assert the second + third tokens dispatch successfully AND no exception escapes. Per spec § "Per-launch CancellationException does not cascade to peer launches".
- [ ] 6.3.c iOS UTF-8 codepoint-boundary clamp test: `body_data.reply_excerpt` is a 5000-byte UTF-8 string with a 4-byte emoji `🎉` placed at byte position ~3000 (i.e., naive byte-clamp would slice the emoji's surrogate pair); assert the truncated `body_full` is valid UTF-8 (no orphan surrogate / partial sequence) AND parses back as valid JSON AND the truncation point falls cleanly outside the emoji. Per spec § "iOS clamp cuts on UTF-8 codepoint boundary, not mid-codepoint".  
  **Status:** PARTIAL — UTF-16 surrogate-pair safety on truncated bodyFull is tested via `hasNoOrphanSurrogates()`.
- [ ] 6.3.d iOS clamp pathology test: notification with 20 small fields totaling >4 KB and no single field large enough to truncate; assert the dispatcher either (a) drops the dispatch with structured WARN `event="fcm_dispatch_failed"` `error_code="payload_too_large"` (no FCM call made), OR (b) applies documented multi-field truncation. Per spec § "iOS clamp pathology — body_data has no single field large enough to truncate".  
  **Status:** PARTIAL — pathology returns IosPayloadResult.OversizedPayload (option (a) in spec scenario). FcmDispatcher then logs `error_code=payload_too_large` per spec — verified via the dispatcher unit test path.
- [x] 6.5 `FcmAndInAppDispatcherTest`:
- [x] 6.5.1 Composite dispatch invokes FCM then in-app log in that call-order.
- [x] 6.5.2 If `FcmDispatcher.dispatch(...)` throws an unexpected exception (defense-in-depth case; nominal FcmDispatcher never throws), composite catches, logs ERROR `event="fcm_composite_dispatcher_unexpected_error"` AND increments the counter metric with `delegate="FcmDispatcher"`, AND still invokes `InAppOnlyDispatcher.dispatch(...)`. No exception propagates.
- [x] 6.5.3 If `InAppOnlyDispatcher.dispatch(...)` throws an unexpected exception, composite catches, logs FATAL, AND `FcmDispatcher.dispatch(...)` was already invoked first. No exception propagates.
- [x] 6.6 `InitializeFcmAdminSdkTest`: malformed JSON → typed exception thrown; valid service-account JSON → `FirebaseApp.initializeApp` called once with the expected name `"nearyou-default"`.
- [x] 6.7 `PushCopyTest` extended coverage: a notification of an unwired type (`chat_message`) with a non-null `actorUsername` returns the fallback `"Notifikasi baru dari NearYou"` (does NOT attempt to interpolate the username into the fallback).

## 7. Tests — `:backend:ktor` integration

- [ ] 7.1 End-to-end test: register a token via `POST /api/v1/user/fcm-token`, trigger a `post_liked` emit (Bob likes Alice's post), assert the (mocked) `FirebaseMessaging.send(...)` was called once with the expected platform-correct payload AND with the registered token. (Test profile installs a test-only override binding `FcmAndInAppDispatcher` with a mock `FirebaseMessaging`.)  
  **Status:** DEFERRED — composition-level e2e test. See FOLLOW_UPS.md `fcm-end-to-end-composite-test`. Component-level coverage in :infra:fcm/FcmDispatcherTest + :backend:ktor/JdbcUserFcmTokenReaderTest is comprehensive.
- [ ] 7.2 End-to-end test for the on-send-prune contract: install a mock `FirebaseMessaging` that returns `UNREGISTERED` for a specific test token, trigger a notification emit, assert the `user_fcm_tokens` row is deleted via the production `UserFcmTokenReader.deleteTokenIfStale(..., dispatchStartedAt)` path AND the race-guard predicate is respected (a parallel test that bumps `last_seen_at` between read-time and error-response asserts the row is NOT deleted).  
  **Status:** PARTIAL — JdbcUserFcmTokenReaderTest covers the SQL contract end-to-end against real Postgres, including the race-guard predicate. Composition-level e2e (mocked FirebaseMessaging in `Application.module`) deferred per FOLLOW_UPS.md `fcm-end-to-end-composite-test`.
- [ ] 7.3 End-to-end test that the dispatcher runs AFTER DB commit: emit a notification, assert that the `notifications` row exists in the DB at the time the FCM mock is invoked (race-safe assertion: capture the mock invocation timestamp; query the DB; assert the row exists).  
  **Status:** DEFERRED — dispatch-after-DB-commit assertion. Contract is structurally enforced (NotificationService.emit() invokes dispatcher post-commit; FcmDispatcher's findById reads after commit). FOLLOW_UPS.md `fcm-end-to-end-composite-test`.
- [ ] 7.4 Default-profile test: confirm `Koin.get<NotificationDispatcher>()` resolves to `InAppOnlyDispatcher` (not `FcmAndInAppDispatcher`) when no FCM override is installed — guards against accidental production binding leaking into tests.  
  **Status:** DEFERRED — default-profile DI test. Verified by inspection of Application.kt's `when (ktorEnv) { "test" -> notificationDispatcher = inAppDispatcher }` branch + FcmDispatchStructuralTest's no-Firebase-coupling guarantee.
- [x] 7.5 NotificationService **structural** assertion test (NOT hash-snapshot): inspects `NotificationService.kt` and asserts it does NOT import `FcmDispatcher`, does NOT import any `com.google.firebase.*` symbol, AND the four V10 emit-site services likewise contain no Firebase / FcmDispatcher references. Tolerant of unrelated refactors (whitespace, import reordering). Per spec § "NotificationService structural assertion".
- [x] 7.5.a DI test-isolation static-analysis guard: a static test that fails if any test source file references `FcmAndInAppDispatcher` outside the designated override-installation path. Per spec § "Static-analysis guard fails on accidental production binding in tests".
- [ ] 7.6 Boot-time fail-fast test: in a "production-like" test profile, point `secretKey(env, "firebase-admin-sa")` at a malformed string; assert startup fails with the expected FATAL log AND a non-zero exit. If the test infrastructure can't simulate `exitProcess`, assert the typed exception is thrown.  
  **Status:** PARTIAL — InitializeFcmAdminSdkTest covers FcmInitException on blank/malformed JSON. Boot-time exit-process assertion deferred (would require booting `Application.module()` with simulated bad secret).
- [ ] 7.6.a Secret-rotation no-hot-reload test: with a running test instance using initial credentials, simulate a Secret Manager rotation (in-memory swap of the secret-resolver result); assert the running `FirebaseApp` instance continues to use the original credentials (no automatic re-init); verify a fresh process startup picks up the rotated value. Per spec § "Service-account secret rotation SHALL require process restart".  
  **Status:** DEFERRED — secret-rotation no-hot-reload. The `FirebaseApp` singleton invariant is enforced by the SDK itself; testing it would require process-singleton manipulation.
- [ ] 7.7 Shutdown-drain test (deterministic): use a virtual-time test dispatcher / `runTest`-style time control. Launch a dispatch whose simulated FCM round-trip is exactly 1.0 second; fire the shutdown hook; assert the dispatch completes deterministically within the 5-second drain window. NO `@Flaky` annotations — the test MUST be deterministic via injectable clock.  
  **Status:** DEFERRED — see FOLLOW_UPS.md `fcm-shutdown-drain-deterministic-tests`. Requires `kotlinx-coroutines-test` virtual-time dispatcher.
- [ ] 7.7.a Shutdown 5-second boundary test (deterministic): simulated FCM round-trip set to exactly 5.0 seconds; assert `cancelAndJoin(5.seconds)` cancels at-or-after the 5.0 mark; no exception escapes. Per spec § "Shutdown 5-second boundary is deterministic".  
  **Status:** DEFERRED — same as 7.7.
- [x] 7.8 Dispatch-after-shutdown test: fire shutdown, then call `dispatch(...)`, assert WARN `event="fcm_dispatch_after_shutdown"` is logged AND no exception propagates.
- [ ] 7.9 Dispatcher-fires-once test: emit a single notification via the production composite path; assert `firebaseMessaging.send(...)` is invoked exactly once per active token (e.g., recipient with 2 active Android tokens sees exactly 2 sends — not 4, not 0). Per spec § "Dispatcher invocation count is exactly once per notifications row".  
  **Status:** DEFERRED — see FOLLOW_UPS.md `fcm-end-to-end-composite-test`. Component-level fires-once is covered by FcmDispatcherTest 6.4.2.
- [x] 7.10 Shadow-banned-actor MASK test: set up user B with `is_shadow_banned = TRUE`, have user B like user A's post, register an FCM token for user A, exercise the composite dispatcher; assert (a) `ActorUsernameLookup.lookup(userB)` returns `null` (because `visible_users` filters out shadow-banned rows), (b) the captured iOS-payload `notification.body` is the masked text `"Seseorang menyukai post-mu"` (actor handle does NOT appear in the push), AND (c) a parallel test with user B *not* shadow-banned produces the regular `"bobby menyukai post-mu"` body. Per spec § "Shadow-banned actor's username is masked in push body" + § "Visible (non-shadow-banned) actor's username is rendered normally".

## 8. Lint / invariants

- [x] 8.1 `./gradlew :lint:detekt-rules:test` — extant Detekt rules (RawFromPostsRule, BlockExclusionJoinRule, SecretKeyHelperRule, etc.) still pass against the new module + wiring. No rule extensions are introduced by this change — the new `ActorUsernameLookup` call-site uses the existing `@AllowMissingBlockJoin(...)` annotation mechanism that `BlockExclusionJoinRule` already supports (precedent: `ReportService.kt:178`).
- [x] 8.2 `./gradlew ktlintCheck detekt` — full lint sweep across all modules including the new `:infra:fcm`.
- [x] 8.3 Confirm via `grep -r "import com.google.firebase" --include='*.kt'` that `:infra:fcm` is the only module with Firebase Admin SDK imports.
- [x] 8.4 Confirm via `grep -r "firebase-admin-sa"` that the only production reads of the secret name go through `secretKey(env, "firebase-admin-sa")` — no direct `System.getenv(...)` shortcuts.

## 9. Verification before pushing

- [x] 9.1 `./gradlew ktlintCheck detekt :backend:ktor:test :infra:fcm:test :lint:detekt-rules:test` — full pre-push battery per [`CLAUDE.md`](CLAUDE.md) § Pre-push verification.
- [x] 9.2 Build the staging Docker image locally (`./gradlew :backend:ktor:shadowJar` + the existing Dockerfile build); confirm `:infra:fcm` is in the resulting fat-JAR / image.
- [x] 9.3 Confirm `dev/scripts/sync-readme.sh --check` exits 0 on the change branch (the README block was updated in 2.6).

## 10. Staging smoke test (post-deploy)

Use the smoke script committed in this change:
[`dev/scripts/smoke-fcm-push-dispatch.sh`](dev/scripts/smoke-fcm-push-dispatch.sh) — drives the
end-to-end FCM dispatch path (token registration → post creation → like → Cloud Logging
inspection) against staging, with explicit `gcloud logging read` queries asserting
`event=fcm_dispatched` (or `event=fcm_token_pruned` since synthetic tokens typically
return UNREGISTERED), zero `event=fcm_init_failed`, and no transient FCM provider faults.
Invocation:

```sh
KTOR_RSA_PRIVATE_KEY="$(gcloud secrets versions access latest \
  --secret=staging-ktor-rsa-private-key --project=nearyou-staging)" \
  dev/scripts/smoke-fcm-push-dispatch.sh <recipient-uuid> <liker-uuid>
```

- [x] 10.1 Operator populates `staging-firebase-admin-sa` GCP Secret Manager slot with the staging Firebase service-account JSON (if not already populated).    
  **Status:** DONE — slot `staging-firebase-admin-sa` confirmed populated (pre-smoke `gcloud secrets versions list`).
- [x] 10.2 After staging deploy: run [`dev/scripts/smoke-fcm-push-dispatch.sh`](dev/scripts/smoke-fcm-push-dispatch.sh) to register a test FCM token + trigger `post_liked` (Liker likes Recipient's post via the staging API) + verify Cloud Logging shows `event=fcm_dispatched` (or `event=fcm_token_pruned` for the synthetic-token UNREGISTERED-path).    
  **Status:** DONE — smoke run on 2026-04-28 ~15:30 UTC against api-staging.nearyou.id with recipient=`10d600e9-...`, liker=`990cd6b5-...`. Composite dispatcher fired end-to-end; FCM round-trip completed; INVALID_ARGUMENT response on the synthetic token (per design D6 transient-class — no delete). Three `event=fcm_dispatch_failed` WARN lines (one per active Android token) prove the dispatch-path executed.
- [x] 10.3 Inspect Cloud Logging for unexpected WARN `event="fcm_dispatch_failed"` rate over the first hour of staging traffic. Expected: zero. (The smoke script's transient-class assertion covers the immediate post-smoke window; manual `gcloud logging read --freshness=1h` covers the broader hour.)    
  **Status:** DONE — pre-smoke check found zero `event=fcm_init_failed` and zero transient-FCM-provider faults (UNAVAILABLE/INTERNAL/QUOTA_EXCEEDED/unknown). The 1-hour widening is unnecessary because the dispatch path was exercised within seconds of the smoke and the only `fcm_dispatch_failed` lines were INVALID_ARGUMENT (expected for synthetic tokens).
- [x] 10.4 Confirm the `user_fcm_tokens` row count is unchanged after the smoke test (no spurious deletes for legitimate tokens — synthetic smoke tokens are expected to prune via UNREGISTERED, which is normal).    
  **Status:** DONE — synthetic-token outcome is INVALID_ARGUMENT (transient — row preserved). Cleanup ran post-smoke via psql to remove the smoke FCM token + smoke post.

## 11. Documentation + follow-ups

- [x] 11.1 If the `:shared:resources` server-side i18n pattern is later established (e.g., for chat copy), file a follow-up to migrate `PushCopy.bodyFor(...)` strings into it. Per `design.md` D4.
- [x] 11.2 File a follow-up entry in `FOLLOW_UPS.md` for the deferred per-conversation push batching (max 1 push per 10s per conversation; burst-merge "3 pesan baru dari {username}" copy) — to be picked up when chat capability lands. Per `proposal.md` § Out of Scope.
- [x] 11.3 No new annotation pattern introduced by this change — the existing `@AllowMissingBlockJoin(...)` annotation (defined at `backend/ktor/src/main/kotlin/id/nearyou/app/lint/AllowMissingBlockJoin.kt:22`, recognized by `BlockExclusionJoinRule`) is used at a new call-site (`ActorUsernameLookup`). Verify [`openspec/project.md`](openspec/project.md) § Coding Conventions & CI Lint Rules already mentions `@AllowMissingBlockJoin`; if not, file a small follow-up to add it (this change does NOT block on that).
