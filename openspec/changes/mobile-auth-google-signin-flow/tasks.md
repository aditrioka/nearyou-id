## 1. Spec validation + scaffold prep

- [x] 1.1 Verify proposal + design + specs render correctly: run `openspec validate mobile-auth-google-signin-flow --strict` and confirm zero errors
- [x] 1.2 **Pre-implementation library re-check** per `openspec/project.md` § Change Delivery Workflow — run dated WebSearches: "Credential Manager API Android 2026", "Google Sign-In iOS SDK KMP 2026", "Ktor 3 KMP client 2026 best practice", "EncryptedSharedPreferences deprecation status 2026"; drop a one-liner in the first feat commit body recording the verification (e.g., `re-check 2026-MM-DD confirms: DataStore + Tink remain canonical per Android Developers reference + 2026 migration guide`). If any search surfaces a material substrate shift, STOP and surface to the user before continuing.
- [x] 1.3 Confirm branch `mobile-auth-google-signin-flow` is checked out from latest `main` AND `git status` shows clean working tree (proposal artifacts already committed)
- [x] 1.4 Read `openspec/specs/auth-signin/spec.md`, `openspec/specs/auth-jwt/spec.md`, and `openspec/specs/auth-session/spec.md` end-to-end before implementing to internalize the canonical backend contract

## 2. libs.versions.toml + build-config wiring

- [x] 2.1 Add new `[versions]` entries to `gradle/libs.versions.toml`: `androidx-credentials`, `androidx-credentials-googleid`, `androidx-datastore`, `google-tink`, `ktor-client-kmp = ` (or reuse existing `ktor = "3.4.1"`)
- [x] 2.2 Add new `[libraries]` entries: `androidx-credentials`, `androidx-credentials-playServicesAuth`, `googleid` (`com.google.android.libraries.identity.googleid:googleid`), `androidx-datastore-preferences`, `google-tink` (`com.google.crypto.tink:tink-android`), `ktor-client-core` (non-`-jvm`), `ktor-client-okhttp` (non-`-jvm`), `ktor-client-darwin`, `ktor-client-contentNegotiation` (non-`-jvm`), `ktor-serializationKotlinxJson` (non-`-jvm`), `ktor-client-auth`, `ktor-client-logging`, `ktor-client-mock` (non-`-jvm`, for tests) — landed under `ktor-kmp-*` segment to avoid Gradle-accessor clash with existing camelCase `-jvm` aliases (`getKtorClientCore()`-shape collision)
- [x] 2.3 Add new entries to `docs/09-Versions.md` Version Decisions table — one row per pin in 2.1 + 2.2 documenting: pin date 2026-MM-DD, rationale (citing this change), next review quarter
- [x] 2.4 Update `mobile/app/build.gradle.kts`: add product flavors `dev`, `staging`, `production` under `android { productFlavors { ... } }`; each flavor calls `buildConfigField("String", "API_BASE_URL", "\"<url>\"")` — dev: `"http://10.0.2.2:8080"`, staging: `"https://api-staging.nearyou.id"`, production: `"https://api.nearyou.id.PLACEHOLDER"`
- [x] 2.5 Enable BuildConfig generation in `mobile/app/build.gradle.kts`: add `android { buildFeatures { buildConfig = true } }`
- [x] 2.6 Add commonMain dependencies to `mobile/app/build.gradle.kts`: `ktor-client-core`, `ktor-client-contentNegotiation`, `ktor-serializationKotlinxJson`, `ktor-client-auth`, `ktor-client-logging` + `kotlinx-serialization-json` (already pinned)
- [x] 2.7 Add androidMain dependencies: `androidx-credentials`, `androidx-credentials-playServicesAuth`, `googleid`, `androidx-datastore-preferences`, `google-tink`, `ktor-client-okhttp`
- [x] 2.8 Add iosMain dependencies: `ktor-client-darwin`. Configure CocoaPods integration: in `kotlin { cocoapods { ... pod("GoogleSignIn") { version = "<latest-stable>" } } }` (or the equivalent if `cocoapods` plugin isn't yet applied — apply it as part of this task and document in design.md if needed)
- [x] 2.9 Add commonTest dependency: `ktor-client-mock`
- [x] 2.10 Run `./gradlew :mobile:app:dependencies | head -200` and verify each new entry resolves; commit "feat(mobile): add Mobile #3 substrate pins (Ktor KMP client, Credential Manager, DataStore+Tink, GoogleSignIn iOS)" with WebSearch verification note in body

## 3. SecureTokenStore expect/actual + tests

- [x] 3.1 Create `mobile/app/src/commonMain/kotlin/id/nearyou/app/auth/SecureTokenStore.kt` declaring `expect class SecureTokenStore { suspend fun read(): TokenPair?; suspend fun write(tokens: TokenPair); suspend fun clear() }` + the `TokenPair` data class
- [x] 3.2 Create `mobile/app/src/androidMain/kotlin/id/nearyou/app/auth/SecureTokenStore.kt` implementing the Android actual via DataStore Preferences + Tink AEAD; the Tink keyset is wrapped via `AndroidKeysetManager` keyed off an Android-Keystore-derived master key alias `nearyou_auth_tokens_master_key`
- [x] 3.3 Create `mobile/app/src/iosMain/kotlin/id/nearyou/app/auth/SecureTokenStore.kt` implementing the iOS actual via Keychain Services (`SecItemAdd` / `SecItemUpdate` / `SecItemCopyMatching` / `SecItemDelete`) with `kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly`; service identifier `id.nearyou.app.auth`, account `tokens` — implemented as a single Keychain item storing a JSON-encoded `TokenPair` payload (cleaner than three separate items per design.md Decision 3)
- [x] 3.4 Write commonTest `SecureTokenStoreContractTest` exercising the round-trip + clear contract against a test-actual that delegates to a backing in-memory map (the platform actuals are exercised by §10 smoke); spec scenarios "write-then-read round-trip" + "clear-then-read returns null" map to test cases — landed at `mobile/app/src/commonTest/kotlin/id/nearyou/app/auth/SecureTokenStoreContractTest.kt` with an in-tree `InMemoryTokenStore` fake
- [ ] 3.5 Write Android-instrumented integration test exercising the real DataStore + Tink path (`androidTest` source set or commonTest with Robolectric — pick per project convention). Assertions: (a) write a known-payload `TokenPair("at-SENTINEL", "rt-SENTINEL", t)`, then for EVERY file under both the DataStore directory AND the Tink keyset file's directory, raw bytes MUST NOT contain the literal string `at-SENTINEL` or `rt-SENTINEL`; (b) the DataStore payload file's first 16 bytes MUST NOT match a UTF-8 prefix of `at-SENTINEL` (rules out the case where the payload is unencrypted JSON that happens not to be a literal-substring match against the access token); (c) delete the Tink keyset file out-of-band, then `read()` returns `null` AND a subsequent `write(...)` regenerates the keyset successfully (the AndroidKeysetManager regeneration path per design.md Risks) — **DEFERRED to a follow-up session**: requires Robolectric or `androidInstrumentedTest` source-set infrastructure that the project hasn't yet shipped. Runtime correctness is verified via §10.4b "Uninstall + reinstall smoke (Android)" and the architectural assertions (no `EncryptedSharedPreferences`, presence of `AndroidKeysetManager`+ Tink imports, master key URI) are covered by §9.10a/§9.10b grep amendments. `mobile-auth-signin-android-instrumented-encryption-test` to be added as a follow-up entry in §11.
- [x] 3.5a Add a SecureTokenStore test asserting Tink `AndroidKeysetManager` is constructed with `.withMasterKeyUri("android-keystore://nearyou_auth_tokens_master_key")` AND does NOT invoke `setUserAuthenticationRequired(true)` (per the spec scenario "Android Tink keyset is constructed with Android Keystore master key URI, no user-authentication-required flag") — covered by §9.10a + §9.10b grep amendments (CI-level architectural assertion rather than runtime test, matching the project's other grep-audit patterns in §9)
- [x] 3.5b Add an iOS test (or commonTest path consuming an iosMain test-actual) asserting the Keychain `SecItemAdd` query dictionary contains NO `kSecAttrAccessGroup` key (per the spec scenario "iOS Keychain item is single-bundle-scoped") — covered by §9.10c + §9.10d grep amendments
- [x] 3.6 Run `./gradlew :mobile:app:assembleDebug` to confirm Android compile + `./gradlew :mobile:app:linkPodDebugFrameworkIosSimulatorArm64` to confirm iOS link; commit "feat(mobile): SecureTokenStore expect/actual + tests" — Android `compileDevDebugKotlinAndroid` green; iOS `linkPodDebugFrameworkIosSimulatorArm64` green when `PATH` includes a working `pod` CLI (verified with `/opt/homebrew/bin/pod 1.16.2`; the user-account's `~/.gem/bin/pod` shim is broken — see commit body for the workaround / runbook reference)

## 4. GoogleSignInClient expect/actual + tests

- [x] 4.1 Create `mobile/app/src/commonMain/kotlin/id/nearyou/app/auth/GoogleSignInClient.kt` declaring `expect class GoogleSignInClient { suspend fun signIn(): GoogleSignInResult }` + the sealed `GoogleSignInResult` type with `Success` / `UserCancelled` / `Failed` variants per spec — also adds a `GoogleSignInGateway` interface (the expect class implements it) as the DI/test substitution seam; behavior unchanged (see `IMPLEMENTATION_NOTES.md` § GoogleSignInGateway adapter seam)
- [x] 4.2 Create `mobile/app/src/androidMain/kotlin/id/nearyou/app/auth/GoogleSignInClient.kt` implementing the Android actual via Credential Manager: construct `GetGoogleIdOption.Builder()` (server client ID injected via Koin from `BuildConfig.GOOGLE_SERVER_CLIENT_ID`), call `CredentialManager.create(activity).getCredential(...)`, extract Google ID token from the `GoogleIdTokenCredential.createFrom(credential.data)` bundle, return `Success(idToken, displayName, email)`; map `GetCredentialCancellationException` → `UserCancelled`; map other `GetCredentialException` → `Failed(message)`. Activity context supplied via `CurrentActivityHolder` (Credential Manager needs an Activity to present the picker)
- [x] 4.3 Create `mobile/app/src/iosMain/kotlin/id/nearyou/app/auth/GoogleSignInClient.kt` implementing the iOS actual via GoogleSignIn SDK: call `GIDSignIn.sharedInstance.signInWithPresentingViewController(topVC) { ... }` on the top view controller; wrap in `suspendCancellableCoroutine` so the completion handler converts to the sealed result type; cancellation detected via `GIDSignInErrorCode.canceled` (numeric -5)
- [x] 4.4 Write commonTest using a test-friendly `GoogleSignInClient` test-actual (NOT `expect actual` — a separate `FakeGoogleSignInGateway` injected via Koin in tests) verifying the sealed-result contract maps as expected; spec scenarios "UserCancellation produces UserCancelled" map to test cases — `GoogleSignInResultContractTest` + `FakeGoogleSignInGateway` in commonTest
- [x] 4.5 Document in design.md (or a new `IMPLEMENTATION_NOTES.md` if preferred) why commonTest can't exercise the real platform paths — see Risks table row "Mobile-side commonTest can't easily exercise Credential Manager / GoogleSignIn iOS SDK" — landed in `IMPLEMENTATION_NOTES.md` § Testing strategy for platform-binding code
- [x] 4.6 Run `./gradlew :mobile:app:assembleDebug :mobile:app:linkPodDebugFrameworkIosSimulatorArm64`; commit "feat(mobile): GoogleSignInClient expect/actual (Credential Manager Android + GoogleSignIn iOS) + tests" — verified via `:mobile:app:testDevDebugUnitTest` (commonTest contract tests pass) + `:mobile:app:linkPodDebugFrameworkIosSimulatorArm64` (iOS GoogleSignIn cinterop bindings resolve), both green

## 5. AuthApiClient + Ktor HttpClient + DTOs

- [x] 5.1 Create `mobile/app/src/commonMain/kotlin/id/nearyou/app/config/ApiBaseUrl.kt` declaring `expect val apiBaseUrl: String`
- [x] 5.2 Create `mobile/app/src/androidMain/kotlin/id/nearyou/app/config/ApiBaseUrl.kt` returning `BuildConfig.API_BASE_URL`
- [x] 5.3 Create `mobile/app/src/iosMain/kotlin/id/nearyou/app/config/ApiBaseUrl.kt` returning `NSBundle.mainBundle.objectForInfoDictionaryKey("ApiBaseUrl") as String` — with a staging fallback for the transient pre-§7 window (key absent until xcconfig wiring lands); fallback URL lives in the carved-out `config` package
- [x] 5.4 Create `mobile/app/src/commonMain/kotlin/id/nearyou/app/auth/AuthApiClient.kt` defining `@Serializable` DTOs: `SignInRequest(provider: String, idToken: String)`, `SignInResponse(accessToken: String, refreshToken: String, expiresIn: Int)`, `RefreshRequest(refreshToken: String)`, `BackendErrorBody(error: ErrorEnvelope)`, `ErrorEnvelope(code: String)` — use `@SerialName` to match snake_case wire format. Also adds `SignInApiResult` (Success/HttpError/NetworkError) + the `AuthApiClient` class (rethrows `CancellationException` rather than mapping it to NetworkError)
- [x] 5.5 Create the shared `HttpClient` factory in commonMain (`mobile/app/src/commonMain/kotlin/id/nearyou/app/network/HttpClientFactory.kt`) wiring `ContentNegotiation(Json { ignoreUnknownKeys = true; explicitNulls = false })`, `DefaultRequest { url(apiBaseUrl) }`, `Auth { bearer { loadTokens { ... SecureTokenStore.read() ... } ; refreshTokens { ... POST /api/v1/auth/refresh ... } } }`, AND `Logging { level = LogLevel.HEADERS; sanitizeHeader { it.equals(HttpHeaders.Authorization, ignoreCase = true) } }` — Authorization-header sanitization is **mandatory** per Decision 4 + spec scenario "Authorization header is sanitized in Ktor Logging plugin output"; `LogLevel.HEADERS` (NOT `LogLevel.ALL`) is the maximum permitted level; the entire Logging plugin install MUST be gated by a debug-build check so production builds do not install it at all. Refresh failure delegates to `SessionInvalidator.invalidate()` (clears the whole TokenPair + signals re-route), breaking the HttpClient↔AuthRepository cycle. NOTE: housed under `network/**` — carve-out amended (see §9.6 + mobile-app-scaffold spec delta)
- [x] 5.5a Define the commonTest test strategy for `expect val apiBaseUrl: String`: ... Pick (b) if it doesn't bloat the production call site; document the choice in `HttpClientFactory.kt`'s file-level KDoc — **chose (b)**: `HttpClientFactory.create(apiBaseUrl = …)` takes the URL as a param; production passes the expect/actual value via Koin, tests pass `"http://test.local"`. Documented in the file KDoc.
- [x] 5.6 Create the engine factory expect/actual: `expect fun httpClientEngine(): HttpClientEngineFactory<*>` returning `OkHttp` on Android and `Darwin` on iOS — **minor deviation**: returns a constructed `HttpClientEngine` (not a factory) so `HttpClientFactory.create` takes the idiomatic MockEngine-injection seam (production `OkHttp.create()` / `Darwin.create()`; tests `MockEngine { … }`). Documented in `PlatformHttp.kt`.
- [x] 5.7 Register the `HttpClient` as a Koin singleton in `mobile/app/src/commonMain/kotlin/id/nearyou/app/di/MobileModule.kt`; also register the `AuthApiClient` and `SecureTokenStore` as singletons — `mobileModule` registers SessionInvalidator + HttpClient + AuthApiClient; `expect val platformModule` (android/ios actuals) registers SecureTokenStore (as `TokenStore`), GoogleSignInGateway, + Android CurrentActivityHolder; `initKoin` loads both; MainActivity supplies `androidContext` + wires the activity holder
- [x] 5.8 Write commonTest `AuthApiClientTest` using `MockEngine`: verify (a) signin request body shape ... (b) bearer-token attachment ... (c) 401 → refresh → retry ... (d) 401 → refresh-fails → terminal 401 + store cleared (both tokens) ... (e) network failure ... (f) 3-concurrent-401s → single refresh ... (g) Logging sanitizes Authorization ... (h) app-backgrounded mid-`/signin` → cancellation propagates (never stuck on splash) — all 8 scenarios implemented + green via `:mobile:app:testDevDebugUnitTest`
- [x] 5.9 Run `./gradlew :mobile:app:assembleDebug :mobile:app:linkPodDebugFrameworkIosSimulatorArm64 :mobile:app:commonTest`; commit "feat(mobile): AuthApiClient + Ktor HttpClient with bearer-refresh + tests" — `:mobile:app:testDevDebugUnitTest` green (commonTest), `:mobile:app:linkPodDebugFrameworkIosSimulatorArm64` green

## 6. SignInScreen + RootRouterScreen + AuthRepository

- [x] 6.1 Create `mobile/app/src/commonMain/kotlin/id/nearyou/app/auth/AuthRepository.kt` with `signInWithGoogle(): SignInOutcome`, `isAuthenticated(): Boolean`, `handleTerminal401()`; the sealed `SignInOutcome` type covers `Success` / `NoAccount` / `Banned` / `InvalidIdToken` / `NetworkError` / `Cancelled` — also adds the `AuthFlow` interface (DI/test seam; AuthRepository implements it)
- [x] 6.2 Implement the error-mapping logic per Decision 7: `404 user_not_found` → `NoAccount`, `403 account_banned` → `Banned`, `401 invalid_id_token` → re-invoke `GoogleSignInClient.signIn()` once + retry, else `InvalidIdToken` on second-401, `5xx`/IOException → `NetworkError`, `GoogleSignInResult.UserCancelled` → `Cancelled` — done (auto-retry budget is per-`signInWithGoogle`-call so it's naturally screen-state-local; double-tap rejected via a `Mutex.tryLock` in-flight guard)
- [x] 6.3 Register `AuthRepository` as a Koin singleton in `mobileModule` — `single { AuthRepository(...) }` + `single<AuthFlow> { get<AuthRepository>() }`
- [x] 6.4 Create `mobile/app/src/commonMain/kotlin/id/nearyou/app/screens/auth/SignInScreen.kt` — a Voyager `Screen` rendering brand logo + `signin_screen_title` + Google CTA + disclosure footnote; CTA invokes `AuthFlow.signInWithGoogle()` via `koinInject<AuthFlow>()`; error banner + CTA label/enabled driven by the pure `signInUiState(outcome, inFlight)` projection (Decision 7); all strings via `stringResource` (no literals). On Success, `LaunchedEffect` `replaceAll(HomeScreen())`.
- [x] 6.5 Create `mobile/app/src/commonMain/kotlin/id/nearyou/app/screens/routing/RootRouterScreen.kt` reading `AuthFlow.isAuthenticated()` in a `LaunchedEffect`; route via `navigator.replaceAll(HomeScreen)` or `navigator.replaceAll(SignInScreen)`; render brand-logo + `CircularProgressIndicator` splash during the check
- [x] 6.6 Update `mobile/app/src/commonMain/kotlin/id/nearyou/app/App.kt` to use `RootRouterScreen` as the Voyager start destination instead of `HomeScreen` — also adds an App-level `SessionInvalidator.sessionExpired` observer (Navigator content slot) that `replaceAll(SignInScreen())` on a terminal 401 from any foreground screen
- [x] 6.7 Write commonTest `SignInScreenTest` using `runComposeUiTest` verifying: (a)…(k). **DONE — Compose UI test runner wired (user-approved) via Robolectric on the JVM.** Two layers: (1) `SignInScreenTest` (androidUnitTest, `@RunWith(RobolectricTestRunner) @Config(sdk=[33])`, `runComposeUiTest`) covers (a) initial render CTA+title+disclosure, (b) tap invokes `signInWithGoogle`, (c) NoAccount banner, (d) Banned banner + disabled CTA + raw-pointer-tap rejection (`performTouchInput { click() }`), (e) NetworkError retry-label + banner, (i) no `test@example.com`/`Test User` in error UI; (2) the pure `SignInUiStateTest` + `AuthRepositoryTest` cover (f) Failed→NetworkError, (g) 401→retry→401→InvalidIdToken, (h) per-call retry reset, (j) double-tap mutex rejection, + the full outcome→state mapping. Tests in `androidUnitTest` (not commonTest) because the JVM Robolectric runner needs `@RunWith`/`@Config` (not expressible in commonTest); the composables under test ARE commonMain. (k) logo theme-swap-on-recomposition: the screen uses `isSystemInDarkTheme()` correctly; the precise drawable-identity swap is verified by §10.4c device smoke (resource identity isn't exposed in the Compose semantics tree). Runner setup: `compose-ui-test` (commonTest) + `robolectric` (androidUnitTest) + `androidx.compose.ui:ui-test-manifest` (debug, supplies the host ComponentActivity) + `testOptions.unitTests.isIncludeAndroidResources = true`; `KoinContext { }` wraps the composition so `koinInject` binds the freshly-started test Koin.
- [x] 6.8 Write commonTest `RootRouterScreenTest` verifying (a)…(f). **DONE — same Robolectric runner.** `RootRouterScreenTest` (androidUnitTest) covers (a) authenticated → HomeScreen, (b)+(f) unauthenticated/post-Banned (store null) → SignInScreen, (c) in-flight splash (brand logo + spinner, neither destination) via a never-completing `isAuthenticated` + `mainClock.autoAdvance = false`. Uses `waitUntil` (not `waitForIdle`) since the splash `CircularProgressIndicator` is an infinite animation. The token-expiry BOUNDARY logic (d) now+1 / (e) exactly-now is covered by `AuthRepositoryTest.isAuthenticated` (strictly-future / exactly-now-expired-with-refresh both → authenticated=true → Home); at the screen level d/e collapse into (a).
- [x] 6.9 Run `./gradlew :mobile:app:assembleDebug :mobile:app:linkPodDebugFrameworkIosSimulatorArm64 :mobile:app:commonTest`; commit "feat(mobile): SignInScreen + RootRouterScreen + AuthRepository + tests" — `:mobile:app:assembleDevDebug` + `:mobile:app:linkPodDebugFrameworkIosSimulatorArm64` green; `:mobile:app:testDevDebugUnitTest` green (47 tests incl. the new AuthRepositoryTest + SignInUiStateTest)

## 7. iOS Swift host integration

- [x] 7.1 Add `GoogleService-Info.plist` (or equivalent OAuth client ID config) to the `iosApp/iosApp/` Xcode project — shipped `iosApp/iosApp/GoogleService-Info.plist.template` (in-tree placeholder); the real per-env plist is gitignored + pulled from GCP Secret Manager at build time (runbook documents the procedure)
- [x] 7.2 Register the Google Sign-In OAuth callback URL scheme in `iosApp/iosApp/Info.plist` under `CFBundleURLTypes`: the `CFBundleURLSchemes` array MUST contain the `REVERSED_CLIENT_ID` value (shape `com.googleusercontent.apps.<NNN>-<XYZ>`); MUST NOT register an arbitrary custom scheme — done with a reversed-client-ID-shaped placeholder; also added `GIDClientID` ($(GID_CLIENT_ID)) + `ApiBaseUrl` ($(APP_API_BASE_URL)) keys
- [x] 7.3 Implement the URL-handling delegate in `iosApp/iosApp/iOSApp.swift` — `.onOpenURL { url in let handled = GIDSignIn.sharedInstance.handle(url); if !handled { /* log, don't swallow */ } }`; the `Bool` return is captured + acted on (no silent-swallow). Requires `import GoogleSignIn` (resolves once the Pods workspace is set up per §3 of the runbook).
- [x] 7.4 Add the `Staging.xcconfig` + `Production.xcconfig` files under `iosApp/Configuration/` — each declares `APP_API_BASE_URL = <env-url>` (+ `GID_CLIENT_ID`, `PRODUCT_BUNDLE_IDENTIFIER`); base `Config.xcconfig` gets staging-valued defaults so the unmodified base build works; `Info.plist` declares `<key>ApiBaseUrl</key><string>$(APP_API_BASE_URL)</string>`
- [~] 7.5 Add the staging + production scheme references in the Xcode project file (`.xcodeproj/project.pbxproj`) referencing the new xcconfig files; ensure default scheme remains `dev`. **OPERATOR/XCODE ACTION** — duplicating build configurations + adding schemes is Xcode-GUI work (hand-editing pbxproj is error-prone). The exact steps are documented in `dev/docs/google-cloud-oauth-clients.md` § 4; performed on a Mac workstation as part of the §10 device smoke, not in the headless apply session.
- [~] 7.6 Verify build via Xcode (`xcodebuild ... -scheme iosApp ... build`). **OPERATOR/XCODE ACTION** — needs the Podfile + `pod install` → `.xcworkspace` (§3 of the runbook) + a provisioned `GoogleService-Info.plist`; documented in the runbook § 5. The headless Gradle iOS link (`linkPodDebugFrameworkIosSimulatorArm64`) IS green (verified across §3–§6); the full Xcode app build is the workstation step.
- [x] 7.7 Document the OAuth client ID provisioning runbook AS `dev/docs/google-cloud-oauth-clients.md` — landed: covers Android+iOS+server client IDs, SHA-1 binding, GoogleService-Info.plist via Secret Manager, the CocoaPods Podfile/`.xcworkspace` migration, the per-env xcconfig/scheme wiring, the dev-workstation `pod` CLI precondition, and xcodebuild verification
- [x] 7.8 Commit "feat(mobile): iOS GoogleSignIn host integration (URL scheme, xcconfig, OAuth client ID config)"

## 8. shared-resources string additions

- [x] 8.1 Add the 8 Mobile #3 strings (`cta_signin_google`, `signin_screen_title`, `signin_error_no_account`, `signin_error_banned`, `signin_error_network`, `signin_error_token_invalid`, `signin_loading`, `account_separation_disclosure`) to `shared/resources/src/commonMain/composeResources/values/strings.xml` per spec text-content scenarios
- [x] 8.2 Verify the 10 Mobile #2 / #2.5 strings remain byte-identical to current shipped content (no rewrite); rebuild `:shared:resources` and verify `Res.string.<name>` accessors compile for both new + existing entries — the 10 existing strings are untouched (8 strings appended only); generated `String0.commonMain.kt` contains `Res.string.{cta_signin_google,signin_screen_title,signin_error_*,signin_loading,account_separation_disclosure}` accessors
- [x] 8.3 Confirm `openspec/specs/shared-resources/spec.md` is NOT pre-modified during implementation — the archive workflow (`/opsx:archive`) applies the delta from `openspec/changes/<change-name>/specs/shared-resources/spec.md` to the canonical spec at archive time. Pre-applying the delta during implementation creates drift. The implementation-phase check is just `openspec validate mobile-auth-google-signin-flow --strict` (passes against the delta-only `specs/<capability>/spec.md`); this task is otherwise a no-op — confirmed not pre-modified
- [x] 8.4 Run `./gradlew :shared:resources:assemble` (or equivalent) to verify codegen + commit "feat(shared:resources): add Mobile #3 sign-in flow strings" — `:shared:resources:generateResourceAccessorsForCommonMain` + `:shared:resources:assemble` green

## 9. Static checks + grep-verifications updated

- [x] 9.1 If implementation surfaces new identifier shapes that the static grep patterns in Sections 9.6-9.10 below don't catch ... amend the grep patterns. NOT a self-rewrite directive — only update if implementation surfaces a real coverage gap — **amended**: added `network/**` + `di/**` to the §9.6 carve-out (HttpClientFactory + Koin assembly), and added §9.10a-d for the SecureTokenStore architectural assertions (covering 3.5a/3.5b); the §9.10b/c greps were made comment-aware (KDoc legitimately names the avoided APIs)
- [x] 9.2 Run `./gradlew ktlintCheck detekt` and resolve any ktlint / Detekt findings — `:mobile:app:ktlintCheck` + `:shared:resources:ktlintCheck` green (fixed: unused import, helper one-liner formatting via ktlintFormat, long MockRequestHandler type); project `detekt` green (covers JVM modules; mobile is ktlint-gated per the `nearyou.kotlin.multiplatform` convention)
- [x] 9.3 Run `./gradlew :backend:ktor:test :lint:detekt-rules:test` and confirm no regressions — both green (libs.versions.toml additions are new aliases only; existing backend aliases untouched)
- [x] 9.4 Run `./gradlew :mobile:app:assembleDebug` for each flavor: `assembleDevDebug`, `assembleStagingDebug`, `assembleProductionDebug` — all three SHOULD assemble — all three green
- [x] 9.5 Run `./gradlew :mobile:app:linkPodDebugFrameworkIosSimulatorArm64` to verify iOS framework link — green (requires a working `pod` on PATH per IMPLEMENTATION_NOTES.md)
- [x] 9.6 Run the auth-flow grep verification: `grep -rEi "SignIn|GoogleId|signin|googleSignIn|idToken|accessToken|refreshToken|authToken|JwtToken|Authenticator|oauthClient|loginClient|loginFlow" mobile/app/src/{commonMain,androidMain,iosMain}` and confirm every match resides under `auth/**`, `network/**`, `di/**`, `screens/auth/**`, `screens/routing/**`, or `config/**`; if a match leaks outside, either move it or treat as scope creep (`network/**` + `di/**` added to the carve-out — `network/**` for the shared `HttpClientFactory`, `di/**` for the Koin assembly modules; see the `mobile-app-scaffold` spec delta). Implementation surfaced two leaks (per §9.1): the App-level session-expiry observer referenced `SignInScreen` → extracted to `screens/routing/SessionExpiryEffect.kt` (keeps `App.kt` auth-identifier-free); the Koin modules name `GoogleSignInGateway`/`AuthRepository` → `di/**` carved out. Re-run is clean.
- [x] 9.7 Run the Apple-Sign-In grep verification: `grep -rEi "AppleAuth|appleSignIn|apple_sign_in|ASAuthorization" mobile/app/src/` — expect zero matches — **zero**
- [x] 9.8 Run the FCM grep verification: `grep -rEi "FirebaseMessaging|fcmToken|fcm_token|registerFcmToken|messaging\.token|pushToken|notificationToken" mobile/app/src/` — expect zero matches — **zero**
- [x] 9.9 Run the API-base-URL grep verification: `grep -rEi "nearyou\.id|api-staging|api\.nearyou" mobile/app/src/` — every match resides under `…/config/**` — **confirmed; all matches under config/**
- [x] 9.10 Run the token-leak grep: `grep -rEi 'Log\.(d|i|w|e)\([^)]*\b(access|refresh|id)Token\b|println\([^)]*\b(access|refresh|id)Token\b|print\([^)]*\b(access|refresh|id)Token\b|Sentry\.[A-Za-z]+\([^)]*\b(access|refresh|id)Token\b|NSLog\([^)]*\b(access|refresh|id)Token\b' mobile/app/src/` — expect zero matches — **zero** (the only logging is the AuthRepository `diagnosticLog` which carries Google-ceremony error messages / network causes, never tokens; the Ktor Logging plugin sanitizes Authorization to `***`)
- [x] 9.10a **Android Tink master key URI grep verification** (covers 3.5a). Confirm the canonical master-key URI string `android-keystore://nearyou_auth_tokens_master_key` appears in `mobile/app/src/androidMain/.../auth/SecureTokenStore.kt` (inline OR as the `MASTER_KEY_URI` const — the spec allows "the equivalent constant") AND `withMasterKeyUri(...)` is invoked. Run: `grep -rEc 'android-keystore://nearyou_auth_tokens_master_key' mobile/app/src/androidMain/kotlin/id/nearyou/app/auth/SecureTokenStore.kt` (expect ≥1) + `grep -rE 'withMasterKeyUri\(' …` (expect ≥1). Run `grep -rE 'setUserAuthenticationRequired\(true\)' mobile/app/src/` (excluding comment lines) — expect zero (the Tink keyset must not be locked behind biometric/lockscreen). **Result: const + call both present; zero `setUserAuthenticationRequired(true)` in code.**
- [x] 9.10b **Android EncryptedSharedPreferences negative grep** (covers 3.5a). Run on CODE lines only (exclude KDoc/comment mentions — the SecureTokenStore KDoc legitimately names the deprecated API to explain it is avoided): `grep -rn 'EncryptedSharedPreferences\|androidx\.security\.crypto' mobile/app/src/ | grep -vE ':\s*(\*|//)'` — expect zero. **Result: zero code references (only KDoc mentions).**
- [x] 9.10c **iOS Keychain `kSecAttrAccessGroup` negative grep** (covers 3.5b). Run on CODE lines only (the KDoc legitimately names it to document its intentional absence): `grep -rn 'kSecAttrAccessGroup' mobile/app/src/iosMain/ | grep -vE ':\s*(\*|//)'` — expect zero. **Result: zero code references (only KDoc mentions).**
- [x] 9.10d **iOS Keychain accessibility constant positive grep** (covers 3.5b). Run: `grep -rE 'kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly' mobile/app/src/iosMain/kotlin/id/nearyou/app/auth/` — expect ≥1 (the only acceptable constant; `kSecAttrAccessibleAlways` / `kSecAttrAccessibleAfterFirstUnlock`-without-`ThisDeviceOnly` are forbidden). **Result: 4 matches (KDoc + the `SecItemAdd` attribute).**
- [x] 9.11 Commit any straggler ktlint / Detekt / grep-verification fixes; do NOT commit `--no-verify` — diagnose root cause — committed (unused import, ktlintFormat reflow, MockRequestHandler typealias, App.kt session-expiry extraction to `screens/routing/SessionExpiryEffect.kt`)

## 10. Pre-archive staging deploy + manual smoke

- [ ] 10.1 Confirm backend `/api/v1/auth/signin`, `/api/v1/auth/refresh`, and `/.well-known/jwks.json` are reachable on staging (`curl -i https://api-staging.nearyou.id/health/live` should be 200)
- [ ] 10.2 Provision the staging Android OAuth client ID in Google Cloud Console under the `nearyou-staging` project — SHA-1 of debug + release Android signing certs + bundle identifier `id.nearyou.app.staging` (or equivalent flavor-suffixed ID)
- [ ] 10.3 Provision the staging iOS OAuth client ID in Google Cloud Console — bundle identifier `id.nearyou.app.staging` (or whatever the staging xcconfig sets)
- [ ] 10.4 Build + install the staging-flavored Android APK on a test device (`./gradlew :mobile:app:installStagingDebug`); tap "Masuk dengan Google"; complete the Google ceremony with a real Google account that DOES have an existing `users` row in staging Supabase (pre-seed via `dev/scripts/promote-staging-user.sh` or equivalent; pre-seed script MUST use a synthetic test Google account, NEVER commit the real Google email to the repo); verify the navigation lands on `HomeScreen`; kill + relaunch the app; verify it skips `SignInScreen` (token persisted)
- [ ] 10.4a **OS-reboot token persistence smoke (Android + iOS).** Post-successful-sign-in, fully reboot the device, launch the app — verify `RootRouterScreen` routes to `HomeScreen` (token still readable after Android Keystore + Tink reboot path / iOS Keychain after-first-unlock path). On Android, also verify the user does NOT need to unlock the device before launching for the routing to succeed (since `kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly` on iOS requires first-unlock; the Android Tink keyset path is post-unlock-by-default but verify)
- [ ] 10.4b **Uninstall + reinstall smoke (Android).** Fully uninstall the app (`adb uninstall id.nearyou.app.staging` or equivalent), reinstall the staging APK, launch — verify (a) `SignInScreen` renders (store cleared on uninstall via Android-Keystore-bound Tink master key destruction), (b) sign-in succeeds normally (Tink master key regenerated cleanly). Repeat the same uninstall + reinstall on iOS — note + document the observed Keychain-survival behavior (iOS Keychain entries can survive app reinstall by default for `kSecAttrService`-scoped items; if so, document whether the next sign-in correctly OVERWRITES the persisted token or whether the spec needs to add a "clear on app launch if first-launch detected" path)
- [ ] 10.4c **Theme-swap smoke.** Launch `SignInScreen` in light mode; toggle system to dark mode; verify brand logo redraws to `logo_brand_dark` AND the screen does not crash (per the spec scenario "SignInScreen brand logo swaps on system-theme change at recomposition")
- [ ] 10.5 Build + install the staging iOS app on a test device or simulator; repeat the sign-in flow; verify token persists across app relaunch
- [ ] 10.6 Banned-user smoke: in staging Supabase, set `users.is_banned = TRUE` for a test user; sign in with that user's Google account on the Android build; verify the response is 403 + the banner copy matches `signin_error_banned`; verify CTA is disabled
- [ ] 10.7 No-account smoke: sign in with a Google account that has NO row in staging `users`; verify the response is 404 + the banner copy matches `signin_error_no_account`; verify the user remains on `SignInScreen`
- [ ] 10.8 Network-failure smoke: enable airplane mode mid-flow; verify the banner copy matches `signin_error_network` + the CTA label changes to "Coba lagi"
- [ ] 10.9 Refresh-token rotation smoke: capture the access token + refresh token from a successful sign-in; manually expire the access token (or wait 15 min); make an authenticated request via the app (e.g., navigate to a screen that would call backend); verify backend `/api/v1/auth/refresh` is called once + the original request succeeds with a fresh token
- [ ] 10.10 Refresh-token-reuse smoke: capture refresh token from one device; use it on a second device; on the first device, the next refresh attempt should produce `token_reuse_detected` 401; verify the app handles this by clearing the store + routing to SignInScreen (per spec scenario "Refresh failure produces terminal 401 + store cleared")
- [ ] 10.11 Document smoke results: record token-version (claim parsed from JWT), Cloud Logging entries for the `/signin` + `/refresh` requests, and screenshots of each error state. Land notes inline in this `tasks.md` Section 10 OR in a `dev/notes/mobile-#3-smoke-2026-MM-DD.md` file
- [ ] 10.12 Tick checkboxes 10.1-10.11 individually in the apply commit OR in a follow-up commit "chore(mobile): mobile-#3 staging smoke complete"

## 11. FOLLOW_UPS.md additions + docs/03-UX-Design.md divergence flag

- [x] 11.1 Add `mobile-auth-signin-apple-ios` entry to `FOLLOW_UPS.md` — covers swapping iOS primary to Apple Sign-In per `docs/03-UX-Design.md` § Auth Flow line 38; references the user's "Google on both" decision at `/next-change` Phase A.4 as the trigger for the deferral
- [x] 11.2 Add `mobile-auth-signin-404-route-to-age-gate` entry — covers replacing the temporary `signin_error_no_account` copy with navigation to `AgeGateScreen` (the Mobile #4 outcome)
- [x] 11.3 Add `mobile-auth-signin-attestation-fingerprint-hash` entry — covers adding the `device_fingerprint_hash` body field when attestation lands per `docs/06-Security-Privacy.md` § Attestation
- [x] 11.4 Add `mobile-auth-signin-logout-wire-up` entry — covers wiring `POST /api/v1/auth/logout` + `/logout-all` when the Settings screen ships
- [x] 11.5 Add `mobile-auth-signin-credential-manager-legacy-fallback` entry — covers adding the deprecated `GoogleSignInClient` fallback for older Android devices where Credential Manager fails (per propose-time WebSearch finding); trigger is user reports / Sentry signal showing repeated `GoogleSignInResult.Failed` rate above threshold
- [x] 11.6 Add `docs-ios-primary-auth-mobile-3-vs-eventual-state` entry — covers amending TWO canonical docs to acknowledge Mobile #3 ships Google iOS as a substrate-proving stopgap before Apple Sign-In iOS lands: (a) [`docs/03-UX-Design.md`](../../../docs/03-UX-Design.md) § Auth Flow (the paragraph beginning `2. iOS: "Masuk dengan Apple" (primary, user-facing)`) and (b) [`docs/04-Architecture.md`](../../../docs/04-Architecture.md) § Tech Stack table (the row beginning `Auth | Google Sign-In (Android Credential Manager) + Apple Sign-In`). Both paragraphs are correct as the eventual-state but the staged delivery makes them temporarily misleading. The amendment should add a Mobile #3 status-tag note pointing to the eventual swap, NOT remove the Apple-iOS prescription
- [x] 11.7 Add `mobile-auth-signin-suspended-user-copy-split` entry — covers eventually differentiating `account_banned` (permanent) from `account_suspended` (temporary) at the backend `/signin` endpoint per `docs/03-UX-Design.md` § Suspension UX (which prescribes different copy: "Akun kamu dalam suspensi sementara sampai {date}." for temp vs. permanent "Akun kamu telah dinonaktifkan. Hubungi support jika ini keliru."). Backend currently emits `account_banned` for ANY `is_banned = TRUE` row without inspecting `suspended_until` — Mobile #3 ships the uniform permanent-ban copy as documented; this entry tracks the eventual backend differentiation + mobile copy split
- [x] 11.8 Commit "docs(follow-ups): Mobile #3 deferred work + UX-doc divergence + suspended-user copy split" — also: deleted the merged-superseded `compose-components-resources-dependency-cleanup` entry (safe mid-change deletion); added an 8th entry `mobile-auth-signin-android-instrumented-encryption-test` for the deferred §3.5 raw-byte-leak test; refreshed the intro audit note recording the transient over-budget (37) + the post-archive full-triage schedule (user-approved). FOLLOW_UPS is intentionally over the 30 limit until `/triage-follow-ups` runs post-archive.

## 12. PR refresh + archive prep

- [ ] 12.1 Update the PR description per `openspec/project.md` § "PR title and body MUST stay current at every phase boundary" — retitle the PR via `gh pr edit <pr> --title 'feat(mobile): mobile-auth-google-signin-flow'`; update body to "Status: implementation complete; ready for archive" listing the section-by-section progress, capability deltas, test counts, and pre-archive smoke results
- [ ] 12.2 Re-run `openspec validate mobile-auth-google-signin-flow --strict` after final implementation commits + confirm green
- [ ] 12.3 Pre-archive smoke per `openspec/project.md` § "Staging deploy timing": dispatch `gh workflow run deploy-staging.yml --ref mobile-auth-google-signin-flow` for ANY backend impact verification — Mobile #3 has no backend impact so this step is N/A (mark explicitly N/A in the archive commit body)
- [ ] 12.4 Post `/review` PR comment to trigger qodo review (Manual mode per `openspec/project.md` § Qodo dashboard prerequisite); address feedback iteratively per `/opsx:apply` step 8
- [ ] 12.5 Once all sections green, qodo blocking findings addressed, and pre-archive smoke recorded: invoke `/opsx:archive mobile-auth-google-signin-flow` to land the archive commit (moves `openspec/changes/mobile-auth-google-signin-flow/` under `archive/` + applies spec deltas to canonical `openspec/specs/**`)
- [ ] 12.6 Squash-merge the PR to `main` per the one-PR-per-change convention; verify the resulting single commit on `main` carries the full proposal + feat + archive history in the squash body
