<?xml version="1.0" encoding="utf-8"?>
<!--
  DEV-FLAVOR ONLY cleartext allowlist (merged via the dev manifest overlay; staging/production
  never see it). Permits http:// ONLY to the local-backend hosts — 10.0.2.2 (the emulator's
  host-loopback alias) and localhost/127.0.0.1 (a physical device through
  `adb reverse tcp:8080 tcp:8080`). Everything else stays cleartext-blocked, mirroring the iOS
  Info.plist NSAllowsLocalNetworking posture (local-only cleartext, never arbitrary hosts).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
