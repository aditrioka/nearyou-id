<?xml version="1.0" encoding="utf-8"?>
<!--
  STAGING-DEBUG ONLY manifest overlay. AGP merges this into the `stagingDebug` variant manifest
  only; `stagingRelease` and every production variant never see StagingTestLoginActivity. The
  exported deep-link entry is gated to the staging DEBUG build (operator-approved, 2026-06-08)
  because staging is internet-facing — keeping the bypass out of any staging *release* artifact.
  The backend RSA signature check is the real gate; this activity only writes a token. Full
  rationale + threat model: mobile/app/maestro/PHASE-3-staging-test-login.md.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <activity
            android:name="id.nearyou.app.staging.StagingTestLoginActivity"
            android:exported="true"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="nearyou-staging" android:host="test-login" />
            </intent-filter>
        </activity>
    </application>
</manifest>
