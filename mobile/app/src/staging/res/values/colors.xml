<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Staging launcher-icon background — burnt orange env tint ("pre-prod,
         caution"). Overrides androidMain's #1E4FD6 for the `staging` flavor
         only (AGP merges flavor res over androidMain). Foreground glyph,
         monochrome layer, adaptive-icon XML, and raster fallbacks are inherited
         unchanged from androidMain — only the background differs.
         See openspec change mobile-env-launcher-icons. -->
    <color name="ic_launcher_background">#C2410C</color>
</resources>
